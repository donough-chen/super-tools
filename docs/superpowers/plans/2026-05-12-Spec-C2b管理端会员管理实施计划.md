# Spec-C2b 管理端会员管理 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 super-tools-admin 实现 `/member` 模块的 3 个二级页面（stats/config/users）+ Tab 子页（等级/套餐/用户/积分流水），消费已实装的 11 个后端接口，提供会员配置管理 + 高敏运营动作（调整积分/等级/开通套餐）+ 统计仪表盘的完整后台运营能力。

**Architecture:** UmiJS 3 + Antd 5 + dva。services 薄封装 11 API；utils/memberFormat 纯函数；pages/Member 3 个独立页 + 共用 BenefitsField JSON 编辑器；Tab 模式 + URL 同步（?tab= ?userId=）；高敏写操作集中在 DetailDrawer 内 3 个独立 Form 实例 + Popconfirm 二次确认；测试用 .tsx + babel-jest（避开 ts-jest 26 + TS 5.4 兼容性问题）。

**Tech Stack:** TypeScript 5.4 / React 18 / UmiJS 3.5 / Antd 5.15 / dva 2.6 / Jest 26 / @testing-library/react 14

**Spec Reference:** `docs/superpowers/specs/2026-05-12-Spec-C2b管理端会员管理设计文档.md`

**Worktree:** `d:\Donough\Projects\super-tools-spec-c2b`（基于 master @ `07681c4`，分支 `spec-c2b`）— 已创建（含 admin/node_modules + admin/src/.umi + super-tool-node/node_modules 三个 junction）

---

## 任务总览

| Task | 范围 | 文件数 | 估行数 |
|---|---|---|---|
| T0 | worktree + junction（**已完成**） | 0 | 0 |
| T1 | 后端契约联调（关键 include 验证） | 0 | 0 |
| T2 | admin 路由模块填充（member.ts） | 1 改 | +20 |
| T3 | services/member.ts（11 API + 类型） | 1 新 | +250 |
| T4 | utils/memberFormat.ts | 1 新 | +50 |
| T5 | pages/Member/Stats（3 .tsx + 1 less） | 4 新 | +180 |
| T6 | pages/Member/Config 容器 + LevelsTab + LevelModal + BenefitsField | 4 新 + 1 less | +320 |
| T7 | pages/Member/Config PlansTab + PlanModal | 2 新 | +200 |
| T8 | pages/Member/Users 容器 + UsersTab | 2 新 | +250 |
| T9 | pages/Member/Users DetailDrawer + 3 个 Form | 4 新 | +400 |
| T10 | pages/Member/Users PointsLogsTab + index.less | 1 新 + 1 less | +180 |
| T11 | 测试 4 个 suites ≥ 13 case | 2 新 | +120 |
| T12 | type-check + jest 全跑 | - | - |
| T13 | 后端 PowerShell smoke + 前端浏览器验收 | - | - |
| T14 | spec + plan + 实施 commits 整合 + 合并 master | - | - |

**预期总量**：20 新建 + 1 修改 ≈ **1970 行实现 + 文档（spec 1205 + plan ~1100 = 2305 行）**

---

## Task 0: worktree + junction（已完成 ✅）

**Files:** 无（基础设施）

> 已在 brainstorming 收尾后创建：
> - worktree path: `d:\Donough\Projects\super-tools-spec-c2b`
> - branch: `spec-c2b`（基于 master `07681c4`）
> - 3 junctions: admin/node_modules + admin/src/.umi + super-tool-node/node_modules

跳过此 Task。

---

## Task 1: 后端契约联调（关键 include 验证）

**Spec:** §2 + §9.1 风险表

**Files:** 无（仅运行 + 记录字段名）

### Step 1: 确认主仓库后端在跑

```powershell
try { $r = Invoke-WebRequest -Uri 'http://localhost:7001' -TimeoutSec 3 -ErrorAction Stop; "alive: $($r.StatusCode)" }
catch { if ($_.Exception.Response) { "alive (4xx/5xx ok): $($_.Exception.Response.StatusCode.value__)" } else { "down" } }
```

如未跑：

```powershell
Set-Location 'd:\Donough\Projects\super-tools\super-tool-node'
npm run dev
# 或切到 worktree 跑（避免端口冲突，二选一）
```

### Step 2: 拿 admin token

```powershell
$body = '{"username":"admin","password":"Admin@123456","clientId":"web_client","clientSecret":"CHANGE_ME_WEB_SECRET"}'
$loginRes = Invoke-RestMethod -Uri "http://localhost:7001/api/auth/login" -Method POST -Body $body -ContentType "application/json"
$token = $loginRes.data.accessToken
$headers = @{ "Authorization" = "Bearer $token" }
```

### Step 3: 5 个关键 include 验证

```powershell
# (1) levels - 验证返回数组结构
"--- (1) /levels ---"
$r1 = Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/levels" -Headers $headers
$r1.data | ConvertTo-Json -Depth 3 | Select-Object -First 30

# (2) plans
"--- (2) /plans ---"
$r2 = Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/plans" -Headers $headers
$r2.data | ConvertTo-Json -Depth 3 | Select-Object -First 30

# (3) users 列表 - 关键：是否 include user/level
"--- (3) /users?page=1 ---"
$r3 = Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/users?page=1&pageSize=3" -Headers $headers
$r3.data.list[0] | ConvertTo-Json -Depth 4

# (4) users 详情 - 关键：是否 include currentPlan
"--- (4) /users/:id ---"
$firstUserId = $r3.data.list[0].userId
$r4 = Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/users/$firstUserId" -Headers $headers
$r4.data | ConvertTo-Json -Depth 4

# (5) stats 字段名验证
"--- (5) /stats ---"
$r5 = Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/stats" -Headers $headers
$r5.data | ConvertTo-Json -Depth 3

# (6) points-logs - 关键：是否 include user
"--- (6) /points-logs?page=1 ---"
$r6 = Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/points-logs?page=1&pageSize=3" -Headers $headers
$r6.data.list[0] | ConvertTo-Json -Depth 3
```

### Step 4: 记录验证结果

填写下表（写入 plan 注释或临时记录）：

```
[T1 联调记录]
(1) levels 返回结构 = data.____ (array / list)
(2) plans 返回结构 = data.____
(3) users.list[0].user 是否存在? ____ (是/否)
(3) users.list[0].level 是否存在? ____
(4) users/:id 详情 currentPlan 是否存在? ____
(5) stats 字段：totalMembers/paidMembers/paidRate/levelDistribution/todayNewMembers 是否齐全? ____
(6) points-logs.list[0].user 是否存在? ____
(6) points-logs.list[0].type 取值（0/1）确认 ____ (0=支出/1=收入 假设是否成立)
```

### Step 5: 根据结果调整 service 类型（如需要）

如发现差异：
- `users.list[0].user` 不存在 → service.ts 把 `user?` 标注为 optional（已是 optional，不改）；UI 列表渲染时兜底显 `#userId`
- `users/:id` 无 `currentPlan` → service.ts 删除 `currentPlan` 字段；UI 改用 `paidPlanCode` 字符串显示
- `points-logs` 无 `user` → 同上处理

如全部 include 都符合 spec § 2 假设，**T2 直接开工无需调整**。

### Step 6: T1 不 commit（仅验证）

---

## Task 2: admin 路由模块填充

**Spec:** §3.1

**Files:**
- Modify: `super-tools-admin/config/routes/modules/member.ts`

### Step 1: 写入路由

worktree 路径：`d:\Donough\Projects\super-tools-spec-c2b\super-tools-admin\config\routes\modules\member.ts`

```typescript
import type { IRoute } from 'umi';

/**
 * 会员管理路由（DB 顶级目录 member，type=1 已存在 — 见 008 迁移）
 * Spec-C2b 填充 3 二级页：
 * - /member/stats   权限码 member:stats:view
 * - /member/config  权限码 member:level:list / member:plan:list（Tab 内分别校验）
 * - /member/users   权限码 member:user:list / member:points:log:view（Tab 内分别校验）
 */
const memberRoutes: IRoute[] = [
  {
    path: '/member',
    routes: [
      { path: '/member', redirect: '/member/stats' },
      {
        path: '/member/stats',
        component: '@/pages/Member/Stats',
        wrappers: ['@/components/AuthWrapper'],
      },
      {
        path: '/member/config',
        component: '@/pages/Member/Config',
        wrappers: ['@/components/AuthWrapper'],
      },
      {
        path: '/member/users',
        component: '@/pages/Member/Users',
        wrappers: ['@/components/AuthWrapper'],
      },
    ],
  },
];

export default memberRoutes;
```

### Step 2: commit

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2b'
git add super-tools-admin/config/routes/modules/member.ts
git commit -m "feat(admin-routes): 填充 /member/{stats,config,users} 3 二级路由"
```

⚠️ tsc 此时会因 `@/pages/Member/Stats` 等不存在报错，**T2 不跑 tsc**，留待 T12 统一验证。

---

## Task 3: services/member.ts

**Spec:** §7.1

**Files:**
- New: `super-tools-admin/src/services/member.ts`

### Step 1: 写入完整 service

完整内容见 spec § 7.1（约 250 行），含：
- 7 个类型导出：`MemberLevel` / `MemberLevelUpdateDTO` / `PaidPlan` / `PaidPlanUpdateDTO` / `MemberUser` / `MemberUserListQuery` / `MemberStats` / `PointsLog` / `PointsLogsQuery`
- 11 个 API：listLevels / updateLevel / listPlans / updatePlan / listMemberUsers / getMemberUser / adjustPoints / adjustLevel / activatePlan / getMemberStats / listPointsLogs

⚠️ 类型字段须与 T1 联调结果对齐。

### Step 2: type-check + commit

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2b\super-tools-admin'
npx tsc --noEmit 2>&1 | Select-Object -Last 10
Set-Location ..
git add super-tools-admin/src/services/member.ts
git commit -m "feat(admin-svc): member service 封装（11 API + 完整类型）"
```

预期：tsc 0 错（service 不依赖任何 page 文件）。

---

## Task 4: utils/memberFormat.ts

**Spec:** §7.2

**Files:**
- New: `super-tools-admin/src/utils/memberFormat.ts`

### Step 1: 写入 utils

完整内容见 spec § 7.2，含：
- `formatCurrency(v)` 货币格式化
- `formatDuration(days)` 时长格式化（年/月/天）
- `levelColor(color, fallback)` 等级颜色降级
- `POINTS_TYPE_LABELS` / `POINTS_TYPE_COLORS` 常量

### Step 2: commit

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2b\super-tools-admin'
npx tsc --noEmit 2>&1 | Select-Object -Last 5
Set-Location ..
git add super-tools-admin/src/utils/memberFormat.ts
git commit -m "feat(admin-utils): memberFormat 货币/时长/积分类型映射"
```

---

## Task 5: pages/Member/Stats

**Spec:** §4

**Files:**
- New: `super-tools-admin/src/pages/Member/Stats/index.tsx`（约 70 行）
- New: `super-tools-admin/src/pages/Member/Stats/StatCards.tsx`（约 50 行）
- New: `super-tools-admin/src/pages/Member/Stats/LevelDistribution.tsx`（约 70 行）
- New: `super-tools-admin/src/pages/Member/Stats/index.less`（约 10 行）

### Step 1: `index.tsx` 主页

要点：
- props 无；自管理 state
- useEffect: `Promise.all([getMemberStats(), listLevels()])` 并行加载
- Card title="会员统计" 容器
- Spin loading 包裹
- 渲染 `<StatCards data={stats} />` + `<LevelDistribution stats={stats} levels={levels} />`

```tsx
import React, { useEffect, useState } from 'react';
import { Card, Spin } from 'antd';
import { getMemberStats, listLevels, MemberStats, MemberLevel } from '@/services/member';
import StatCards from './StatCards';
import LevelDistribution from './LevelDistribution';
import './index.less';

const StatsPage: React.FC = () => {
  const [stats, setStats] = useState<MemberStats | null>(null);
  const [levels, setLevels] = useState<MemberLevel[]>([]);
  const [loading, setLoading] = useState(false);

  useEffect(() => {
    setLoading(true);
    Promise.all([getMemberStats(), listLevels()])
      .then(([s, l]: any[]) => {
        if (s?.code === 200) setStats(s.data);
        if (l?.code === 200) setLevels(l.data || []);
      })
      .finally(() => setLoading(false));
  }, []);

  return (
    <Card title="会员统计" className="page-member-stats">
      <Spin spinning={loading}>
        <StatCards data={stats} />
        <div style={{ marginTop: 24 }}>
          <h3>等级分布</h3>
          <LevelDistribution stats={stats} levels={levels} />
        </div>
      </Spin>
    </Card>
  );
};

export default StatsPage;
```

### Step 2: `StatCards.tsx`

要点：
- props: `{ data: MemberStats | null }`
- 4 列 Row + Col + Card + Statistic
- 4 卡片：总会员 / 付费会员 / 付费率（% 后缀） / 今日新增（+ 前缀）
- data 为 null 时显示 0

### Step 3: `LevelDistribution.tsx`

要点：
- props: `{ stats: MemberStats | null; levels: MemberLevel[] }`
- 计算 totalCount = Σ levelDistribution
- 按 levels 排序（level ASC）渲染
- 每行：`<Tag color={lv.color}>{lv.name}</Tag>` + `<Progress percent={pct} strokeColor={lv.color} format={() => '${cnt} (${pct}%)'} />`
- 数据缺失（cnt=0）也展示行（一致性）

### Step 4: `index.less`

```less
.page-member-stats {
  .level-row {
    display: flex;
    align-items: center;
    margin-bottom: 8px;
    .level-name { width: 90px; flex-shrink: 0; }
    .ant-progress { flex: 1; }
  }
}
```

### Step 5: commit

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2b'
git add super-tools-admin/src/pages/Member/Stats/
git commit -m "feat(admin/member): /stats 页 — 4 数字卡片 + 等级分布 Progress"
```

---

## Task 6: pages/Member/Config 容器 + LevelsTab + LevelModal + BenefitsField

**Spec:** §5.1 / §5.2 / §5.3 / §5.4 / §5.5

**Files:**
- New: `super-tools-admin/src/pages/Member/Config/index.tsx`（约 50 行）
- New: `super-tools-admin/src/pages/Member/Config/BenefitsField.tsx`（约 60 行）
- New: `super-tools-admin/src/pages/Member/Config/LevelsTab.tsx`（约 130 行）
- New: `super-tools-admin/src/pages/Member/Config/LevelModal.tsx`（约 130 行）
- New: `super-tools-admin/src/pages/Member/Config/index.less`（约 15 行）

### Step 1: `index.tsx` 容器

```tsx
import React, { useState } from 'react';
import { Card, Tabs } from 'antd';
import LevelsTab from './LevelsTab';
import PlansTab from './PlansTab';  // T7 创建
import './index.less';

const ConfigPage: React.FC = () => {
  const [tab, setTab] = useState<'levels' | 'plans'>('levels');
  return (
    <Card title="会员配置" className="page-member-config">
      <Tabs activeKey={tab} onChange={(k) => setTab(k as any)}
        items={[
          { key: 'levels', label: '会员等级', children: <LevelsTab /> },
          { key: 'plans', label: '付费套餐', children: <PlansTab /> },
        ]}
      />
    </Card>
  );
};

export default ConfigPage;
```

⚠️ T6 阶段 `PlansTab` 还不存在，先建占位（T7 替换）：

```tsx
// PlansTab.tsx 占位
import React from 'react';
import { Empty } from 'antd';
const PlansTab: React.FC = () => <Empty description="付费套餐（T7 实现）" />;
export default PlansTab;
```

### Step 2: `BenefitsField.tsx` 自定义 Form 受控组件

完整内容见 spec § 5.5。要点：
- props `{ value?: object; onChange?: (v: object) => void }`
- 内部 state: text（JSON.stringify(value)）
- onChange: try parse → 成功调 onChange（object），失败显示红字 error
- TextArea 用 monospace 字体

### Step 3: `LevelsTab.tsx`

要点：
- 内部 state：list / loading / editing / modalVisible
- useEffect 拉 listLevels
- 行内 Switch 改 status 直接调 updateLevel
- 列定义见 spec § 5.3
- 操作列只有"编辑"（无删除）

```tsx
import React, { useEffect, useState, useMemo } from 'react';
import { Table, Button, Switch, Tag, Avatar, message, Space } from 'antd';
import { ReloadOutlined } from '@ant-design/icons';
import AuthButton from '@/components/AuthButton';
import { listLevels, updateLevel, MemberLevel } from '@/services/member';
import { formatCurrency } from '@/utils/memberFormat';
import { formatDateTime } from '@/utils/format';
import LevelModal from './LevelModal';

const LevelsTab: React.FC = () => {
  const [list, setList] = useState<MemberLevel[]>([]);
  const [loading, setLoading] = useState(false);
  const [editing, setEditing] = useState<MemberLevel | null>(null);
  const [modalVisible, setModalVisible] = useState(false);

  const fetch = async () => {
    setLoading(true);
    try {
      const res: any = await listLevels();
      if (res?.code === 200) setList(res.data || []);
    } finally { setLoading(false); }
  };
  useEffect(() => { fetch(); }, []);

  const handleStatus = async (row: MemberLevel, c: boolean) => {
    const res: any = await updateLevel(row.id, { status: c ? 1 : 0 });
    if (res?.code === 200) { message.success('已更新'); fetch(); }
    else message.error(res?.message || '更新失败');
  };

  const columns = useMemo(() => [
    { title: 'ID', dataIndex: 'id', width: 60 },
    { title: '等级', dataIndex: 'level', width: 70 },
    { title: '编码', dataIndex: 'code', width: 110, render: (v: string) => <Tag>{v}</Tag> },
    { title: '名称', dataIndex: 'name', width: 110 },
    { title: '图标', dataIndex: 'icon', width: 60,
      render: (v: string) => <Avatar src={v} size="small">{!v && '?'}</Avatar> },
    { title: '颜色', dataIndex: 'color', width: 110,
      render: (v: string) => v ? (
        <Space size={4}>
          <span style={{ display: 'inline-block', width: 14, height: 14, background: v, borderRadius: 2, border: '1px solid #ddd' }} />
          <code style={{ fontSize: 12 }}>{v}</code>
        </Space>
      ) : '-' },
    { title: '升级积分', dataIndex: 'upgradePoints', width: 100 },
    { title: '升级成长', dataIndex: 'upgradeGrowth', width: 100 },
    { title: '升级消费', dataIndex: 'upgradeConsume', width: 110, render: (v: any) => formatCurrency(v) },
    { title: '排序', dataIndex: 'sort', width: 70 },
    { title: '状态', dataIndex: 'status', width: 80,
      render: (v: number, row: MemberLevel) => (
        <AuthButton permCode="member:level:update">
          <Switch checked={v === 1} onChange={(c) => handleStatus(row, c)} />
        </AuthButton>
      ),
    },
    { title: '操作', width: 80, fixed: 'right' as const,
      render: (_: any, row: MemberLevel) => (
        <AuthButton permCode="member:level:update">
          <a onClick={() => { setEditing(row); setModalVisible(true); }}>编辑</a>
        </AuthButton>
      ),
    },
  ], []);

  return (
    <>
      <Space style={{ marginBottom: 16 }}>
        <Button icon={<ReloadOutlined />} onClick={fetch}>刷新</Button>
      </Space>
      <Table rowKey="id" columns={columns} dataSource={list}
        loading={loading} pagination={false} scroll={{ x: 1200 }} />
      <LevelModal
        visible={modalVisible}
        editing={editing}
        onClose={() => setModalVisible(false)}
        onSuccess={fetch}
      />
    </>
  );
};

export default LevelsTab;
```

### Step 4: `LevelModal.tsx`

要点：
- props: `{ visible, editing, onClose, onSuccess }`
- 字段表见 spec § 5.4（id/level/code disabled，benefits 用 BenefitsField）
- onOk: validateFields → updateLevel → message → onSuccess + onClose

### Step 5: `index.less`

```less
.page-member-config {
  .level-color-block {
    display: inline-block;
    width: 14px;
    height: 14px;
    border-radius: 2px;
    border: 1px solid #ddd;
    vertical-align: middle;
  }
}
```

### Step 6: commit

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2b'
git add super-tools-admin/src/pages/Member/Config/
git commit -m "feat(admin/member): /config 容器 + LevelsTab + LevelModal + BenefitsField"
```

---

## Task 7: pages/Member/Config — PlansTab + PlanModal

**Spec:** §5.6 / §5.7

**Files:**
- Modify (replace): `super-tools-admin/src/pages/Member/Config/PlansTab.tsx`（约 130 行）
- New: `super-tools-admin/src/pages/Member/Config/PlanModal.tsx`（约 130 行）

### Step 1: 替换 `PlansTab.tsx` 为完整实现

要点同 LevelsTab，差异：
- 列：编码/名称/时长(formatDuration)/价格(formatCurrency)/原价(划线 + formatCurrency)/赠送积分/赠送成长/排序/状态/操作
- service 改用 listPlans + updatePlan
- 价格列：`<span>¥199.00</span> <s style={{color: '#999'}}>¥299.00</s>`

### Step 2: `PlanModal.tsx`

字段表见 spec § 5.7。差异于 LevelModal：
- code 不可改
- price / originalPrice 用 InputNumber `precision={2}` `min={0}`
- giftPoints / giftGrowth 用 InputNumber `min={0}`
- durationDays InputNumber `min={0}` + 提示"0 表示永久"
- benefits 用 BenefitsField

### Step 3: commit

```powershell
git add super-tools-admin/src/pages/Member/Config/PlansTab.tsx super-tools-admin/src/pages/Member/Config/PlanModal.tsx
git commit -m "feat(admin/member): PlansTab + PlanModal 完整实现"
```

---

## Task 8: pages/Member/Users — 容器 + UsersTab

**Spec:** §6.1 / §6.2 / §6.3

**Files:**
- New: `super-tools-admin/src/pages/Member/Users/index.tsx`（约 70 行）
- New: `super-tools-admin/src/pages/Member/Users/UsersTab.tsx`（约 180 行）

### Step 1: `index.tsx` 容器（Tab + URL 同步）

完整代码见 spec § 6.2。要点：
- 从 URL search params 初始化 tab 和 userIdFilter
- onChange Tab 时用 history.replaceState 更新 URL
- DetailDrawer 内"查积分流水"按钮 → onJumpToLogs(userId) → setUserIdFilter + setTab('logs') + replaceState
- PointsLogsTab 占位先建：`const PointsLogsTab: React.FC<{initialUserId?: number}> = () => <Empty description="积分流水（T10 实现）" />`

### Step 2: `UsersTab.tsx`

要点：
- props: `{ onJumpToLogs?: (userId: number) => void }`
- 3 字段筛选栏：levelCode Select / isPaid Select / keyword Input
- levelCode 选项：useEffect 拉 listLevels 动态构造
- 列定义见 spec § 6.3.2
- 列表分页用 paginate helper 返回的 list/total
- 操作列只一个"详情"按钮 → setTarget + setDrawerVisible(true)
- DetailDrawer 占位（T9 替换为完整版）

```tsx
// DetailDrawer 占位（T9 替换）
// 文件：UsersTab.tsx 顶部临时定义 + 引用
const DetailDrawer: React.FC<{ visible: boolean; target: MemberUser | null; onClose: () => void; onSuccess: () => void; onJumpToLogs?: (uid: number) => void; }> = ({ visible, onClose }) => (
  <Drawer open={visible} onClose={onClose} title="会员详情（构建中）" width={720} />
);
```

### Step 3: commit

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2b'
git add super-tools-admin/src/pages/Member/Users/index.tsx super-tools-admin/src/pages/Member/Users/UsersTab.tsx
git commit -m "feat(admin/member): /users 容器（Tab + URL 同步）+ UsersTab 列表"
```

---

## Task 9: pages/Member/Users — DetailDrawer + 3 个 Form

**Spec:** §6.4 / §6.5 / §6.6 / §6.7

**Files:**
- New: `super-tools-admin/src/pages/Member/Users/DetailDrawer.tsx`（约 130 行）
- New: `super-tools-admin/src/pages/Member/Users/AdjustPointsForm.tsx`（约 80 行）
- New: `super-tools-admin/src/pages/Member/Users/AdjustLevelForm.tsx`（约 70 行）
- New: `super-tools-admin/src/pages/Member/Users/ActivatePlanForm.tsx`（约 70 行）

### Step 1: `DetailDrawer.tsx`

要点：
- props: `{ visible, target, onClose, onSuccess, onJumpToLogs? }`
- useEffect: visible + target 变化时调 getMemberUser
- 4 段 Collapse defaultActiveKey=['basic','points','level','plan']：
  - **basic**：Descriptions 展示用户/等级/积分/成长/累计消费/付费状态/套餐/到期时间 + "查积分流水"按钮（onClick: onJumpToLogs(userId) + onClose）
  - **points**：`<AdjustPointsForm userId={detail.userId} onSuccess={async () => { await refetch(); onSuccess(); }} />`
  - **level**：`<AdjustLevelForm userId={detail.userId} currentLevelId={detail.levelId} onSuccess={...} />`
  - **plan**：`<ActivatePlanForm userId={detail.userId} onSuccess={...} />`

### Step 2: `AdjustPointsForm.tsx`

完整内容见 spec § 6.5。要点：
- 独立 `Form.useForm()` 实例
- points / growthDelta / remark 3 字段
- remark 必填（min 1, max 200）
- 提交按钮包 Popconfirm "确认调整？此操作会写入审计日志"
- 调用 adjustPoints → 成功后 form.resetFields() + onSuccess

### Step 3: `AdjustLevelForm.tsx`

完整内容见 spec § 6.6。要点：
- props: `{ userId; currentLevelId; onSuccess }`
- useEffect 拉 listLevels
- Select 显示所有 status===1 等级，标记 disabled if status===0
- initialValue=currentLevelId（提示用户当前是哪一级）

### Step 4: `ActivatePlanForm.tsx`

完整内容见 spec § 6.7。要点：
- 拉 listPlans，filter status===1
- Select label 含 `${name} (${durationDays}天 / ¥${price})`
- Alert info "开通后将立即生效，赠送对应积分/成长值"

### Step 5: 替换 UsersTab 中的 DetailDrawer 占位

```tsx
// UsersTab.tsx 顶部
import DetailDrawer from './DetailDrawer';
// 删除占位定义
```

### Step 6: commit

```powershell
git add super-tools-admin/src/pages/Member/Users/DetailDrawer.tsx \
        super-tools-admin/src/pages/Member/Users/AdjustPointsForm.tsx \
        super-tools-admin/src/pages/Member/Users/AdjustLevelForm.tsx \
        super-tools-admin/src/pages/Member/Users/ActivatePlanForm.tsx \
        super-tools-admin/src/pages/Member/Users/UsersTab.tsx
git commit -m "feat(admin/member): DetailDrawer 4 段折叠 + 3 个独立 Form 实例（积分/等级/套餐）"
```

---

## Task 10: pages/Member/Users — PointsLogsTab + index.less

**Spec:** §6.8

**Files:**
- New: `super-tools-admin/src/pages/Member/Users/PointsLogsTab.tsx`（约 150 行）
- New: `super-tools-admin/src/pages/Member/Users/index.less`（约 15 行）
- Modify: `super-tools-admin/src/pages/Member/Users/index.tsx`（替换 PointsLogsTab 占位）

### Step 1: `PointsLogsTab.tsx`

要点：
- props: `{ initialUserId?: number }`
- 5 字段筛选：userId InputNumber（initialValue=initialUserId）/ type Select / source Input / 时间范围 RangePicker
- 列定义见 spec § 6.8.2
- type 列用 POINTS_TYPE_LABELS + POINTS_TYPE_COLORS
- points 列：`<span style={{ color: v >= 0 ? 'green' : 'red' }}>{v >= 0 ? '+' : ''}{v}</span>`
- 标准分页

```tsx
import React, { useEffect, useMemo, useState } from 'react';
import {
  Table, InputNumber, Select, Input, DatePicker, Form, Button, Space, Tag,
} from 'antd';
import { ReloadOutlined } from '@ant-design/icons';
import { listPointsLogs, PointsLog, PointsLogsQuery } from '@/services/member';
import { formatDateTime } from '@/utils/format';
import { POINTS_TYPE_LABELS, POINTS_TYPE_COLORS } from '@/utils/memberFormat';

const { RangePicker } = DatePicker;

interface Props {
  initialUserId?: number;
}

const PointsLogsTab: React.FC<Props> = ({ initialUserId }) => {
  const [data, setData] = useState<{ list: PointsLog[]; total: number }>({ list: [], total: 0 });
  const [loading, setLoading] = useState(false);
  const [page, setPage] = useState(1);
  const [pageSize, setPageSize] = useState(20);
  const [filters, setFilters] = useState<PointsLogsQuery>(
    initialUserId ? { userId: initialUserId } : {}
  );
  const [form] = Form.useForm();

  useEffect(() => {
    if (initialUserId) form.setFieldsValue({ userId: initialUserId });
  }, [initialUserId, form]);

  const fetch = async () => {
    setLoading(true);
    try {
      const res: any = await listPointsLogs({ page, pageSize, ...filters });
      if (res?.code === 200 && res.data) {
        setData({ list: res.data.list || [], total: res.data.total || 0 });
      }
    } finally { setLoading(false); }
  };

  useEffect(() => {
    fetch();
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [page, pageSize, filters]);

  const columns = useMemo(() => [
    { title: 'ID', dataIndex: 'id', width: 80 },
    { title: '时间', dataIndex: 'createdAt', width: 160, render: (v: string) => formatDateTime(v) },
    { title: '用户', dataIndex: 'userId', width: 120,
      render: (uid: number, row: PointsLog) =>
        row.user ? `${row.user.username}(#${uid})` : `#${uid}` },
    { title: '类型', dataIndex: 'type', width: 80,
      render: (v: number) => <Tag color={POINTS_TYPE_COLORS[v]}>{POINTS_TYPE_LABELS[v] || v}</Tag> },
    { title: '来源', dataIndex: 'source', width: 110, render: (v: string) => <Tag>{v}</Tag> },
    { title: '积分', dataIndex: 'points', width: 100,
      render: (v: number) => (
        <span style={{ color: v >= 0 ? '#52c41a' : '#ff4d4f', fontWeight: 500 }}>
          {v >= 0 ? '+' : ''}{v}
        </span>
      ) },
    { title: '余额', dataIndex: 'balance', width: 100 },
    { title: '成长变动', dataIndex: 'growthDelta', width: 100, render: (v: number) => v || '-' },
    { title: '备注', dataIndex: 'remark', ellipsis: true, render: (v: string) => v || '-' },
  ], []);

  return (
    <>
      <Form form={form} layout="inline" style={{ marginBottom: 16 }}
        onFinish={(v) => {
          setPage(1);
          setFilters({
            userId: v.userId,
            type: v.type,
            source: v.source || undefined,
            startDate: v.range?.[0]?.toISOString(),
            endDate: v.range?.[1]?.toISOString(),
          });
        }}
        onReset={() => { setPage(1); setFilters({}); }}
      >
        <Form.Item name="userId">
          <InputNumber placeholder="用户 ID" min={1} style={{ width: 120 }} />
        </Form.Item>
        <Form.Item name="type">
          <Select placeholder="类型" allowClear style={{ width: 100 }}
            options={[{ value: 1, label: '收入' }, { value: 0, label: '支出' }]} />
        </Form.Item>
        <Form.Item name="source">
          <Input placeholder="来源" allowClear style={{ width: 110 }} />
        </Form.Item>
        <Form.Item name="range">
          <RangePicker showTime />
        </Form.Item>
        <Form.Item>
          <Space>
            <Button type="primary" htmlType="submit">查询</Button>
            <Button htmlType="reset">重置</Button>
          </Space>
        </Form.Item>
      </Form>

      <Space style={{ marginBottom: 16 }}>
        <Button icon={<ReloadOutlined />} onClick={fetch}>刷新</Button>
      </Space>

      <Table
        rowKey="id" columns={columns} dataSource={data.list} loading={loading}
        pagination={{
          current: page, pageSize, total: data.total,
          showSizeChanger: true,
          showTotal: (t) => `共 ${t} 条`,
          onChange: (p, ps) => { setPage(p); setPageSize(ps); },
        }}
        scroll={{ x: 1100 }}
      />
    </>
  );
};

export default PointsLogsTab;
```

### Step 2: `index.less`

```less
.page-member-users {
  // 占位，目前无特殊样式
}
```

### Step 3: 替换 index.tsx 中的 PointsLogsTab 占位

```tsx
// pages/Member/Users/index.tsx
import PointsLogsTab from './PointsLogsTab';
// 删除占位定义
```

### Step 4: commit

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2b'
git add super-tools-admin/src/pages/Member/Users/PointsLogsTab.tsx \
        super-tools-admin/src/pages/Member/Users/index.tsx \
        super-tools-admin/src/pages/Member/Users/index.less
git commit -m "feat(admin/member): PointsLogsTab + index.less + 替换 index.tsx 占位"
```

---

## Task 11: 测试 4 个 suites ≥ 13 case

**Spec:** §8

**Files:**
- New: `super-tools-admin/src/services/__tests__/member.test.tsx`（约 90 行）
- New: `super-tools-admin/src/utils/__tests__/memberFormat.test.tsx`（约 50 行）

### Step 1: `member.test.tsx`（11 case）

11 个 API 各 1 case。固定文件头注释（与 C1/C2a 一致）：

```typescript
/**
 * member service 测试
 *
 * 用 .tsx 扩展名走 babel-jest（jest config 已配置），
 * 避开 ts-jest 26 + TypeScript 5.4 的 hoist-jest `getMutableClone` bug。
 */
```

完整 11 case 模板：

```typescript
import {
  listLevels, updateLevel, listPlans, updatePlan,
  listMemberUsers, getMemberUser, adjustPoints, adjustLevel, activatePlan,
  getMemberStats, listPointsLogs,
} from '@/services/member';

jest.mock('@/utils/request');
import request from '@/utils/request';

describe('member service', () => {
  beforeEach(() => jest.clearAllMocks());

  it('listLevels → GET /api/admin/member/levels', async () => {
    await listLevels();
    expect(request).toHaveBeenCalledWith('/api/admin/member/levels');
  });

  it('updateLevel → PUT /levels/:id + body', async () => {
    await updateLevel(2, { name: '白银', sort: 10 });
    expect(request).toHaveBeenCalledWith('/api/admin/member/levels/2', {
      method: 'PUT', data: { name: '白银', sort: 10 },
    });
  });

  it('listPlans → GET /plans', async () => {
    await listPlans();
    expect(request).toHaveBeenCalledWith('/api/admin/member/plans');
  });

  it('updatePlan → PUT /plans/:id + body', async () => {
    await updatePlan(3, { price: 99 });
    expect(request).toHaveBeenCalledWith('/api/admin/member/plans/3', {
      method: 'PUT', data: { price: 99 },
    });
  });

  it('listMemberUsers → GET /users + params', async () => {
    await listMemberUsers({ page: 1, pageSize: 20, levelCode: 'silver' });
    expect(request).toHaveBeenCalledWith('/api/admin/member/users', {
      params: { page: 1, pageSize: 20, levelCode: 'silver' },
    });
  });

  it('getMemberUser → GET /users/:id', async () => {
    await getMemberUser(7);
    expect(request).toHaveBeenCalledWith('/api/admin/member/users/7');
  });

  it('adjustPoints → POST /users/:id/adjust-points + body', async () => {
    await adjustPoints(7, 100, 0, '双 11 补偿');
    expect(request).toHaveBeenCalledWith('/api/admin/member/users/7/adjust-points', {
      method: 'POST',
      data: { points: 100, growthDelta: 0, remark: '双 11 补偿' },
    });
  });

  it('adjustLevel → PUT /users/:id/level + body { levelId }', async () => {
    await adjustLevel(7, 3);
    expect(request).toHaveBeenCalledWith('/api/admin/member/users/7/level', {
      method: 'PUT', data: { levelId: 3 },
    });
  });

  it('activatePlan → POST /users/:id/activate-plan + body { planCode }', async () => {
    await activatePlan(7, 'monthly');
    expect(request).toHaveBeenCalledWith('/api/admin/member/users/7/activate-plan', {
      method: 'POST', data: { planCode: 'monthly' },
    });
  });

  it('getMemberStats → GET /stats', async () => {
    await getMemberStats();
    expect(request).toHaveBeenCalledWith('/api/admin/member/stats');
  });

  it('listPointsLogs → GET /points-logs + params', async () => {
    await listPointsLogs({ page: 1, userId: 5, type: 1 });
    expect(request).toHaveBeenCalledWith('/api/admin/member/points-logs', {
      params: { page: 1, userId: 5, type: 1 },
    });
  });
});
```

### Step 2: `memberFormat.test.tsx`（4-5 case）

```typescript
import {
  formatCurrency, formatDuration, levelColor,
  POINTS_TYPE_LABELS, POINTS_TYPE_COLORS,
} from '@/utils/memberFormat';

describe('memberFormat utils', () => {
  it('formatCurrency: string DECIMAL → ¥xxx.xx', () => {
    expect(formatCurrency('199.5')).toBe('¥199.50');
    expect(formatCurrency(0)).toBe('¥0.00');
    expect(formatCurrency(null)).toBe('-');
    expect(formatCurrency(undefined)).toBe('-');
    expect(formatCurrency('abc')).toBe('-');
  });

  it('formatDuration', () => {
    expect(formatDuration(0)).toBe('永久');
    expect(formatDuration(7)).toBe('7 天');
    expect(formatDuration(30)).toBe('1 月');
    expect(formatDuration(365)).toBe('1 年');
    expect(formatDuration(60)).toBe('2 月');
  });

  it('levelColor 降级', () => {
    expect(levelColor('#abc')).toBe('#abc');
    expect(levelColor()).toBe('#999');
    expect(levelColor(undefined, '#000')).toBe('#000');
  });

  it('POINTS_TYPE labels & colors', () => {
    expect(POINTS_TYPE_LABELS[0]).toBe('支出');
    expect(POINTS_TYPE_LABELS[1]).toBe('收入');
    expect(POINTS_TYPE_COLORS[0]).toBe('red');
    expect(POINTS_TYPE_COLORS[1]).toBe('green');
  });
});
```

### Step 3: 跑测试

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2b\super-tools-admin'
npx jest src/services/__tests__/member.test.tsx src/utils/__tests__/memberFormat.test.tsx --no-coverage
```

预期：2 suites 全 PASS，≥ 14 case（11 + 4）。

### Step 4: commit

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2b'
git add super-tools-admin/src/services/__tests__/member.test.tsx \
        super-tools-admin/src/utils/__tests__/memberFormat.test.tsx
git commit -m "test(admin/c2b): member service + memberFormat utils 2 suites ≥ 14 case PASS"
```

---

## Task 12: type-check + jest 全跑

**Files:** 无（仅运行）

### Step 1: 前端 tsc

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2b\super-tools-admin'
npx tsc --noEmit
```

预期：0 错。如出现 useSelector / useDispatch / history 报错，重建 src/.umi junction（参考 C2a）。

### Step 2: 前端 jest 全量

```powershell
npx jest --no-coverage
```

预期：C2b 新增 2 suites 全 PASS；总体除去预先已 fail 的 auth.test.ts 全绿。

### Step 3: 如有失败，定位修复

修完后回 Step 1 重跑。

⚠️ 不修复预先存在的失败（auth.test.ts），与 C2a 同原则。

---

## Task 13: 后端 PowerShell smoke + 前端浏览器验收

**Spec:** §8.5

**Files:** 无（仅运行 + 浏览器点击）

### Step 1: 启动 worktree dev（后端 + admin）

```powershell
# 终端 1
Set-Location 'd:\Donough\Projects\super-tools-spec-c2b\super-tool-node'
npm run dev

# 终端 2
Set-Location 'd:\Donough\Projects\super-tools-spec-c2b\super-tools-admin'
npm start
```

⚠️ 主仓库 dev 需关闭避免端口冲突。

### Step 2: 后端 PowerShell smoke（11 case）

```powershell
$body = '{"username":"admin","password":"Admin@123456","clientId":"web_client","clientSecret":"CHANGE_ME_WEB_SECRET"}'
$loginRes = Invoke-RestMethod -Uri "http://localhost:7001/api/auth/login" -Method POST -Body $body -ContentType "application/json"
$token = $loginRes.data.accessToken
$headers = @{ "Authorization" = "Bearer $token" }

"--- (1) /levels ---"
(Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/levels" -Headers $headers).data.Count

"--- (2) /levels/2 update sort ---"
$lvBefore = (Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/levels" -Headers $headers).data | Where-Object { $_.id -eq 2 } | Select-Object -First 1
$origSort = $lvBefore.sort
Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/levels/2" -Method PUT -Headers $headers -Body "{`"sort`":$($origSort+1)}" -ContentType "application/json" | Out-Null
"updated"
# 还原
Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/levels/2" -Method PUT -Headers $headers -Body "{`"sort`":$origSort}" -ContentType "application/json" | Out-Null

"--- (3) /plans ---"
(Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/plans" -Headers $headers).data.Count

"--- (4) /users ---"
$users = Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/users?page=1&pageSize=3" -Headers $headers
"users.list count: $($users.data.list.Count), total: $($users.data.total)"

"--- (5) /users/:id detail ---"
$firstUid = $users.data.list[0].userId
(Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/users/$firstUid" -Headers $headers).data | ConvertTo-Json -Depth 2 | Select-Object -First 20

"--- (6) /users/:id/adjust-points 不传 remark（应 400）---"
try {
  Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/users/$firstUid/adjust-points" -Method POST -Headers $headers -Body '{"points":1}' -ContentType "application/json"
} catch { $_.ErrorDetails.Message }

"--- (7) /users/:id/adjust-points +1 with remark ---"
Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/users/$firstUid/adjust-points" -Method POST -Headers $headers -Body '{"points":1,"growthDelta":0,"remark":"smoke 测试"}' -ContentType "application/json" | Out-Null
# 还原 -1
Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/users/$firstUid/adjust-points" -Method POST -Headers $headers -Body '{"points":-1,"growthDelta":0,"remark":"smoke 还原"}' -ContentType "application/json" | Out-Null
"adjust ok + 还原"

"--- (8) /users/:id/level levelId=999999（应 404）---"
try {
  Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/users/$firstUid/level" -Method PUT -Headers $headers -Body '{"levelId":999999}' -ContentType "application/json"
} catch { $_.ErrorDetails.Message }

"--- (9) /users/:id/activate-plan planCode=不存在（应 4xx）---"
try {
  Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/users/$firstUid/activate-plan" -Method POST -Headers $headers -Body '{"planCode":"not_exist"}' -ContentType "application/json"
} catch { $_.ErrorDetails.Message }

"--- (10) /stats ---"
(Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/stats" -Headers $headers).data

"--- (11) /points-logs ---"
$logs = Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/points-logs?page=1&pageSize=5" -Headers $headers
"logs.list count: $($logs.data.list.Count), total: $($logs.data.total)"
```

### Step 3: 前端浏览器验收（清单见 spec § 8.5 前端）

打开 http://localhost:8000（或项目实际端口），登录 super_admin。

按 spec § 8.5 三大模块（stats/config/users）逐项 check。重点项：
- benefits JSON 输入合法 + 非法的差异提示
- 调整积分不填 remark 的拦截
- URL 同步：抽屉"查积分流水" → URL 变化 → 流水筛选预填
- 切回 users Tab → URL 还原

### Step 4: 不 commit（仅验证）

如发现 bug，记录到 follow-up 段，按需修复后回 T8-T10 之一重做。

---

## Task 14: spec + plan + 实施 commits 整合 + 合并 master

**Files:** 无（仅 git）

### Step 1: 在 worktree 内 commit spec + plan

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2b'
git add docs/superpowers/specs/2026-05-12-Spec-C2b管理端会员管理设计文档.md \
        docs/superpowers/plans/2026-05-12-Spec-C2b管理端会员管理实施计划.md
git commit -m "docs(spec-c2b): 设计文档 + 实施计划"
```

### Step 2: 检查 commit 链

```powershell
git log --oneline master..spec-c2b
```

预期：约 11-13 commits（docs + 路由 + service + utils + stats + config x2 + users x3 + tests = 10-11 + docs）。

### Step 3: 切回主仓库 + --no-ff 合并

```powershell
Set-Location 'd:\Donough\Projects\super-tools'
git status --short    # 须干净
git checkout master
git merge spec-c2b --no-ff -m "Merge branch 'spec-c2b' into master" `
  -m "Spec-C2b 管理端会员管理（纯前端）" `
  -m "- /member/stats（统计仪表盘：4 卡片 + 等级分布 Progress）" `
  -m "- /member/config（Tab：等级 / 套餐，含 BenefitsField JSON 编辑器）" `
  -m "- /member/users（Tab：用户 / 流水，含 4 段抽屉 + 3 个独立 Form）" `
  -m "- 11 后端接口消费 + 高敏写操作 Popconfirm 二次确认 + 审计后端已埋点" `
  -m "- URL 同步 ?tab= ?userId= 跨 Tab 跳转预填" `
  -m "- 测试 ≥ 14 case PASS（service 11 + utils 4）"
```

### Step 4: 主仓库 tsc 兜底

```powershell
Set-Location 'super-tools-admin'
npx tsc --noEmit
```

预期：0 错。

### Step 5: 清理 worktree

```powershell
Set-Location 'd:\Donough\Projects\super-tools'
git worktree remove ../super-tools-spec-c2b --force 2>&1 | Out-String
git worktree prune
git branch -d spec-c2b
git worktree list
git branch -a
```

⚠️ Windows 文件锁可能导致物理目录残留，与 A1/A2/C1/C2a 同款问题，不阻断。

### Step 6: push

```powershell
git push origin master
```

### Step 7: 最终核对

- [ ] master HEAD 含 Spec-C2b 全部 commits
- [ ] 主仓库 admin 含 Member 3 页 + service + utils + tests
- [ ] 远端 master 已推送
- [ ] worktree git 元数据已清

---

## 收官检查清单

### 必达项

- [ ] T1：后端契约联调通过（11 接口字段名记录）
- [ ] T2：admin 路由模块填充
- [ ] T3：services/member.ts（11 API + 类型）
- [ ] T4：utils/memberFormat.ts
- [ ] T5：/member/stats 页面（StatCards + LevelDistribution）
- [ ] T6-T7：/member/config 页面（Tab + LevelsTab/LevelModal + PlansTab/PlanModal + BenefitsField）
- [ ] T8-T10：/member/users 页面（Tab 容器 + UsersTab + DetailDrawer + 3 Form + PointsLogsTab）
- [ ] T11：≥ 14 case PASS（≥ 13 目标已超）
- [ ] T12：tsc 0 错
- [ ] T13：后端 11 case smoke + 前端 4 大类浏览器验收
- [ ] T14：spec-c2b 已合并 master + 推送远端

### 加分项

- [ ] 调整积分时显示当前余额 + 调整后预览
- [ ] benefits JSON 编辑器加"格式化"按钮
- [ ] 等级分布按 cnt DESC 排序

### 不做项（明确）

- 等级 / 套餐 增删功能
- 图表库引入
- C 端会员中心改动
- 支付集成

---

## 风险与回滚

### 关键风险

| 风险 | 触发条件 | 缓解 |
|---|---|---|
| getMemberUsers 不 include user/level | T1 联调发现 | service 类型改 optional + UI fallback |
| BenefitsField 嵌套对象编辑被误删 | 用户手误 | 实时校验 + 取消按钮回退 |
| URL 同步与 dva history push 冲突 | 不一致 | 用 history.replaceState 不入栈 |
| 抽屉内 3 个 Form 共存 状态串扰 | useForm 实例混用 | 每个 Form 独立 useForm() |
| ts-jest 26 + TS 5.4 测试失败 | .ts 文件 | 严格用 .tsx 走 babel-jest |
| levels 数据序对 sort 不稳定 | 显示乱序 | UI 显式按 level ASC 排序 |
| Windows 文件锁阻止 worktree 删除 | T14 git worktree remove | 等同 C1/C2a：git prune + 物理目录手动清 |

### 回滚策略

- **前端**：`git revert <merge-commit>`，无 DB 状态需回滚
- **半路撤回**：worktree 内 `git reset --hard master` 重来

---

**结尾**：plan 待 review 通过后即可进入 T1 执行。建议执行模式 = 主 agent 直跑（spec § Q11=2，C2a 已验证）。
