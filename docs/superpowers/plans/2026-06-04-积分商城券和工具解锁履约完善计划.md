# 积分商城 — coupon / tool_unlock 履约完善计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** 完善积分商城虚拟商品履约链路：coupon 可实际使用、tool_unlock 真正解锁工具权限

**Background:**
- `fulfillVirtual()` 对 `coupon` / `tool_unlock` 只返回内存对象，未持久化
- 用户兑换后"看不见、用不了"兑现的权益

**Tech Stack:** Egg.js, Sequelize, MySQL, React, Zustand, TypeScript

---

## Task 1: 数据库迁移 — 新建两张表 ✅

**Files:**
- Create: `super-tool-node/database/028_user_coupons_and_unlocks.sql` ✅
- Modify: `super-tool-node/app/model/user_coupon.ts` (新建) ✅
- Modify: `super-tool-node/app/model/user_tool_unlock.ts` (新建) ✅

- [x] **Step 1: 创建数据库迁移脚本** ✅
- [x] **Step 2: 创建 Sequelize 模型** ✅
- [x] **Step 3: 执行迁移并验证** ✅

---

## Task 2: 后端 — 完善 fulfillVirtual 持久化 ✅

**Files:**
- Modify: `super-tool-node/app/service/pointsMall.ts` (`fulfillVirtual` 方法) ✅
- Modify: `super-tool-node/app/model/index.ts` (注册新模型) ✅

- [x] **Step 1: 修改 coupon 履约 — 写入 user_coupons 表** ✅
- [x] **Step 2: 修改 tool_unlock 履约 — 写入 user_tool_unlocks 表** ✅
- [x] **Step 3: 调整 exchange 事务中 fulfillVirtual 调用顺序** ✅

---

## Task 3: 后端 — coupon 使用接口 + tool 权限校验 ✅

**Files:**
- Create: `super-tool-node/app/controller/userCoupon.ts` (新) ✅
- Create: `super-tool-node/app/service/userCoupon.ts` (新) ✅
- Modify: `super-tool-node/app/service/tool.ts` (`checkToolAccess` 方法) ✅
- Modify: `super-tool-node/app/router.ts` (注册路由) ✅

- [x] **Step 1: 创建 UserCouponService — 券的使用逻辑** ✅
- [x] **Step 2: 修改 tool.checkToolAccess — 增加 tool_unlock 校验** ✅
- [x] **Step 3: 注册路由** ✅

---

## Task 4: 前端 — 券包页 + 工具解锁状态展示 ✅

**Files:**
- Create: `super-tools-web/packages/h5/micro-tools/pages/points-mall/coupons/` (新页面) ✅
- Modify: `super-tools-web/packages/h5/micro-tools/pages/points-mall/index.tsx` (增加"我的券"入口) ✅
- Modify: `super-tools-web/packages/h5/micro-tools/service/pointsMall.ts` (增加券 API) ✅
- Modify: `super-tools-web/packages/h5/micro-tools/store/pointsMall.ts` (增加券 store) ✅

- [x] **Step 1: 前端 API — 获取我的券列表** ✅
- [x] **Step 2: 券包页面** ✅
- [x] **Step 3: 积分商城首页增加"我的券"入口** ✅

---

## Task 5: 前端 — 工具解锁状态展示 ✅

**Files:**
- Create: `super-tools-web/packages/h5/micro-tools/pages/tools/[code].tsx` (工具详情页) ✅
- Create: `super-tools-web/packages/h5/micro-tools/pages/tools/[code].less` (样式) ✅
- Modify: `super-tools-web/packages/h5/micro-tools/routes.config.ts` (添加路由) ✅

- [x] **Step 1: 工具卡片展示解锁状态** ✅
- [x] **Step 2: 工具详情页展示解锁状态 + 解锁按钮** ✅

---

## Task 6: 集成测试 🔲

- [ ] **Step 1: 测试 coupon 履约**
  1. 用 silver 账号兑换"满5减1券"
  2. 查 `user_coupons` 表确认写入
  3. 调用 `GET /api/user/coupons` 确认返回
  4. 测试使用券（模拟下单）

- [ ] **Step 2: 测试 tool_unlock 履约**
  1. 兑换"JSON Pro 7天解锁"
  2. 查 `user_tool_unlocks` 表确认写入
  3. 调用 `checkToolAccess` 接口确认返回 allowed=true
  4. 过期后确认 allowed=false

- [ ] **Step 3: 边界情况**
  - 券过期后 status 自动变 expired
  - tool_unlock 过期后 status 变 expired
  - 重复兑换同一 tool_unlock（应该允许，产生新记录）

---

## 部署检查清单 🔲

- [ ] 执行 `028_user_coupons_and_unlocks.sql`
- [ ] 重启后端服务
- [ ] 验证 coupon 兑换后 `user_coupons` 有记录
- [ ] 验证 tool_unlock 兑换后 `user_tool_unlocks` 有记录
- [ ] 验证 `checkToolAccess` 对解锁用户返回 allowed=true
- [ ] 前端券包页可正常展示
- [ ] 提交代码并创建 PR

---

**Plan complete.** 

**总结：**
- ✅ Task 1-5 已完成
- 🔲 Task 6 (集成测试) 待执行
- 🔲 部署检查清单待完成

**已完成的功能：**
1. 数据库表创建（user_coupons, user_tool_unlocks）
2. 后端履约逻辑完善（fulfillVirtual 持久化）
3. 券的使用接口和工具权限校验
4. 前端券包页展示
5. 工具详情页和解锁状态展示
6. 清理临时脚本

**下一步：**
执行集成测试，验证所有功能正常工作，然后部署上线。
