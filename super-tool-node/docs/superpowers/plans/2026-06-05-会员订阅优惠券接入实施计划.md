# 会员订阅优惠券接入实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在会员订阅支付流程中接入积分商城兑换的优惠券，支持满减券和折扣券的使用，前端收银台自动展示并选择最佳优惠券。

**Architecture:** 采用"支付创建时验证优惠券，支付成功时扣减优惠券"的策略。优惠券配置增加"可用场景"字段，支持按场景隔离使用。支付表增加 `coupon_id` 字段关联优惠券，支付成功回调时标记优惠券为已使用。

**Tech Stack:** Egg.js (Node.js), MySQL (Sequelize), React (H5)

---

## 数据库变更

### Task 1: 数据库迁移 - 优惠券场景字段

**Files:**
- Create: `super-tool-node/database/029_add_coupon_scenes_and_payment_coupon.sql`

**变更说明：**
1. `points_mall_items` 表增加 `applicable_scenes` JSON 字段（适用场景：points_mall、member_subscription）
2. `user_coupons` 表增加 `applicable_scenes` JSON 字段（从商品配置继承）
3. `member_payments` 表增加 `coupon_id` 字段（关联使用的优惠券）
4. 更新 `pointsMall.ts` 兑换逻辑，创建优惠券时写入 `applicable_scenes`

- [ ] **Step 1: 创建迁移 SQL 文件**

```sql
-- ============================================================================
-- 029 · 优惠券场景字段 + 支付优惠券关联
--
-- 目的：支持优惠券按场景使用（积分商城兑换 vs 会员订阅抵扣）
-- 前置依赖：028_user_coupons_and_unlocks.sql
-- 幂等：使用 ALTER TABLE IF COLUMN NOT EXISTS 模式
-- ============================================================================

USE `superadmin_db`;
SET NAMES utf8mb4;
SET FOREIGN_KEY_CHECKS = 0;

-- #1: points_mall_items 增加 applicable_scenes 字段
SET @col_exists := (
  SELECT COUNT(*) FROM information_schema.columns
  WHERE table_schema = DATABASE() AND table_name = 'points_mall_items'
    AND column_name = 'applicable_scenes'
);
SET @sql := IF(@col_exists = 0,
  'ALTER TABLE `points_mall_items`
   ADD COLUMN `applicable_scenes` JSON DEFAULT NULL
     COMMENT ''适用场景 JSON 数组，如 [\"points_mall\", \"member_subscription\"]，NULL 表示全场景可用''',
  'SELECT ''§1 points_mall_items.applicable_scenes already exists, skipped'' AS msg'
);
PREPARE stmt FROM @sql; EXECUTE stmt; DEALLOCATE PREPARE stmt;

-- #2: user_coupons 增加 applicable_scenes 字段
SET @col_exists := (
  SELECT COUNT(*) FROM information_schema.columns
  WHERE table_schema = DATABASE() AND table_name = 'user_coupons'
    AND column_name = 'applicable_scenes'
);
SET @sql := IF(@col_exists = 0,
  'ALTER TABLE `user_coupons`
   ADD COLUMN `applicable_scenes` JSON DEFAULT NULL
     COMMENT ''适用场景，从 points_mall_items 继承，NULL 表示全场景可用'',
  'SELECT ''§2 user_coupons.applicable_scenes already exists, skipped'' AS msg'
);
PREPARE stmt FROM @sql; EXECUTE stmt; DEALLOCATE PREPARE stmt;

-- #3: user_coupons 增加 locked_payment_id 字段（防止并发使用）
SET @col_exists := (
  SELECT COUNT(*) FROM information_schema.columns
  WHERE table_schema = DATABASE() AND table_name = 'user_coupons'
    AND column_name = 'locked_payment_id'
);
SET @sql := IF(@col_exists = 0,
  'ALTER TABLE `user_coupons`
   ADD COLUMN `locked_payment_id` BIGINT UNSIGNED DEFAULT NULL
     COMMENT ''锁定该优惠券的支付ID（防止并发使用）'',
  'SELECT ''§3 user_coupons.locked_payment_id already exists, skipped'' AS msg'
);
PREPARE stmt FROM @sql; EXECUTE stmt; DEALLOCATE PREPARE stmt;

-- #4: member_payments 增加 coupon_id 字段
SET @col_exists := (
  SELECT COUNT(*) FROM information_schema.columns
  WHERE table_schema = DATABASE() AND table_name = 'member_payments'
    AND column_name = 'coupon_id'
);
SET @sql := IF(@col_exists = 0,
  'ALTER TABLE `member_payments`
   ADD COLUMN `coupon_id` BIGINT UNSIGNED DEFAULT NULL
     COMMENT ''使用的优惠券ID'',
  'SELECT ''§4 member_payments.coupon_id already exists, skipped'' AS msg'
);
PREPARE stmt FROM @sql; EXECUTE stmt; DEALLOCATE PREPARE stmt;

-- #5: member_payments 增加 coupon_discount_amount 字段
SET @col_exists := (
  SELECT COUNT(*) FROM information_schema.columns
  WHERE table_schema = DATABASE() AND table_name = 'member_payments'
    AND column_name = 'coupon_discount_amount'
);
SET @sql := IF(@col_exists = 0,
  'ALTER TABLE `member_payments`
   ADD COLUMN `coupon_discount_amount` DECIMAL(10,2) DEFAULT 0
     COMMENT ''优惠券抵扣金额'',
  'SELECT ''§5 member_payments.coupon_discount_amount already exists, skipped'' AS msg'
);
PREPARE stmt FROM @sql; EXECUTE stmt; DEALLOCATE PREPARE stmt;

-- #6: 为 user_coupons 增加索引
SET @idx_exists := (
  SELECT COUNT(*) FROM information_schema.statistics
  WHERE table_schema = DATABASE() AND table_name = 'user_coupons'
    AND index_name = 'idx_locked_payment'
);
SET @sql := IF(@idx_exists = 0,
  'ALTER TABLE `user_coupons` ADD INDEX `idx_locked_payment` (`locked_payment_id`)',
  'SELECT ''§6 idx_locked_payment already exists, skipped'' AS msg'
);
PREPARE stmt FROM @sql; EXECUTE stmt; DEALLOCATE PREPARE stmt;

SET FOREIGN_KEY_CHECKS = 1;

-- 验证
SELECT '029_migration_check' AS check_item,
  (SELECT COUNT(*) FROM information_schema.columns WHERE table_schema = DATABASE() AND table_name = 'points_mall_items' AND column_name = 'applicable_scenes') AS has_items_scenes,
  (SELECT COUNT(*) FROM information_schema.columns WHERE table_schema = DATABASE() AND table_name = 'user_coupons' AND column_name = 'applicable_scenes') AS has_coupons_scenes,
  (SELECT COUNT(*) FROM information_schema.columns WHERE table_schema = DATABASE() AND table_name = 'user_coupons' AND column_name = 'locked_payment_id') AS has_locked_payment,
  (SELECT COUNT(*) FROM information_schema.columns WHERE table_schema = DATABASE() AND table_name = 'member_payments' AND column_name = 'coupon_id') AS has_payment_coupon;
```

- [ ] **Step 2: 执行迁移 SQL**

Run: `mysql -u root -p superadmin_db < super-tool-node/database/029_add_coupon_scenes_and_payment_coupon.sql`

Expected: 所有 ALTER 语句执行成功，验证查询返回 1  for each check.

- [ ] **Step 3: 更新 UserCoupon model**

Modify: `super-tool-node/app/model/user_coupon.ts`

```typescript
import { Application } from 'egg';
import { DataTypes } from 'sequelize';

export interface UserCouponAttributes {
  id: number;
  userId: number;
  orderId: number;
  couponCode: string;
  couponType: 'fixed' | 'percent';
  discount: number;
  threshold: number;
  applicableScenes?: string[];  // 新增：适用场景
  status: 'unused' | 'used' | 'expired';
  lockedPaymentId?: number;  // 新增：锁定的支付ID
  usedAt?: Date;
  expireAt: Date;
  createdAt?: Date;
  updatedAt?: Date;
}

export default (app: Application) => {
  const { STRING, BIGINT, DATE, DECIMAL, ENUM, JSON: JSON_TYPE } = DataTypes;

  return app.model.define('UserCoupon', {
    id: { type: BIGINT.UNSIGNED, autoIncrement: true, primaryKey: true },
    userId: { type: BIGINT.UNSIGNED, allowNull: false, field: 'user_id' },
    orderId: { type: BIGINT.UNSIGNED, allowNull: false, field: 'order_id' },
    couponCode: { type: STRING(50), allowNull: false, unique: true, field: 'coupon_code' },
    couponType: { type: ENUM('fixed', 'percent'), allowNull: false, field: 'coupon_type' },
    discount: { type: DECIMAL(10, 2), allowNull: false, field: 'discount' },
    threshold: { type: DECIMAL(10, 2), allowNull: false, defaultValue: 0, field: 'threshold' },
    applicableScenes: { type: JSON_TYPE, allowNull: true, field: 'applicable_scenes' },  // 新增
    status: { type: ENUM('unused', 'used', 'expired'), allowNull: false, defaultValue: 'unused' },
    lockedPaymentId: { type: BIGINT.UNSIGNED, allowNull: true, field: 'locked_payment_id' },  // 新增
    usedAt: { type: DATE, allowNull: true, field: 'used_at' },
    expireAt: { type: DATE, allowNull: false, field: 'expire_at' },
    createdAt: { type: DATE, field: 'created_at' },
    updatedAt: { type: DATE, field: 'updated_at' },
  }, {
    tableName: 'user_coupons',
    timestamps: true,
    underscored: true,
  });
};
```

- [ ] **Step 4: 更新 PointsMallItem model**

Modify: `super-tool-node/app/model/points_mall_item.ts`

```typescript
// 在 interface 中增加：
export interface PointsMallItemAttributes {
  // ... 现有字段 ...
  applicableScenes?: string[];  // 新增：适用场景
}

// 在 model define 中增加：
applicableScenes: { type: JSON_TYPE, allowNull: true, field: 'applicable_scenes' },
```

- [ ] **Step 5: Commit**

```bash
git add super-tool-node/database/029_add_coupon_scenes_and_payment_coupon.sql super-tool-node/app/model/user_coupon.ts super-tool-node/app/model/points_mall_item.ts
git commit -m "feat: 优惠券增加适用场景字段，支付表增加优惠券关联字段"
```

---

## 后端：优惠券兑换逻辑更新

### Task 2: 更新优惠券兑换逻辑（pointsMall service）

**Files:**
- Modify: `super-tool-node/app/service/pointsMall.ts`

**目标：** 兑换优惠券时，从 `points_mall_items` 继承 `applicable_scenes` 到 `user_coupons`。

- [ ] **Step 1: 更新兑换逻辑，写入 applicableScenes**

Modify: `super-tool-node/app/service/pointsMall.ts` - `exchangeItem` 方法

在创建 `user_coupon` 时，增加 `applicable_scenes` 字段：

```typescript
// 在 exchangeItem 方法中，创建 user_coupon 的地方，修改为：
const couponCode = this._generateCouponCode();
await ctx.model.UserCoupon.create({
  userId,
  orderId: order.id,
  couponCode,
  couponType: item.fulfillConfig.couponType,
  discount: item.fulfillConfig.discount,
  threshold: item.fulfillConfig.threshold || 0,
  applicableScenes: item.applicableScenes,  // 继承商品的适用场景
  status: 'unused',
  expireAt: expireAt,
});
```

- [ ] **Step 2: 更新管理端商品编辑，支持设置 applicableScenes**

Modify: `super-tools-admin/src/pages/Points/Mall/Items/ItemEditDrawer.tsx`

在表单中增加"适用场景"多选框（points_mall、member_subscription）。

```tsx
// 在表单中增加：
<Form.Item label="适用场景">
  <Checkbox.Group
    value={formData.applicableScenes || []}
    onChange={(val) => setFormData({ ...formData, applicableScenes: val })}
  >
    <Checkbox value="points_mall">积分商城</Checkbox>
    <Checkbox value="member_subscription">会员订阅</Checkbox>
  </Checkbox.Group>
</Form.Item>
```

- [ ] **Step 3: Commit**

```bash
git add super-tool-node/app/service/pointsMall.ts super-tools-admin/src/pages/Points/Mall/Items/ItemEditDrawer.tsx
git commit -m "feat: 优惠券兑换时继承适用场景，管理端支持配置适用场景"
```

---

## 后端：优惠券查询 API

### Task 3: 新增获取会员订阅可用优惠券 API

**Files:**
- Create: `super-tool-node/app/controller/coupon.ts` (新增 Controller)
- Modify: `super-tool-node/app/router.ts`

**目标：** 提供 API 供前端查询当前用户可用于会员订阅的优惠券列表。

- [ ] **Step 1: 创建 CouponController**

Create: `super-tool-node/app/controller/coupon.ts`

```typescript
import BaseController from './base';

/**
 * 优惠券 Controller
 * 路由前缀: /api/coupons
 */
export default class CouponController extends BaseController {
  /** GET /api/coupons/available-for-subscription — 获取可用于会员订阅的优惠券 */
  async availableForSubscription() {
    const userId = (this.ctx.state.user as any).id;
    const { amount } = this.ctx.query; // 订阅金额，用于门槛校验

    const targetAmount = amount ? Number(amount) : 0;

    const coupons = await this.service.coupon.getAvailableForSubscription(userId, targetAmount);

    // 自动计算最佳优惠券
    const bestCoupon = this.service.coupon.pickBestCoupon(coupons, targetAmount);

    this.success({
      list: coupons,
      bestCoupon,
    });
  }
}
```

- [ ] **Step 2: 创建 CouponService**

Create: `super-tool-node/app/service/coupon.ts`

```typescript
import BaseService from './base';

export default class CouponService extends BaseService {
  /**
   * 获取可用于会员订阅的优惠券列表
   * 规则：
   * 1. status = 'unused'
   * 2. 未过期（expire_at > now()）
   * 3. applicable_scenes 包含 'member_subscription'（或 NULL 表示全场景）
   * 4. 满减券需要 targetAmount >= threshold
   */
  async getAvailableForSubscription(userId: number, targetAmount: number) {
    const { ctx, app } = this;
    const userIdNum = typeof userId === 'number' ? userId : Number(userId);

    const coupons = await ctx.model.UserCoupon.findAll({
      where: {
        userId: userIdNum,
        status: 'unused',
        expireAt: { [app.Sequelize.Op.gt]: new Date() },
        [app.Sequelize.Op.or]: [
          { applicableScenes: { [app.Sequelize.Op.is]: null } },
          { applicableScenes: { [app.Sequelize.Op.like]: '%"member_subscription"%' } },
        ],
      },
      order: [['expireAt', 'ASC']],
    });

    // 过滤：满减券需要满足门槛
    const validCoupons = coupons.filter((c: any) => {
      const coupon = c.toJSON();
      // 无门槛券（threshold = 0）始终可用
      if (coupon.threshold === 0) return true;
      // 满减券需要订阅金额 >= 门槛
      return targetAmount >= coupon.threshold;
    });

    return validCoupons.map((c: any) => {
      const coupon = c.toJSON();
      // 计算抵扣金额
      const discountAmount = this._calcDiscountAmount(coupon, targetAmount);
      return {
        ...coupon,
        discountAmount,
      };
    });
  }

  /**
   * 选择最佳优惠券（每次限用一张）
   * 规则：抵扣金额最大的优先
   */
  pickBestCoupon(coupons: any[], targetAmount: number): any | null {
    if (!coupons || coupons.length === 0) return null;

    let best = null;
    let maxDiscount = 0;

    for (const coupon of coupons) {
      const discountAmount = coupon.discountAmount || this._calcDiscountAmount(coupon, targetAmount);
      if (discountAmount > maxDiscount) {
        maxDiscount = discountAmount;
        best = coupon;
      }
    }

    return best;
  }

  /**
   * 计算优惠券抵扣金额
   */
  _calcDiscountAmount(coupon: any, targetAmount: number): number {
    if (coupon.couponType === 'fixed') {
      // 固定金额抵扣
      return Math.min(coupon.discount, targetAmount); // 抵扣金额不超过订单金额
    } else if (coupon.couponType === 'percent') {
      // 折扣券：discount 是折扣率（0.9 = 9折）
      const discountRate = Number(coupon.discount);
      return Math.round(targetAmount * (1 - discountRate) * 100) / 100;
    }
    return 0;
  }

  /**
   * 验证优惠券是否可用于订阅
   */
  async validateCouponForSubscription(couponId: number, userId: number, targetAmount: number) {
    const coupon = await this.ctx.model.UserCoupon.findOne({
      where: {
        id: couponId,
        userId,
        status: 'unused',
        expireAt: { [this.app.Sequelize.Op.gt]: new Date() },
      },
    });

    if (!coupon) return { valid: false, error: '优惠券不存在或已使用' };

    const couponData = (coupon as any).toJSON();

    // 检查适用场景
    if (couponData.applicableScenes) {
      const scenes = Array.isArray(couponData.applicableScenes)
        ? couponData.applicableScenes
        : JSON.parse(couponData.applicableScenes);
      if (!scenes.includes('member_subscription')) {
        return { valid: false, error: '该优惠券不可用于会员订阅' };
      }
    }

    // 检查门槛
    if (couponData.threshold > 0 && targetAmount < couponData.threshold) {
      return { valid: false, error: `该优惠券需要满足 ${couponData.threshold} 元门槛` };
    }

    // 计算抵扣金额
    const discountAmount = this._calcDiscountAmount(couponData, targetAmount);

    return {
      valid: true,
      coupon: couponData,
      discountAmount,
      finalAmount: Math.max(0, targetAmount - discountAmount),
    };
  }

  /**
   * 锁定优惠券（创建支付时）
   */
  async lockCoupon(couponId: number, paymentId: number) {
    await this.ctx.model.UserCoupon.update(
      { lockedPaymentId: paymentId },
      { where: { id: couponId, status: 'unused', lockedPaymentId: null } }
    );
  }

  /**
   * 解锁优惠券（支付失败时）
   */
  async unlockCoupon(couponId: number) {
    await this.ctx.model.UserCoupon.update(
      { lockedPaymentId: null },
      { where: { id: couponId } }
    );
  }

  /**
   * 标记优惠券为已使用（支付成功时）
   */
  async markCouponUsed(couponId: number) {
    await this.ctx.model.UserCoupon.update(
      { status: 'used', usedAt: new Date(), lockedPaymentId: null },
      { where: { id: couponId } }
    );
  }
}
```

- [ ] **Step 3: 注册路由**

Modify: `super-tool-node/app/router.ts`

在文件中增加：

```typescript
  // 优惠券路由
  app.post('coupons.availableForSubscription', '/api/coupons/available-for-subscription', controller.coupon.availableForSubscription);
```

注意：需要先在 router.ts 中引入 coupon controller（如果尚未引入）。

- [ ] **Step 4: 编写单元测试**

Create: `super-tool-node/test/app/service/coupon.test.ts`

测试覆盖：
- `getAvailableForSubscription` - 正常返回可用优惠券
- `pickBestCoupon` - 正确选择抵扣金额最大的优惠券
- `validateCouponForSubscription` - 验证场景、门槛、过期
- `_calcDiscountAmount` - fixed 和 percent 类型计算正确

- [ ] **Step 5: Commit**

```bash
git add super-tool-node/app/controller/coupon.ts super-tool-node/app/service/coupon.ts super-tool-node/app/router.ts super-tool-node/test/app/service/coupon.test.ts
git commit -m "feat: 新增优惠券查询 API 和服务"
```

---

## 后端：支付流程接入优惠券

### Task 4: 更新 PaymentService 支持优惠券

**Files:**
- Modify: `super-tool-node/app/service/payment.ts`
- Modify: `super-tool-node/app/controller/payment.ts`（如有）

**目标：** 支付创建时传入 `couponId`，验证并计算抵扣金额，支付成功时标记优惠券为已使用。

- [ ] **Step 1: 更新 CreatePaymentInput 接口和 create 方法**

Modify: `super-tool-node/app/service/payment.ts`

```typescript
interface CreatePaymentInput {
  orderId: number;
  userId: number;
  provider: ProviderCode;
  couponId?: number;  // 新增：优惠券ID
}

export default class PaymentService extends BaseService {
  async create(input: CreatePaymentInput) {
    const { orderId, userId, provider, couponId } = input;

    const order = await this.ctx.model.MemberOrder.findOne({
      where: { id: orderId, userId },
    });
    if (!order) this.ctx.throw(404, '订单不存在');
    const orderData = (order as any).toJSON();
    if (orderData.status !== 0) this.ctx.throw(400, `订单状态不允许支付（当前 ${orderData.status}）`);
    if (new Date(orderData.expireAt) < new Date()) {
      await (order as any).update({ status: 3 });
      this.ctx.throw(400, '订单已过期，请重新下单');
    }

    const existedSuccess = await this.ctx.model.MemberPayment.findOne({
      where: { orderId, status: 1 },
    });
    if (existedSuccess) this.ctx.throw(400, '订单已支付');

    // 优惠券处理逻辑
    let discountAmount = 0;
    let finalAmount = Number(orderData.amount);
    if (couponId) {
      const validation = await this.service.coupon.validateCouponForSubscription(
        couponId,
        userId,
        Number(orderData.amount)
      );
      if (!validation.valid) {
        this.ctx.throw(400, validation.error);
      }
      discountAmount = validation.discountAmount;
      finalAmount = validation.finalAmount;
    }

    const paymentNo = this._genPaymentNo();
    const payment = await this.ctx.model.MemberPayment.create({
      paymentNo,
      orderId,
      userId,
      provider,
      amount: finalAmount,  // 使用抵扣后的金额
      couponId: couponId || null,
      couponDiscountAmount: discountAmount,
      status: 0,
    });

    // 锁定优惠券
    if (couponId) {
      await this.service.coupon.lockCoupon(couponId, (payment as any).id);
    }

    // 调用支付提供商
    const providerImpl = provider === 'mock'
      ? getPaymentProvider(provider)
      : createProvider(provider, this.ctx);
    const baseUrl = (this.app.config as any).appConfig?.baseUrl || 'http://localhost:7001';
    const notifyUrl = `${baseUrl}/api/payments/${provider}/notify`;
    const prepayResult = await providerImpl.createPrepay({
      paymentNo,
      amount: finalAmount,  // 使用抵扣后的金额
      subject: `${orderData.planSnapshot?.name || orderData.planCode} 会员订阅`,
      planName: orderData.planSnapshot?.name,
      userId,
      notifyUrl,
    });

    await (payment as any).update({ prepayData: prepayResult.prepayData });

    return {
      paymentNo,
      paymentId: (payment as any).id,
      provider,
      prepayData: prepayResult.prepayData,
      cashierUrl: prepayResult.cashierUrl,
      discountAmount,
      finalAmount,
    };
  }
```

- [ ] **Step 2: 更新支付成功回调，标记优惠券为已使用**

Modify: `super-tool-node/app/service/payment.ts` - `_applyPaymentSuccess` 方法

在方法中增加标记优惠券为已使用的逻辑：

```typescript
  async _applyPaymentSuccess(payment: any, providerTradeNo: string, rawPayload: any) {
    await this.ctx.model.MemberPayment.transaction(async (t) => {
      // ... 现有逻辑 ...

      // 新增：标记优惠券为已使用
      const paymentData = (payment as any).toJSON();
      if (paymentData.couponId) {
        await this.service.coupon.markCouponUsed(paymentData.couponId);
      }

      // ... 现有逻辑 ...
    });
  }
```

- [ ] **Step 3: 更新支付失败/过期逻辑，解锁优惠券**

Modify: `super-tool-node/app/service/payment.ts`

在支付失败处理逻辑中（可能在 `_applyPaymentFailure` 或类似方法中），增加解锁优惠券的逻辑：

```typescript
  // 在支付失败时解锁优惠券
  async _handlePaymentFailure(paymentId: number) {
    const payment = await this.ctx.model.MemberPayment.findByPk(paymentId);
    if (payment && (payment as any).couponId) {
      await this.service.coupon.unlockCoupon((payment as any).couponId);
    }
    // ... 现有逻辑 ...
  }
```

注意：需要检查现有的支付失败处理逻辑在哪里，确保解锁优惠券的代码被正确调用。

- [ ] **Step 4: 更新 PaymentController（如有）**

如果 `payment.ts` controller 中有参数验证，需要增加 `couponId` 的可选参数：

Modify: `super-tool-node/app/controller/payment.ts`

```typescript
  async create() {
    this.validate({
      orderId: { type: 'number', required: true },
      provider: { type: 'string', required: true },
      couponId: { type: 'number', required: false },  // 新增
    });
    // ...
  }
```

- [ ] **Step 5: Commit**

```bash
git add super-tool-node/app/service/payment.ts super-tool-node/app/controller/payment.ts
git commit -m "feat: 支付流程接入优惠券，支持抵扣"
```

---

## 前端：收银台优惠券选择

### Task 5: 前端收银台接入优惠券展示和选择

**Files:**
- Modify: `super-tools-web/packages/h5/micro-tools/pages/member/cashier/index.tsx`
- Modify: `super-tools-web/packages/h5/micro-tools/service/order.ts`（或对应的 API service）
- Modify: `super-tools-web/packages/h5/micro-tools/store/member.ts`（如有状态管理）

**目标：** 收银台页面展示可用优惠券，自动选择最佳优惠券，用户可手动更换或取消使用。

- [ ] **Step 1: 新增获取可用优惠券的 API 调用**

Modify: `super-tools-web/packages/h5/micro-tools/service/order.ts`（或创建 `coupon.ts`）

```typescript
import request from '../utils/request';

export async function getAvailableCoupons(amount: number) {
  return request.get('/api/coupons/available-for-subscription', {
    params: { amount },
  });
}
```

- [ ] **Step 2: 更新收银台页面，获取并展示优惠券**

Modify: `super-tools-web/packages/h5/micro-tools/pages/member/cashier/index.tsx`

在页面加载时，获取可用优惠券，并自动选择最佳优惠券：

```tsx
import { getAvailableCoupons } from '../../service/coupon';

// 在组件状态中增加：
const [coupons, setCoupons] = useState<any[]>([]);
const [bestCoupon, setBestCoupon] = useState<any>(null);
const [selectedCoupon, setSelectedCoupon] = useState<any>(null);

// 在 useEffect 中加载优惠券：
useEffect(() => {
  const loadCoupons = async () => {
    const res = await getAvailableCoupons(orderAmount);
    setCoupons(res.list || []);
    setBestCoupon(res.bestCoupon || null);
    // 自动选择最佳优惠券
    if (res.bestCoupon) {
      setSelectedCoupon(res.bestCoupon);
    }
  };
  loadCoupons();
}, [orderAmount]);

// 在渲染中展示优惠券选择区域：
{
  coupons.length > 0 && (
    <div className="coupon-section">
      <div className="coupon-header">
        <span>优惠券</span>
        <span className="coupon-count">{coupons.length} 张可用</span>
      </div>
      <div className="coupon-list">
        {coupons.map((coupon) => (
          <div
            key={coupon.id}
            className={`coupon-item ${selectedCoupon?.id === coupon.id ? 'selected' : ''}`}
            onClick={() => setSelectedCoupon(coupon.id === selectedCoupon?.id ? null : coupon)}
          >
            <div className="coupon-discount">¥{coupon.discountAmount}</div>
            <div className="coupon-info">
              <div className="coupon-type">{coupon.couponType === 'fixed' ? '满减券' : '折扣券'}</div>
              <div className="coupon-threshold">
                {coupon.threshold > 0 ? `满${coupon.threshold}可用` : '无门槛'}
              </div>
            </div>
            <div className="coupon-expire">过期: {coupon.expireAt}</div>
          </div>
        ))}
      </div>
      <div className="coupon-summary">
        已选优惠券：{selectedCoupon ? `抵扣 ¥${selectedCoupon.discountAmount}` : '未选择'}
      </div>
    </div>
  )
}
```

- [ ] **Step 3: 更新支付创建逻辑，传入 couponId**

Modify: `super-tools-web/packages/h5/micro-tools/pages/member/cashier/index.tsx`

在调用创建支付时，传入 `couponId`：

```typescript
const handlePay = async () => {
  const params: any = {
    orderId: orderId,
    provider: selectedProvider,
  };
  if (selectedCoupon) {
    params.couponId = selectedCoupon.id;
  }
  const res = await createPayment(params);
  // ... 后续支付逻辑 ...
};
```

- [ ] **Step 4: 样式优化**

Modify: `super-tools-web/packages/h5/micro-tools/pages/member/cashier/index.less`

增加优惠券区域的样式：

```less
.coupon-section {
  margin: 16px;
  padding: 16px;
  background: #fff;
  border-radius: 8px;

  .coupon-header {
    display: flex;
    justify-content: space-between;
    margin-bottom: 12px;
    font-weight: bold;
  }

  .coupon-item {
    display: flex;
    align-items: center;
    padding: 12px;
    margin-bottom: 8px;
    border: 2px solid #eee;
    border-radius: 8px;
    cursor: pointer;

    &.selected {
      border-color: #1890ff;
      background: #e6f7ff;
    }

    .coupon-discount {
      font-size: 24px;
      font-weight: bold;
      color: #f5222d;
      margin-right: 12px;
    }

    .coupon-info {
      flex: 1;
    }
  }
}
```

- [ ] **Step 5: Commit**

```bash
git add super-tools-web/packages/h5/micro-tools/pages/member/cashier/index.tsx super-tools-web/packages/h5/micro-tools/pages/member/cashier/index.less super-tools-web/packages/h5/micro-tools/service/coupon.ts
git commit -m "feat: 收银台接入优惠券展示和选择"
```

---

## 测试与验收

### Task 6: 端到端测试

**Files:**
- Create: `super-tool-node/test/integration/coupon-subscription.test.ts`

- [ ] **Step 1: 编写端到端测试**

测试覆盖场景：
1. 用户有可用优惠券，订阅时自动选择最佳优惠券
2. 使用满减券，订阅金额满足门槛，抵扣正确
3. 使用折扣券，折扣计算正确
4. 无门槛券可用于任意金额订阅
5. 满减券订阅金额不满足门槛，不能使用
6. 优惠券不可用场景（已使用、已过期、场景不匹配）
7. 支付成功后，优惠券标记为已使用
8. 支付失败后，优惠券解锁可再次使用

- [ ] **Step 2: 手动验收**

在开发环境执行以下验收：
1. 在积分商城兑换一张优惠券（配置为可用于会员订阅）
2. 进入会员订阅页面，选择套餐，进入收银台
3. 验证收银台正确展示可用优惠券
4. 验证自动选择最佳优惠券
5. 完成支付，验证订单金额为抵扣后金额
6. 验证优惠券标记为已使用
7. 验证会员权益正确开通

- [ ] **Step 3: Commit**

```bash
git add super-tool-node/test/integration/coupon-subscription.test.ts
git commit -m "test: 增加优惠券订阅场景的端到端测试"
```

---

## 部署检查清单

- [ ] 数据库迁移已执行（029 SQL）
- [ ] 环境变量配置正确
- [ ] 管理端已配置优惠券商品的适用场景
- [ ] 前端已部署，收银台优惠券功能正常
- [ ] 后端已部署，支付流程正确接入优惠券
- [ ] 支付成功回调正确标记优惠券为已使用
- [ ] 支付失败/过期正确解锁优惠券

---

**计划完成。**
