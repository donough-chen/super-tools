# 工具管理模块 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **REQUIRED DOMAIN SKILL:** `node-api-dev`（9 阶段开发流程已嵌入下面任务）

**Goal:** 在 `super-tool-node`（Egg.js + Sequelize + MySQL）项目中新增「工具分类 + 工具」管理模块，包含 11 个工具分类、233 个工具的种子数据初始化、4 个 H5 端接口、10 个管理端接口、完整测试与文档。

**Architecture:** 双表（`tool_categories` + `tools`）+ 一个 Service + 两个 Controller（H5 / 管理端）。会员权限由 `required_level_code` + `require_paid` 两个正交字段定义；用户点击工具跳转时通过 `/api/tools/:code/access` 懒校验。

**Tech Stack:** Egg.js 3.x · TypeScript 5.x · Sequelize · MySQL 8 · Redis · egg-mock

**Spec:** [`docs/superpowers/specs/2026-05-06-工具管理模块设计文档.md`](../specs/2026-05-06-工具管理模块设计文档.md)

---

## 文件结构总览

| 类别 | 路径 | 职责 |
|------|------|------|
| Migration | `database/004_add_tools_system.sql` | 建表 + 种子（一次性执行） |
| 种子生成器 | `scripts/build-tools-seed.js` | **已存在**，从 `test1.js` 生成 SQL |
| Model | `app/model/tool_category.ts` | `ToolCategory` Sequelize 模型 |
|  | `app/model/tool.ts` | `Tool` Sequelize 模型 + association |
| Service | `app/service/tool.ts` | 业务逻辑（H5 + 管理端） |
| Controller | `app/controller/tool.ts` | H5 端 4 个 action |
|  | `app/controller/admin/tool.ts` | 管理端 10 个 action |
| Router | `app/router.ts` | 末尾追加 14 条路由 |
| Constants | `app/constants/errorCodes.ts` | 末尾追加 `1008xx` 工具模块错误码 |
| Tests | `test/api/tool.test.ts` | H5 端接口测试 |
|  | `test/api/admin-tool.test.ts` | 管理端接口测试 |
| API 文档 | `docs/api/tool/README.md` | H5 端 API 文档 |
|  | `docs/api/admin-tool/README.md` | 管理端 API 文档 |
| 索引 | `docs/api/README.md` | 追加工具模块索引 |
|  | `docs/changelog/2026-05.md` | 创建/追加本月变更 |

---

## 任务总览

> 严格遵循 `node-api-dev` 9 阶段流程：

| # | 任务 | node-api-dev 阶段 |
|---|------|---|
| 1 | 错误码扩展 | 配套 |
| 2 | Migration SQL 编写 | 阶段 1 数据库设计 |
| 3 | Migration 部署（⚠️ 需用户确认） | 阶段 2 数据库部署 |
| 4 | Model：ToolCategory | 阶段 4 |
| 5 | Model：Tool | 阶段 4 |
| 6 | Service：H5 端核心查询 | 阶段 5 |
| 7 | Service：权限校验 `checkToolAccess` | 阶段 5 |
| 8 | Service：管理端 CRUD + 批量发布 | 阶段 5 |
| 9 | Controller H5 端 + 路由 | 阶段 6+7 |
| 10 | Controller 管理端 + 路由 | 阶段 6+7 |
| 11 | 集成测试：H5 端 | 阶段 8 |
| 12 | 集成测试：管理端 | 阶段 8 |
| 13 | API 文档 + 索引 + 变更日志 | 阶段 9 |

阶段 3（接口设计）已在 spec 中完成，本计划直接复用。

---

## Task 1: 错误码扩展

**Files:**
- Modify: `app/constants/errorCodes.ts`（追加 `1008xx` 区间）

- [ ] **Step 1: 在 `errorCodes.ts` 末尾追加工具模块错误码**

打开 `app/constants/errorCodes.ts`，在 `ErrorCodes` 对象闭合 `}` 之前追加：

```typescript
  // 工具模块错误 1008xx
  TOOL_NOT_FOUND: { code: 100801, message: '工具不存在' },
  TOOL_OFFLINE: { code: 100802, message: '工具已下架' },
  TOOL_CODE_EXISTS: { code: 100803, message: '工具编码已存在' },
  TOOL_CATEGORY_NOT_FOUND: { code: 100804, message: '分类不存在' },
  TOOL_CATEGORY_CODE_EXISTS: { code: 100805, message: '分类编码已存在' },
  TOOL_CATEGORY_HAS_TOOLS: { code: 100806, message: '该分类下尚有工具，请先移除或删除后再操作' },
  TOOL_ACCESS_NEED_LEVEL: { code: 100807, message: '当前等级不足' },
  TOOL_ACCESS_NEED_PAID: { code: 100808, message: '需要付费会员' },
  TOOL_ACCESS_PAID_EXPIRED: { code: 100809, message: '付费会员已过期' },
```

- [ ] **Step 2: 验证 TS 编译**

```bash
npx tsc --noEmit -p tsconfig.json
```
Expected: 无与 `errorCodes.ts` 相关的报错。

- [ ] **Step 3: Commit**

```bash
git add app/constants/errorCodes.ts
git commit -m "feat(tool): add 1008xx error codes for tool module"
```

---

## Task 2: Migration SQL 编写

**Files:**
- Run: `scripts/build-tools-seed.js`（已存在）
- Create: `database/004_add_tools_system.sql`

- [ ] **Step 1: 运行种子生成脚本**

```bash
cd super-tool-node
node scripts/build-tools-seed.js
```
Expected: `[build-tools-seed] OK`，`分类: 11 个 ... 工具: 233 个`，生成 `scripts/.tmp/tools-seed.sql`。

- [ ] **Step 2: 创建 Migration 文件**

Create `database/004_add_tools_system.sql`：

```sql
-- ============================================================
-- 迁移脚本: 004_add_tools_system.sql
-- 版本: 2.3.0
-- 创建时间: 2026-05-06
-- 说明: 新增工具分类 + 工具管理模块
-- 设计文档: docs/superpowers/specs/2026-05-06-工具管理模块设计文档.md
-- ============================================================

USE `superadmin_db`;

SET NAMES utf8mb4;
SET FOREIGN_KEY_CHECKS = 0;

-- ------------------------------------------------------------
-- 1. 工具分类表
-- ------------------------------------------------------------
CREATE TABLE IF NOT EXISTS `tool_categories` (
  `id`           INT UNSIGNED     NOT NULL AUTO_INCREMENT,
  `code`         VARCHAR(30)      NOT NULL                 COMMENT '分类编码',
  `name`         VARCHAR(50)      NOT NULL                 COMMENT '分类名称',
  `icon`         VARCHAR(500)     DEFAULT NULL             COMMENT '分类图标 URL',
  `description`  VARCHAR(500)     DEFAULT NULL             COMMENT '分类描述',
  `sort`         INT              NOT NULL DEFAULT 0       COMMENT '排序（越小越前）',
  `status`       TINYINT UNSIGNED NOT NULL DEFAULT 1       COMMENT '状态:0禁用,1启用',
  `created_at`   DATETIME         NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at`   DATETIME         NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_code`           (`code`),
  INDEX       `idx_status_sort`  (`status`, `sort`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='工具分类表';

-- ------------------------------------------------------------
-- 2. 工具表
-- ------------------------------------------------------------
CREATE TABLE IF NOT EXISTS `tools` (
  `id`                   BIGINT UNSIGNED  NOT NULL AUTO_INCREMENT,
  `code`                 VARCHAR(60)      NOT NULL                          COMMENT '工具编码（前端路由 slug）',
  `name`                 VARCHAR(80)      NOT NULL                          COMMENT '工具名称',
  `description`          VARCHAR(500)     NOT NULL DEFAULT ''               COMMENT '工具描述',
  `keyword`              VARCHAR(500)     NOT NULL DEFAULT ''               COMMENT '搜索关键字',
  `category_id`          INT UNSIGNED     NOT NULL                          COMMENT '分类ID',
  `category_code`        VARCHAR(30)      NOT NULL                          COMMENT '分类编码（冗余）',
  `icon`                 VARCHAR(500)     NOT NULL DEFAULT ''               COMMENT '工具图标',
  `color`                VARCHAR(20)      NOT NULL DEFAULT ''               COMMENT '主题色',
  `path`                 VARCHAR(200)     NOT NULL DEFAULT ''               COMMENT '前端路由路径',
  `is_feature`           TINYINT UNSIGNED NOT NULL DEFAULT 0                COMMENT '是否特色:0否,1是',
  `required_level_code`  VARCHAR(30)      NOT NULL DEFAULT 'free'           COMMENT '最低成长等级要求',
  `require_paid`         TINYINT UNSIGNED NOT NULL DEFAULT 0                COMMENT '是否要求付费:0否,1是',
  `status`               TINYINT UNSIGNED NOT NULL DEFAULT 0                COMMENT '状态:0下架,1已发布',
  `sort`                 INT              NOT NULL DEFAULT 0                COMMENT '同分类内排序',
  `created_at`           DATETIME         NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at`           DATETIME         NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_code`                    (`code`),
  INDEX       `idx_status`                (`status`),
  INDEX       `idx_cat_status_sort`       (`category_code`, `status`, `sort`),
  INDEX       `idx_feature_status`        (`is_feature`, `status`),
  INDEX       `idx_required_level`        (`required_level_code`),
  INDEX       `idx_require_paid`          (`require_paid`),
  CONSTRAINT `fk_tool_category` FOREIGN KEY (`category_id`)
    REFERENCES `tool_categories` (`id`) ON DELETE RESTRICT ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='工具表';


-- ============================================================
-- 种子数据
-- 11 个分类 + 233 个工具，全部默认 status=1, is_feature=0,
-- required_level_code='free', require_paid=0
-- 以下两段 INSERT 从 scripts/.tmp/tools-seed.sql 复制
-- ============================================================

-- 【粘贴 INSERT IGNORE INTO `tool_categories` ...; 整段】

-- 【粘贴 INSERT IGNORE INTO `tools` ...; 整段】


SET FOREIGN_KEY_CHECKS = 1;
```

- [ ] **Step 3: 复制种子数据**

打开 `scripts/.tmp/tools-seed.sql`，将其中两段 `INSERT IGNORE INTO ...;` 完整复制到 Step 2 文件中预留的两个占位位置。

- [ ] **Step 4: 校验文件**

```bash
node -e "const s=require('fs').readFileSync('database/004_add_tools_system.sql','utf8');console.log('bytes:',s.length,'has_seed_categories:',s.includes('INSERT IGNORE INTO `tool_categories`'),'has_seed_tools:',s.includes('INSERT IGNORE INTO `tools`'));"
```
Expected: `bytes` > 30000，两个 `has_seed_*` 均为 `true`。

- [ ] **Step 5: Commit**

```bash
git add database/004_add_tools_system.sql scripts/build-tools-seed.js
git commit -m "feat(db): add migration 004 for tools system with seed data"
```

---

## Task 3: 数据库部署（⚠️ HARD-GATE — 需用户确认）

> ⚠️ 此任务执行真实数据库写入。开始前必须向用户展示 SQL 摘要并获得明确确认（来自 `node-api-dev` 阶段 2）。

**Files:** None

- [ ] **Step 1: 向用户展示 Migration**

输出 `database/004_add_tools_system.sql` 路径与摘要（建表 2 张 + 种子 11+233 行），等待用户回复"确认执行"或等价表述。

- [ ] **Step 2: 执行 Migration**

获得确认后执行：

```bash
cd super-tool-node
npm run db:cli
```

交互界面：
1. `\l` — 确认列表中存在 `004_add_tools_system.sql`
2. `\r 4` — 执行该文件（按提示二次确认）
3. `\d` — 列表中应出现 `tool_categories` 和 `tools`
4. `\q` 退出

- [ ] **Step 3: 验证种子数据**

```bash
npm run db:cli
```
执行：
```
\s SELECT COUNT(*) AS c FROM tool_categories;
\s SELECT COUNT(*) AS c FROM tools;
\s SELECT category_code, COUNT(*) AS c FROM tools GROUP BY category_code ORDER BY c DESC;
```
Expected: `tool_categories=11`、`tools=233`，分组分布 = LIFE 35 / CALCULATION 30 / TEXT 30 / DEV 29 / QUERY 28 / IMAGE 27 / DAILY 16 / DOCUMENT 13 / INTELLIGENT 11 / VIDEO 8 / AUDIO 6。

- [ ] **Step 4: 无 commit（数据库变更不进 git）**

---

## Task 4: Model — ToolCategory

**Files:**
- Create: `app/model/tool_category.ts`

- [ ] **Step 1: 创建 ToolCategory Model**

Create `app/model/tool_category.ts`：

```typescript
import { Application } from 'egg';
import { DataTypes } from 'sequelize';

export interface ToolCategoryAttributes {
  id: number;
  code: string;
  name: string;
  icon?: string | null;
  description?: string | null;
  sort: number;
  status: number;
  createdAt?: Date;
  updatedAt?: Date;
}

export default (app: Application) => {
  const { STRING, INTEGER, TINYINT } = DataTypes;

  const ToolCategory = app.model.define('ToolCategory', {
    id:          { type: INTEGER.UNSIGNED, autoIncrement: true, primaryKey: true },
    code:        { type: STRING(30), allowNull: false, unique: true },
    name:        { type: STRING(50), allowNull: false },
    icon:        { type: STRING(500), allowNull: true },
    description: { type: STRING(500), allowNull: true },
    sort:        { type: INTEGER, allowNull: false, defaultValue: 0 },
    status:      { type: TINYINT.UNSIGNED, allowNull: false, defaultValue: 1 },
  }, {
    tableName: 'tool_categories',
    timestamps: true,
    underscored: true,
  });

  (ToolCategory as any).associate = () => {
    ToolCategory.hasMany(app.model.Tool, { foreignKey: 'category_id', as: 'tools' });
  };

  return ToolCategory;
};
```

- [ ] **Step 2: 启动 dev 验证 Model 加载**

```bash
npm run dev
```
观察日志无 Sequelize 报错；Ctrl+C 终止。
（注：`app.model.Tool` 引用此时尚未存在，但 `associate` 是延迟调用，不会立即报错。）

- [ ] **Step 3: Commit**

```bash
git add app/model/tool_category.ts
git commit -m "feat(model): add ToolCategory model"
```

---

## Task 5: Model — Tool

**Files:**
- Create: `app/model/tool.ts`

- [ ] **Step 1: 创建 Tool Model**

Create `app/model/tool.ts`：

```typescript
import { Application } from 'egg';
import { DataTypes } from 'sequelize';

export interface ToolAttributes {
  id: number;
  code: string;
  name: string;
  description: string;
  keyword: string;
  categoryId: number;
  categoryCode: string;
  icon: string;
  color: string;
  path: string;
  isFeature: number;
  requiredLevelCode: string;
  requirePaid: number;
  status: number;
  sort: number;
  createdAt?: Date;
  updatedAt?: Date;
}

export default (app: Application) => {
  const { STRING, INTEGER, BIGINT, TINYINT } = DataTypes;

  const Tool = app.model.define('Tool', {
    id:                { type: BIGINT.UNSIGNED, autoIncrement: true, primaryKey: true },
    code:              { type: STRING(60), allowNull: false, unique: true },
    name:              { type: STRING(80), allowNull: false },
    description:       { type: STRING(500), allowNull: false, defaultValue: '' },
    keyword:           { type: STRING(500), allowNull: false, defaultValue: '' },
    categoryId:        { type: INTEGER.UNSIGNED, allowNull: false, field: 'category_id' },
    categoryCode:      { type: STRING(30), allowNull: false, field: 'category_code' },
    icon:              { type: STRING(500), allowNull: false, defaultValue: '' },
    color:             { type: STRING(20), allowNull: false, defaultValue: '' },
    path:              { type: STRING(200), allowNull: false, defaultValue: '' },
    isFeature:         { type: TINYINT.UNSIGNED, allowNull: false, defaultValue: 0, field: 'is_feature' },
    requiredLevelCode: { type: STRING(30), allowNull: false, defaultValue: 'free', field: 'required_level_code' },
    requirePaid:       { type: TINYINT.UNSIGNED, allowNull: false, defaultValue: 0, field: 'require_paid' },
    status:            { type: TINYINT.UNSIGNED, allowNull: false, defaultValue: 0 },
    sort:              { type: INTEGER, allowNull: false, defaultValue: 0 },
  }, {
    tableName: 'tools',
    timestamps: true,
    underscored: true,
  });

  (Tool as any).associate = () => {
    Tool.belongsTo(app.model.ToolCategory, { foreignKey: 'category_id', as: 'category' });
  };

  return Tool;
};
```

- [ ] **Step 2: 启动 dev 验证两个 Model 联合加载**

```bash
npm run dev
```
Expected: 无报错，association 建立成功；Ctrl+C 终止。

- [ ] **Step 3: 通过 db:cli 做基本查询冒烟**

```bash
npm run db:cli
```
```
\s SELECT id, code, name FROM tool_categories ORDER BY sort LIMIT 3;
\s SELECT id, code, name, category_code FROM tools LIMIT 3;
\q
```
Expected: 各返回 3 行真实数据。

- [ ] **Step 4: Commit**

```bash
git add app/model/tool.ts
git commit -m "feat(model): add Tool model with ToolCategory association"
```

---

## Task 6: Service — H5 端核心查询

**Files:**
- Create: `app/service/tool.ts`

- [ ] **Step 1: 创建 Service 框架与 H5 端方法**

Create `app/service/tool.ts`：

```typescript
import BaseService, { PaginationResult, PaginationOptions } from './base';

// ==================== 类型定义 ====================

export interface ToolDto {
  id: number;
  code: string;
  name: string;
  description: string;
  keyword: string;
  categoryCode: string;
  icon: string;
  color: string;
  path: string;
  isFeature: number;
  requiredLevelCode: string;
  requirePaid: number;
  sort: number;
}

export interface CategoryDto {
  id: number;
  code: string;
  name: string;
  icon: string | null;
  description?: string | null;
  sort: number;
}

export type CategoryWithTools = CategoryDto & { tools: ToolDto[] };

export interface HomeAggregateResult {
  mode: 'aggregate';
  categories: CategoryWithTools[];
}

export interface HomePaginatedResult {
  mode: 'paginated';
  categories: CategoryDto[];
  tools: PaginationResult<ToolDto>;
}

export type HomeResult = HomeAggregateResult | HomePaginatedResult;

export interface AccessAllowed {
  allowed: true;
  tool: { code: string; name: string; path: string };
}
export interface AccessDenied {
  allowed: false;
  reason: 'need_level' | 'need_paid' | 'paid_expired';
  required: { levelCode: string; levelName: string; requirePaid: boolean };
  current: { levelCode: string; isPaid: boolean };
}
export type AccessResult = AccessAllowed | AccessDenied;

const HOME_AGGREGATE_CACHE_KEY = 'tool:home:aggregate';
const CATEGORIES_CACHE_KEY = 'tool:categories:all';
const HOME_AGGREGATE_TTL = 300;
const CATEGORIES_TTL = 600;

export default class ToolService extends BaseService {

  // ==================== H5 端 ====================

  async getHomeData(query: {
    categoryCode?: string;
    keyword?: string;
    page?: number;
    pageSize?: number;
  }): Promise<HomeResult> {
    const { categoryCode, keyword, page, pageSize } = query;

    if (!categoryCode && !keyword) {
      // 聚合模式
      return this.getOrSetCache<HomeAggregateResult>(
        HOME_AGGREGATE_CACHE_KEY,
        async () => {
          const categoriesRaw = await this.fetchCategories();
          const tools = await this.ctx.model.Tool.findAll({
            where: { status: 1 },
            order: [['sort', 'ASC'], ['id', 'ASC']],
          });
          const toolsDto = tools.map((t: any) => this.toToolDto(t.toJSON()));

          const byCode: Record<string, ToolDto[]> = {};
          for (const t of toolsDto) {
            if (!byCode[t.categoryCode]) byCode[t.categoryCode] = [];
            byCode[t.categoryCode].push(t);
          }
          const categories: CategoryWithTools[] = categoriesRaw.map(c => ({
            ...c,
            tools: byCode[c.code] || [],
          }));
          return { mode: 'aggregate', categories };
        },
        HOME_AGGREGATE_TTL,
      );
    }

    // 分页模式
    const categories = await this.fetchCategories();
    const { Op } = require('sequelize');
    const where: any = { status: 1 };
    if (categoryCode) where.categoryCode = categoryCode;
    if (keyword) {
      const like = { [Op.like]: `%${keyword}%` };
      where[Op.or] = [{ name: like }, { description: like }, { keyword: like }];
    }

    const result = await this.paginate<any>(
      this.ctx.model.Tool,
      { where, order: [['sort', 'ASC'], ['id', 'ASC']] },
      { page, pageSize },
    );

    return {
      mode: 'paginated',
      categories,
      tools: {
        ...result,
        list: result.list.map((t: any) => this.toToolDto(t.toJSON ? t.toJSON() : t)),
      },
    };
  }

  async getFeatureTools(pagination: PaginationOptions): Promise<PaginationResult<ToolDto>> {
    const result = await this.paginate<any>(
      this.ctx.model.Tool,
      {
        where: { isFeature: 1, status: 1 },
        order: [['sort', 'ASC'], ['id', 'ASC']],
      },
      pagination,
    );
    return {
      ...result,
      list: result.list.map((t: any) => this.toToolDto(t.toJSON ? t.toJSON() : t)),
    };
  }

  async getMemberTools(pagination: PaginationOptions): Promise<PaginationResult<ToolDto>> {
    const { Op } = require('sequelize');
    const result = await this.paginate<any>(
      this.ctx.model.Tool,
      {
        where: {
          status: 1,
          [Op.or]: [
            { requiredLevelCode: { [Op.ne]: 'free' } },
            { requirePaid: 1 },
          ],
        },
        order: [['sort', 'ASC'], ['id', 'ASC']],
      },
      pagination,
    );
    return {
      ...result,
      list: result.list.map((t: any) => this.toToolDto(t.toJSON ? t.toJSON() : t)),
    };
  }

  // ==================== 私有辅助 ====================

  private async fetchCategories(): Promise<CategoryDto[]> {
    return this.getOrSetCache<CategoryDto[]>(
      CATEGORIES_CACHE_KEY,
      async () => {
        const list = await this.ctx.model.ToolCategory.findAll({
          where: { status: 1 },
          order: [['sort', 'ASC'], ['id', 'ASC']],
        });
        return list.map((c: any) => {
          const j = c.toJSON();
          return { id: j.id, code: j.code, name: j.name, icon: j.icon, description: j.description, sort: j.sort };
        });
      },
      CATEGORIES_TTL,
    );
  }

  private toToolDto(t: any): ToolDto {
    return {
      id: t.id,
      code: t.code,
      name: t.name,
      description: t.description,
      keyword: t.keyword,
      categoryCode: t.categoryCode || t.category_code,
      icon: t.icon,
      color: t.color,
      path: t.path,
      isFeature: t.isFeature ?? t.is_feature,
      requiredLevelCode: t.requiredLevelCode || t.required_level_code,
      requirePaid: t.requirePaid ?? t.require_paid,
      sort: t.sort,
    };
  }
}
```

- [ ] **Step 2: 启动 dev 验证编译**

```bash
npm run dev
```
Expected: 无 TS 报错；终止。

- [ ] **Step 3: Commit**

```bash
git add app/service/tool.ts
git commit -m "feat(service): add ToolService H5 methods (home/feature/member)"
```

---

## Task 7: Service — 权限校验 `checkToolAccess`

**Files:**
- Modify: `app/service/tool.ts`（追加方法）

- [ ] **Step 1: 在 ToolService 类的 `getMemberTools` 方法后追加 `checkToolAccess`**

在 `// ==================== 私有辅助 ====================` 注释**前**插入：

```typescript
  // ==================== 权限校验 ====================

  /**
   * 工具使用前权限校验（用户点击工具卡片时调用）
   */
  async checkToolAccess(userId: number, toolCode: string): Promise<AccessResult> {
    const tool = await this.ctx.model.Tool.findOne({ where: { code: toolCode } });
    if (!tool) this.ctx.throw(404, '工具不存在');

    const t = (tool as any).toJSON();
    if (t.status !== 1) this.ctx.throw(400, '工具已下架');

    // 免费工具快速通道
    if (t.requiredLevelCode === 'free' && t.requirePaid === 0) {
      return { allowed: true, tool: { code: t.code, name: t.name, path: t.path } };
    }

    const member = await this.ctx.model.UserMember.findOne({
      where: { userId },
      include: [{ model: this.ctx.model.MemberLevel, as: 'level' }],
    });
    if (!member) this.ctx.throw(404, '会员记录不存在');

    const m = (member as any).toJSON();
    const currentLevelCode: string = m.levelCode;
    const currentLevelValue: number = m.level?.level ?? 0;
    const isPaid = !!m.isPaid;
    const paidExpireAt: Date | null = m.paidExpireAt ? new Date(m.paidExpireAt) : null;

    const requiredInfo = await this.getLevelByCode(t.requiredLevelCode);
    const requiredLevelValue = requiredInfo?.level ?? 0;
    const requiredLevelName = requiredInfo?.name ?? t.requiredLevelCode;

    if (currentLevelValue < requiredLevelValue) {
      return {
        allowed: false,
        reason: 'need_level',
        required: { levelCode: t.requiredLevelCode, levelName: requiredLevelName, requirePaid: t.requirePaid === 1 },
        current: { levelCode: currentLevelCode, isPaid },
      };
    }

    if (t.requirePaid === 1) {
      if (!isPaid) {
        return {
          allowed: false,
          reason: 'need_paid',
          required: { levelCode: t.requiredLevelCode, levelName: requiredLevelName, requirePaid: true },
          current: { levelCode: currentLevelCode, isPaid: false },
        };
      }
      if (paidExpireAt && paidExpireAt <= new Date()) {
        return {
          allowed: false,
          reason: 'paid_expired',
          required: { levelCode: t.requiredLevelCode, levelName: requiredLevelName, requirePaid: true },
          current: { levelCode: currentLevelCode, isPaid: true },
        };
      }
    }

    return { allowed: true, tool: { code: t.code, name: t.name, path: t.path } };
  }
```

- [ ] **Step 2: 在私有辅助区域追加 `getLevelByCode`**

在 `fetchCategories` 方法**后**追加：

```typescript
  private async getLevelByCode(code: string): Promise<{ level: number; name: string } | null> {
    const levels = await (this.service as any).member.getLevelList();
    const found = levels.find((l: any) => l.code === code);
    return found ? { level: found.level, name: found.name } : null;
  }
```

- [ ] **Step 3: 启动 dev 验证编译**

```bash
npm run dev
```
Expected: 无 TS 报错；终止。

- [ ] **Step 4: Commit**

```bash
git add app/service/tool.ts
git commit -m "feat(service): add checkToolAccess for permission validation"
```

---

## Task 8: Service — 管理端 CRUD + 批量发布

**Files:**
- Modify: `app/service/tool.ts`

- [ ] **Step 1: 在 `checkToolAccess` 后、私有辅助区前追加管理端方法**

```typescript
  // ==================== 管理端 - 分类 ====================

  async listCategoriesAdmin(query: any): Promise<PaginationResult<any>> {
    const { keyword, status, ...pagination } = query;
    const { Op, literal } = require('sequelize');
    const where: any = {};
    if (status !== undefined) where.status = Number(status);
    if (keyword) {
      const like = { [Op.like]: `%${keyword}%` };
      where[Op.or] = [{ name: like }, { code: like }];
    }
    return this.paginate<any>(
      this.ctx.model.ToolCategory,
      {
        where,
        order: [['sort', 'ASC'], ['id', 'ASC']],
        attributes: {
          include: [
            [literal('(SELECT COUNT(*) FROM tools t WHERE t.category_id = ToolCategory.id)'), 'toolCount'],
          ],
        },
      },
      pagination,
    );
  }

  async createCategory(data: any) {
    const exist = await this.ctx.model.ToolCategory.findOne({ where: { code: data.code } });
    if (exist) this.ctx.throw(409, '分类编码已存在');
    const created = await this.ctx.model.ToolCategory.create({
      code: data.code,
      name: data.name,
      icon: data.icon ?? null,
      description: data.description ?? null,
      sort: data.sort ?? 0,
      status: data.status ?? 1,
    } as any);
    await this.clearCache('tool:*');
    return (created as any).toJSON();
  }

  async updateCategory(id: number, data: any) {
    const cat = await this.ctx.model.ToolCategory.findByPk(id);
    if (!cat) this.ctx.throw(404, '分类不存在');
    const oldCode = (cat as any).code;
    if (data.code && data.code !== oldCode) {
      const dup = await this.ctx.model.ToolCategory.findOne({ where: { code: data.code } });
      if (dup) this.ctx.throw(409, '分类编码已存在');
    }
    await (cat as any).update({
      ...(data.code !== undefined && { code: data.code }),
      ...(data.name !== undefined && { name: data.name }),
      ...(data.icon !== undefined && { icon: data.icon }),
      ...(data.description !== undefined && { description: data.description }),
      ...(data.sort !== undefined && { sort: data.sort }),
      ...(data.status !== undefined && { status: data.status }),
    });
    if (data.code && data.code !== oldCode) {
      await this.ctx.model.Tool.update(
        { categoryCode: data.code },
        { where: { categoryId: id } } as any,
      );
    }
    await this.clearCache('tool:*');
    return (cat as any).toJSON();
  }

  async deleteCategory(id: number) {
    const cat = await this.ctx.model.ToolCategory.findByPk(id);
    if (!cat) this.ctx.throw(404, '分类不存在');
    const count = await this.ctx.model.Tool.count({ where: { categoryId: id } });
    if (count > 0) {
      this.ctx.throw(400, `该分类下尚有 ${count} 个工具，请先移除或删除后再操作`);
    }
    await (cat as any).destroy();
    await this.clearCache('tool:*');
  }

  // ==================== 管理端 - 工具 ====================

  async listToolsAdmin(query: any): Promise<PaginationResult<any>> {
    const { Op } = require('sequelize');
    const {
      categoryCode, status, isFeature, requiredLevelCode, requirePaid, keyword,
      ...pagination
    } = query;
    const where: any = {};
    if (categoryCode) where.categoryCode = categoryCode;
    if (status !== undefined) where.status = Number(status);
    if (isFeature !== undefined) where.isFeature = Number(isFeature);
    if (requiredLevelCode) where.requiredLevelCode = requiredLevelCode;
    if (requirePaid !== undefined) where.requirePaid = Number(requirePaid);
    if (keyword) {
      const like = { [Op.like]: `%${keyword}%` };
      where[Op.or] = [{ name: like }, { code: like }, { description: like }, { keyword: like }];
    }
    return this.paginate<any>(
      this.ctx.model.Tool,
      {
        where,
        order: [['updated_at', 'DESC']],
        include: [{ model: this.ctx.model.ToolCategory, as: 'category', attributes: ['id', 'code', 'name'] }],
      },
      pagination,
    );
  }

  async getToolById(id: number) {
    const tool = await this.ctx.model.Tool.findByPk(id, {
      include: [{ model: this.ctx.model.ToolCategory, as: 'category', attributes: ['id', 'code', 'name'] }],
    });
    if (!tool) this.ctx.throw(404, '工具不存在');
    return (tool as any).toJSON();
  }

  async createTool(data: any) {
    const exist = await this.ctx.model.Tool.findOne({ where: { code: data.code } });
    if (exist) this.ctx.throw(409, '工具编码已存在');
    const category = await this.ctx.model.ToolCategory.findByPk(data.categoryId);
    if (!category) this.ctx.throw(400, '分类不存在');
    const tool = await this.ctx.model.Tool.create({
      code: data.code,
      name: data.name,
      description: data.description || '',
      keyword: data.keyword || '',
      categoryId: data.categoryId,
      categoryCode: (category as any).code,
      icon: data.icon || '',
      color: data.color || '',
      path: data.path,
      isFeature: data.isFeature ?? 0,
      requiredLevelCode: data.requiredLevelCode ?? 'free',
      requirePaid: data.requirePaid ?? 0,
      status: data.status ?? 0,
      sort: data.sort ?? 0,
    } as any);
    await this.clearCache('tool:*');
    return (tool as any).toJSON();
  }

  async updateTool(id: number, data: any) {
    const tool = await this.ctx.model.Tool.findByPk(id);
    if (!tool) this.ctx.throw(404, '工具不存在');
    const oldCode = (tool as any).code;
    if (data.code && data.code !== oldCode) {
      const dup = await this.ctx.model.Tool.findOne({ where: { code: data.code } });
      if (dup) this.ctx.throw(409, '工具编码已存在');
    }
    let categoryCode: string | undefined;
    if (data.categoryId !== undefined && data.categoryId !== (tool as any).categoryId) {
      const category = await this.ctx.model.ToolCategory.findByPk(data.categoryId);
      if (!category) this.ctx.throw(400, '分类不存在');
      categoryCode = (category as any).code;
    }
    const updateData: any = {};
    const fields = ['code', 'name', 'description', 'keyword', 'categoryId', 'icon', 'color', 'path', 'isFeature', 'requiredLevelCode', 'requirePaid', 'status', 'sort'];
    for (const f of fields) if (data[f] !== undefined) updateData[f] = data[f];
    if (categoryCode) updateData.categoryCode = categoryCode;
    await (tool as any).update(updateData);
    await this.clearCache('tool:*');
    return (tool as any).toJSON();
  }

  async deleteTool(id: number) {
    const tool = await this.ctx.model.Tool.findByPk(id);
    if (!tool) this.ctx.throw(404, '工具不存在');
    await (tool as any).destroy();
    await this.clearCache('tool:*');
  }

  async batchPublish(ids: number[], status: 0 | 1): Promise<{ affected: number }> {
    if (!Array.isArray(ids) || ids.length === 0) this.ctx.throw(422, '参数验证失败');
    if (ids.length > 500) this.ctx.throw(422, '单次最多处理 500 个工具');
    if (status !== 0 && status !== 1) this.ctx.throw(422, '参数验证失败');
    const { Op } = require('sequelize');
    const [affected] = await this.ctx.model.Tool.update(
      { status },
      { where: { id: { [Op.in]: ids } } } as any,
    );
    await this.clearCache('tool:*');
    return { affected };
  }
```

- [ ] **Step 2: 启动 dev 验证编译**

```bash
npm run dev
```
Expected: 无 TS 报错；终止。

- [ ] **Step 3: Commit**

```bash
git add app/service/tool.ts
git commit -m "feat(service): add admin CRUD + batchPublish for ToolService"
```

---

## Task 9: Controller — H5 端 + 路由

**Files:**
- Create: `app/controller/tool.ts`
- Modify: `app/router.ts`

- [ ] **Step 1: 创建 H5 端 Controller**

Create `app/controller/tool.ts`：

```typescript
import BaseController from './base';

/**
 * H5 端工具 Controller
 * 路由前缀: /api/tools
 */
export default class ToolController extends BaseController {

  /** GET /api/tools/home — 首页聚合/分页双模式 */
  async home() {
    const { categoryCode, keyword, page, pageSize } = this.ctx.query;
    const data = await this.service.tool.getHomeData({
      categoryCode: categoryCode ? String(categoryCode) : undefined,
      keyword: keyword ? String(keyword) : undefined,
      page: page ? Number(page) : undefined,
      pageSize: pageSize ? Number(pageSize) : undefined,
    });
    this.success(data);
  }

  /** GET /api/tools/feature — 特色 Tab */
  async featureList() {
    const pagination = this.getPagination();
    const result = await this.service.tool.getFeatureTools(pagination);
    this.paginated(result);
  }

  /** GET /api/tools/member — 会员专属 Tab */
  async memberList() {
    const pagination = this.getPagination();
    const result = await this.service.tool.getMemberTools(pagination);
    this.paginated(result);
  }

  /** GET /api/tools/:code/access — 使用前权限校验（需登录） */
  async checkAccess() {
    const userId = (this.ctx.state.user as any)?.userId;
    if (!userId) this.ctx.throw(401, '请提供认证Token');
    const code = String(this.ctx.params.code);
    const data = await this.service.tool.checkToolAccess(userId, code);
    this.success(data);
  }
}
```

- [ ] **Step 2: 注册 H5 端路由**

打开 `app/router.ts`，在 `// ==================== 会员管理（管理端） ====================` 块后追加：

```typescript
  // ==================== 工具（H5 端） ====================
  router.get('/api/tools/home', controller.tool.home);
  router.get('/api/tools/feature', controller.tool.featureList);
  router.get('/api/tools/member', controller.tool.memberList);
  router.get('/api/tools/:code/access', auth, controller.tool.checkAccess);
```

- [ ] **Step 3: 启动 dev 冒烟**

```bash
npm run dev
```
新终端执行：
```bash
curl http://127.0.0.1:7001/api/tools/home
```
Expected: `{ code: 200, data: { mode: 'aggregate', categories: [11 项] } }`，所有分类 tools 总数 = 233。Ctrl+C 终止。

- [ ] **Step 4: Commit**

```bash
git add app/controller/tool.ts app/router.ts
git commit -m "feat(controller): add H5 ToolController with 4 routes"
```

---

## Task 10: Controller — 管理端 + 路由

**Files:**
- Create: `app/controller/admin/tool.ts`
- Modify: `app/router.ts`

- [ ] **Step 1: 创建管理端 Controller**

Create `app/controller/admin/tool.ts`：

```typescript
import BaseController from '../base';

/**
 * 管理端工具 Controller
 * 路由前缀: /api/admin/tool-categories  与  /api/admin/tools
 */
export default class AdminToolController extends BaseController {

  // ==================== 分类 ====================

  /** GET /api/admin/tool-categories */
  async listCategories() {
    const pagination = this.getPagination();
    const { keyword, status } = this.ctx.query;
    const result = await this.service.tool.listCategoriesAdmin({
      ...pagination,
      keyword: keyword ? String(keyword) : undefined,
      status: status !== undefined ? Number(status) : undefined,
    });
    this.paginated(result);
  }

  /** POST /api/admin/tool-categories */
  async createCategory() {
    this.validate({
      code: { type: 'string', required: true, min: 2, max: 30 },
      name: { type: 'string', required: true, min: 1, max: 50 },
      icon: { type: 'string', required: false, max: 500 },
      description: { type: 'string', required: false, max: 500 },
      sort: { type: 'number', required: false },
      status: { type: 'enum', values: [0, 1], required: false },
    });
    const data = await this.service.tool.createCategory(this.ctx.request.body);
    this.created(data);
  }

  /** PUT /api/admin/tool-categories/:id */
  async updateCategory() {
    const data = await this.service.tool.updateCategory(
      Number(this.ctx.params.id),
      this.ctx.request.body,
    );
    this.success(data, '更新成功');
  }

  /** DELETE /api/admin/tool-categories/:id */
  async deleteCategory() {
    await this.service.tool.deleteCategory(Number(this.ctx.params.id));
    this.success(null, '删除成功');
  }

  // ==================== 工具 ====================

  /** GET /api/admin/tools */
  async listTools() {
    const pagination = this.getPagination();
    const { categoryCode, status, isFeature, requiredLevelCode, requirePaid, keyword } = this.ctx.query;
    const result = await this.service.tool.listToolsAdmin({
      ...pagination,
      categoryCode: categoryCode ? String(categoryCode) : undefined,
      status: status !== undefined ? Number(status) : undefined,
      isFeature: isFeature !== undefined ? Number(isFeature) : undefined,
      requiredLevelCode: requiredLevelCode ? String(requiredLevelCode) : undefined,
      requirePaid: requirePaid !== undefined ? Number(requirePaid) : undefined,
      keyword: keyword ? String(keyword) : undefined,
    });
    this.paginated(result);
  }

  /** GET /api/admin/tools/:id */
  async showTool() {
    const data = await this.service.tool.getToolById(Number(this.ctx.params.id));
    this.success(data);
  }

  /** POST /api/admin/tools */
  async createTool() {
    this.validate({
      code: { type: 'string', required: true, min: 2, max: 60 },
      name: { type: 'string', required: true, min: 1, max: 80 },
      description: { type: 'string', required: false, max: 500 },
      keyword: { type: 'string', required: false, max: 500 },
      categoryId: { type: 'number', required: true },
      icon: { type: 'string', required: false, max: 500 },
      color: { type: 'string', required: false, max: 20 },
      path: { type: 'string', required: true, min: 1, max: 200 },
      isFeature: { type: 'enum', values: [0, 1], required: false },
      requiredLevelCode: { type: 'enum', values: ['free', 'silver', 'gold', 'diamond', 'black'], required: false },
      requirePaid: { type: 'enum', values: [0, 1], required: false },
      status: { type: 'enum', values: [0, 1], required: false },
      sort: { type: 'number', required: false },
    });
    const data = await this.service.tool.createTool(this.ctx.request.body);
    this.created(data);
  }

  /** PUT /api/admin/tools/:id */
  async updateTool() {
    const data = await this.service.tool.updateTool(
      Number(this.ctx.params.id),
      this.ctx.request.body,
    );
    this.success(data, '更新成功');
  }

  /** DELETE /api/admin/tools/:id */
  async deleteTool() {
    await this.service.tool.deleteTool(Number(this.ctx.params.id));
    this.success(null, '删除成功');
  }

  /** PUT /api/admin/tools/batch-publish */
  async batchPublish() {
    this.validate({
      ids: { type: 'array', itemType: 'number', required: true, min: 1, max: 500 },
      status: { type: 'enum', values: [0, 1], required: true },
    });
    const { ids, status } = this.ctx.request.body;
    const data = await this.service.tool.batchPublish(ids, status);
    this.success(data, '批量处理成功');
  }
}
```

- [ ] **Step 2: 注册管理端路由**

打开 `app/router.ts`，在 Task 9 添加的 H5 端路由块后追加：

```typescript
  // ==================== 工具分类管理（管理端） ====================
  router.get('/api/admin/tool-categories', auth, controller.admin.tool.listCategories);
  router.post('/api/admin/tool-categories', auth, controller.admin.tool.createCategory);
  router.put('/api/admin/tool-categories/:id', auth, controller.admin.tool.updateCategory);
  router.delete('/api/admin/tool-categories/:id', auth, controller.admin.tool.deleteCategory);

  // ==================== 工具管理（管理端） ====================
  // 注意: batch-publish 必须放在 :id 路由之前，避免被 :id 参数捕获
  router.put('/api/admin/tools/batch-publish', auth, controller.admin.tool.batchPublish);
  router.get('/api/admin/tools', auth, controller.admin.tool.listTools);
  router.get('/api/admin/tools/:id', auth, controller.admin.tool.showTool);
  router.post('/api/admin/tools', auth, controller.admin.tool.createTool);
  router.put('/api/admin/tools/:id', auth, controller.admin.tool.updateTool);
  router.delete('/api/admin/tools/:id', auth, controller.admin.tool.deleteTool);
```

- [ ] **Step 3: 启动 dev 冒烟**

```bash
npm run dev
```
新终端先登录拿 token（账号见 `test/helpers/setup.ts` 的 `TEST_ADMIN`），然后：
```bash
curl -H "Authorization: Bearer <TOKEN>" "http://127.0.0.1:7001/api/admin/tool-categories?page=1&pageSize=5"
```
Expected: `total: 11`，list 含 5 项。Ctrl+C 终止。

- [ ] **Step 4: Commit**

```bash
git add app/controller/admin/tool.ts app/router.ts
git commit -m "feat(controller): add Admin ToolController with 10 routes"
```

---

## Task 11: 集成测试 — H5 端

**Files:**
- Create: `test/api/tool.test.ts`

- [ ] **Step 1: 创建 H5 端测试文件**

Create `test/api/tool.test.ts`：

```typescript
import assert from 'assert';
import { MockApplication } from 'egg-mock';
import mm from 'egg-mock';
import { TEST_CLIENT, TEST_USER, assertSuccess } from '../helpers/setup';

describe('Tool API /api/tools (H5)', () => {
  let app: MockApplication;
  let userToken: string;

  before(async () => {
    app = mm.app({ baseDir: process.cwd() });
    await app.ready();
    const res = await app.httpRequest()
      .post('/api/auth/login')
      .send({
        username: TEST_USER.username,
        password: TEST_USER.password,
        clientId: TEST_CLIENT.clientId,
        clientSecret: TEST_CLIENT.clientSecret,
      });
    userToken = res.body.data?.accessToken;
  });

  after(async () => {
    await app.close();
    mm.restore();
  });

  describe('GET /api/tools/home', () => {
    it('aggregate mode (no query) returns 11 categories with 233 tools', async () => {
      const res = await app.httpRequest().get('/api/tools/home').expect(200);
      assertSuccess(res.body);
      assert.strictEqual(res.body.data.mode, 'aggregate');
      assert.strictEqual(res.body.data.categories.length, 11);
      const total = res.body.data.categories.reduce((s: number, c: any) => s + (c.tools?.length || 0), 0);
      assert.strictEqual(total, 233);
    });

    it('paginated mode by categoryCode=DAILY returns 16 tools', async () => {
      const res = await app.httpRequest()
        .get('/api/tools/home?categoryCode=DAILY&page=1&pageSize=20')
        .expect(200);
      assertSuccess(res.body);
      assert.strictEqual(res.body.data.mode, 'paginated');
      assert.strictEqual(res.body.data.tools.total, 16);
    });

    it('paginated mode by keyword=黄金 hits gold-price', async () => {
      const res = await app.httpRequest().get('/api/tools/home?keyword=黄金').expect(200);
      assertSuccess(res.body);
      assert.ok(res.body.data.tools.total >= 1);
      const codes = res.body.data.tools.list.map((t: any) => t.code);
      assert.ok(codes.includes('gold-price'));
    });

    it('non-existent categoryCode returns empty list', async () => {
      const res = await app.httpRequest()
        .get('/api/tools/home?categoryCode=NOT_EXIST')
        .expect(200);
      assertSuccess(res.body);
      assert.strictEqual(res.body.data.tools.total, 0);
    });
  });

  describe('GET /api/tools/feature', () => {
    it('returns empty (seeds have is_feature=0)', async () => {
      const res = await app.httpRequest().get('/api/tools/feature').expect(200);
      assertSuccess(res.body);
      assert.strictEqual(res.body.data.total, 0);
    });
  });

  describe('GET /api/tools/member', () => {
    it('returns empty (seeds have required_level=free && require_paid=0)', async () => {
      const res = await app.httpRequest().get('/api/tools/member').expect(200);
      assertSuccess(res.body);
      assert.strictEqual(res.body.data.total, 0);
    });
  });

  describe('GET /api/tools/:code/access', () => {
    it('without token returns 401', async () => {
      await app.httpRequest().get('/api/tools/gold-price/access').expect(401);
    });

    it('free tool with logged-in user returns allowed=true', async () => {
      const res = await app.httpRequest()
        .get('/api/tools/gold-price/access')
        .set('Authorization', `Bearer ${userToken}`)
        .expect(200);
      assertSuccess(res.body);
      assert.strictEqual(res.body.data.allowed, true);
      assert.strictEqual(res.body.data.tool?.code, 'gold-price');
    });

    it('non-existent tool returns 404', async () => {
      await app.httpRequest()
        .get('/api/tools/__not_exist__/access')
        .set('Authorization', `Bearer ${userToken}`)
        .expect(404);
    });
  });
});
```

- [ ] **Step 2: 运行测试**

按项目惯例运行测试（具体命令请参考 `package.json` 的 `scripts.test`）：

```bash
npm test -- --grep "Tool API"
```
Expected: 全部用例通过。如失败，请按 `setup.ts` 中 `TEST_USER` 实际等级状态调整断言（如 TEST_USER 不是 free 等级，需相应修改预期）。

- [ ] **Step 3: Commit**

```bash
git add test/api/tool.test.ts
git commit -m "test(tool): add H5 ToolController integration tests"
```

---

## Task 12: 集成测试 — 管理端

**Files:**
- Create: `test/api/admin-tool.test.ts`

- [ ] **Step 1: 创建管理端测试文件**

Create `test/api/admin-tool.test.ts`：

```typescript
import assert from 'assert';
import { MockApplication } from 'egg-mock';
import mm from 'egg-mock';
import { TEST_CLIENT, TEST_ADMIN, assertSuccess } from '../helpers/setup';

describe('Admin Tool API /api/admin', () => {
  let app: MockApplication;
  let adminToken: string;
  let createdCategoryId: number;
  let createdToolId: number;

  before(async () => {
    app = mm.app({ baseDir: process.cwd() });
    await app.ready();
    const res = await app.httpRequest()
      .post('/api/auth/login')
      .send({
        username: TEST_ADMIN.username,
        password: TEST_ADMIN.password,
        clientId: TEST_CLIENT.clientId,
        clientSecret: TEST_CLIENT.clientSecret,
      });
    adminToken = res.body.data?.accessToken;
  });

  after(async () => {
    await app.close();
    mm.restore();
  });

  // ==================== 分类 ====================

  describe('Tool Categories CRUD', () => {
    it('GET returns >= 11 seeded categories', async () => {
      const res = await app.httpRequest()
        .get('/api/admin/tool-categories?page=1&pageSize=20')
        .set('Authorization', `Bearer ${adminToken}`)
        .expect(200);
      assertSuccess(res.body);
      assert.ok(res.body.data.total >= 11);
    });

    it('POST creates category', async () => {
      const res = await app.httpRequest()
        .post('/api/admin/tool-categories')
        .set('Authorization', `Bearer ${adminToken}`)
        .send({ code: 'TEST_CAT_X', name: '测试分类X', sort: 99 })
        .expect(201);
      assert.strictEqual(res.body.code, 201);
      createdCategoryId = res.body.data.id;
    });

    it('POST duplicate code returns 409', async () => {
      const res = await app.httpRequest()
        .post('/api/admin/tool-categories')
        .set('Authorization', `Bearer ${adminToken}`)
        .send({ code: 'TEST_CAT_X', name: '重复' });
      assert.strictEqual(res.status, 409);
    });

    it('PUT updates name', async () => {
      const res = await app.httpRequest()
        .put(`/api/admin/tool-categories/${createdCategoryId}`)
        .set('Authorization', `Bearer ${adminToken}`)
        .send({ name: '测试分类X-改' })
        .expect(200);
      assertSuccess(res.body);
      assert.strictEqual(res.body.data.name, '测试分类X-改');
    });

    it('DELETE empty category succeeds', async () => {
      await app.httpRequest()
        .delete(`/api/admin/tool-categories/${createdCategoryId}`)
        .set('Authorization', `Bearer ${adminToken}`)
        .expect(200);
    });

    it('DELETE seed category with tools returns 400', async () => {
      const list = await app.httpRequest()
        .get('/api/admin/tool-categories?page=1&pageSize=20&keyword=DAILY')
        .set('Authorization', `Bearer ${adminToken}`);
      const daily = list.body.data.list.find((c: any) => c.code === 'DAILY');
      assert.ok(daily);
      const res = await app.httpRequest()
        .delete(`/api/admin/tool-categories/${daily.id}`)
        .set('Authorization', `Bearer ${adminToken}`);
      assert.strictEqual(res.status, 400);
    });
  });

  // ==================== 工具 ====================

  describe('Tools CRUD', () => {
    it('GET returns 233 tools total', async () => {
      const res = await app.httpRequest()
        .get('/api/admin/tools?page=1&pageSize=20')
        .set('Authorization', `Bearer ${adminToken}`)
        .expect(200);
      assertSuccess(res.body);
      assert.strictEqual(res.body.data.total, 233);
    });

    it('GET filter categoryCode=DAILY returns 16', async () => {
      const res = await app.httpRequest()
        .get('/api/admin/tools?page=1&pageSize=50&categoryCode=DAILY')
        .set('Authorization', `Bearer ${adminToken}`)
        .expect(200);
      assert.strictEqual(res.body.data.total, 16);
    });

    it('POST creates new tool', async () => {
      const list = await app.httpRequest()
        .get('/api/admin/tool-categories?page=1&pageSize=20')
        .set('Authorization', `Bearer ${adminToken}`);
      const daily = list.body.data.list.find((c: any) => c.code === 'DAILY');
      const res = await app.httpRequest()
        .post('/api/admin/tools')
        .set('Authorization', `Bearer ${adminToken}`)
        .send({
          code: 'test-tool-zzz',
          name: '测试工具ZZZ',
          description: '集成测试',
          categoryId: daily.id,
          path: '/test-tool-zzz',
          status: 0,
        })
        .expect(201);
      assert.ok(res.body.data?.id);
      createdToolId = res.body.data.id;
    });

    it('PUT updates isFeature=1 and status=1', async () => {
      const res = await app.httpRequest()
        .put(`/api/admin/tools/${createdToolId}`)
        .set('Authorization', `Bearer ${adminToken}`)
        .send({ isFeature: 1, status: 1 })
        .expect(200);
      assertSuccess(res.body);
      assert.strictEqual(res.body.data.isFeature, 1);
      assert.strictEqual(res.body.data.status, 1);
    });

    it('GET /api/tools/feature now returns >=1 (created tool is feature)', async () => {
      const res = await app.httpRequest().get('/api/tools/feature').expect(200);
      assertSuccess(res.body);
      assert.ok(res.body.data.total >= 1);
    });

    it('PUT batch-publish to status=0 affects >=1', async () => {
      const res = await app.httpRequest()
        .put('/api/admin/tools/batch-publish')
        .set('Authorization', `Bearer ${adminToken}`)
        .send({ ids: [createdToolId], status: 0 })
        .expect(200);
      assertSuccess(res.body);
      assert.ok(res.body.data.affected >= 1);
    });

    it('PUT batch-publish empty ids returns 422', async () => {
      const res = await app.httpRequest()
        .put('/api/admin/tools/batch-publish')
        .set('Authorization', `Bearer ${adminToken}`)
        .send({ ids: [], status: 1 });
      assert.strictEqual(res.status, 422);
    });

    it('POST with non-existent categoryId returns 400', async () => {
      const res = await app.httpRequest()
        .post('/api/admin/tools')
        .set('Authorization', `Bearer ${adminToken}`)
        .send({
          code: 'test-tool-invalid-cat',
          name: 'X',
          categoryId: 999999,
          path: '/x',
        });
      assert.strictEqual(res.status, 400);
    });

    it('DELETE created tool succeeds', async () => {
      await app.httpRequest()
        .delete(`/api/admin/tools/${createdToolId}`)
        .set('Authorization', `Bearer ${adminToken}`)
        .expect(200);
    });

    it('unauthorized request returns 401', async () => {
      await app.httpRequest().get('/api/admin/tools').expect(401);
    });
  });
});
```

- [ ] **Step 2: 运行测试**

```bash
npm test -- --grep "Admin Tool API"
```
Expected: 全部用例通过。

- [ ] **Step 3: 全量回归**

```bash
npm test
```
Expected: 原有 auth / member 等模块测试未受影响，全部通过。

- [ ] **Step 4: Commit**

```bash
git add test/api/admin-tool.test.ts
git commit -m "test(tool): add Admin ToolController integration tests"
```

---

## Task 13: 文档 + 索引 + 变更日志

**Files:**
- Create: `docs/api/tool/README.md`
- Create: `docs/api/admin-tool/README.md`
- Modify: `docs/api/README.md`
- Create or Modify: `docs/changelog/2026-05.md`
- Modify: `docs/database/README.md`（如存在）

- [ ] **Step 1: 创建 H5 端 API 文档**

Create `docs/api/tool/README.md`：

```markdown
# 工具模块 API 文档（H5 端）

> 路由前缀: `/api/tools`
> 设计文档: [工具管理模块设计文档](../../superpowers/specs/2026-05-06-工具管理模块设计文档.md)

## 接口清单

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| GET | `/api/tools/home` | 否 | 首页聚合/分页双模式 |
| GET | `/api/tools/feature` | 否 | 特色功能 Tab |
| GET | `/api/tools/member` | 否 | 会员专属 Tab |
| GET | `/api/tools/:code/access` | 是 | 使用前权限校验 |

## 1. GET /api/tools/home

### Query
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| categoryCode | string | 否 | 分类编码，传入则走分页模式 |
| keyword | string | 否 | 关键字搜索，传入则走分页模式 |
| page | int | 否 | 默认 1 |
| pageSize | int | 否 | 默认 20，最大 100 |

### 响应（聚合模式）
```json
{ "code": 200, "data": { "mode": "aggregate", "categories": [{ "id":1, "code":"DAILY", "name":"日常应用", "tools":[...] }] } }
```

### 响应（分页模式）
```json
{ "code": 200, "data": { "mode": "paginated", "categories": [...], "tools": { "list":[...], "total":16, "page":1, "pageSize":20, "totalPages":1 } } }
```

## 2. GET /api/tools/feature

分页返回 `is_feature=1 AND status=1` 的工具。

## 3. GET /api/tools/member

分页返回 `status=1 AND (required_level_code != 'free' OR require_paid = 1)` 的工具。

## 4. GET /api/tools/:code/access

### 响应（allowed=true）
```json
{ "code": 200, "data": { "allowed": true, "tool": { "code":"ai-chat", "name":"AI对话", "path":"/ai-chat" } } }
```

### 响应（allowed=false）
```json
{ "code": 200, "data": { "allowed": false, "reason": "need_level",
  "required": { "levelCode":"silver", "levelName":"银牌会员", "requirePaid": false },
  "current":  { "levelCode":"free", "isPaid": false } } }
```

`reason`: `need_level` | `need_paid` | `paid_expired`

### 错误
| HTTP | 场景 |
|------|------|
| 401 | 未登录 |
| 404 | 工具不存在 |
| 400 | 工具已下架 |
```

- [ ] **Step 2: 创建管理端 API 文档**

Create `docs/api/admin-tool/README.md`：

```markdown
# 工具管理 API 文档（管理端）

> 路由前缀: `/api/admin/tool-categories`、`/api/admin/tools`
> 鉴权: 全部需 auth

## 分类接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/tool-categories` | 分类列表（分页） |
| POST | `/api/admin/tool-categories` | 创建分类 |
| PUT | `/api/admin/tool-categories/:id` | 更新分类 |
| DELETE | `/api/admin/tool-categories/:id` | 删除分类（旗下有工具则 400） |

### POST Body
```json
{ "code":"TEST", "name":"测试", "icon":null, "description":null, "sort":0, "status":1 }
```

## 工具接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/tools` | 工具列表（支持 categoryCode/status/isFeature/requiredLevelCode/requirePaid/keyword 筛选） |
| GET | `/api/admin/tools/:id` | 工具详情 |
| POST | `/api/admin/tools` | 创建工具 |
| PUT | `/api/admin/tools/:id` | 更新工具 |
| DELETE | `/api/admin/tools/:id` | 删除工具 |
| PUT | `/api/admin/tools/batch-publish` | 批量发布/下架 |

### POST Body
```json
{
  "code":"my-tool", "name":"我的工具", "description":"...",
  "keyword":"kw1|kw2", "categoryId":1, "icon":"/icons/x.svg",
  "color":"#F39C12", "path":"/my-tool",
  "isFeature":0, "requiredLevelCode":"free", "requirePaid":0,
  "status":0, "sort":0
}
```

### batch-publish Body
```json
{ "ids": [1, 2, 3], "status": 1 }
```
响应：`{ "code":200, "data":{ "affected": 3 } }`
```

- [ ] **Step 3: 更新全局 API 索引**

打开 `docs/api/README.md`（如果存在），在接口索引表末尾追加：
```markdown
| [工具模块（H5）](tool/README.md) | 首页/特色/会员/权限校验（4个） | ✅ 已发布 |
| [工具管理（管理端）](admin-tool/README.md) | 分类+工具 CRUD + 批量发布（10个） | ✅ 已发布 |
```

如果文件不存在则跳过此步。

- [ ] **Step 4: 创建或更新变更日志**

Create or modify `docs/changelog/2026-05.md`，追加：

```markdown
# 2026-05 变更日志

## 2026-05-06 — 新增工具管理模块

### 新增
- 数据库：`tool_categories`、`tools` 两张表（migration 004）
- 种子数据：11 个工具分类 + 233 个工具（来源 `test1.js`）
- Model：`ToolCategory`、`Tool`
- Service：`ToolService`（H5 聚合/筛选、权限校验、管理端 CRUD、批量发布、缓存治理）
- Controller：H5 端 `tool.ts`、管理端 `admin/tool.ts`
- Router：14 条路由
- 错误码：`1008xx` 工具模块区间

### 接口数量
- H5 端：4 个（/home, /feature, /member, /:code/access）
- 管理端：10 个（分类 CRUD × 4 + 工具 CRUD × 5 + 批量发布 × 1）

### 设计文档
- `docs/superpowers/specs/2026-05-06-工具管理模块设计文档.md`
- `docs/superpowers/plans/2026-05-06-工具管理模块实施计划.md`
```

- [ ] **Step 5: 更新 docs/README.md 顶层索引**

打开 `docs/README.md`，在 API 文档表中追加工具模块的 2 行索引（仿照现有"认证模块"的格式）。

- [ ] **Step 6: Commit**

```bash
git add docs/api/tool/README.md docs/api/admin-tool/README.md docs/changelog/2026-05.md docs/README.md
# 如果修改了 docs/api/README.md 也一并 add
git commit -m "docs(tool): add API docs and changelog for tool module"
```

---

## Self-Review（计划自审清单）

### 1. Spec 覆盖

| Spec 章节 | 对应任务 |
|----------|---------|
| 第三节 数据库 | Task 2, 3, 4, 5 |
| 第四节 权限模型 | Task 7（checkToolAccess） |
| 5.1 H5 接口（4 个） | Task 9（Controller）+ Task 11（测试） |
| 5.2 安全 | Task 9 中 auth 中间件 |
| 第六节 Service 方法 | Task 6/7/8 |
| 第七节 缓存 | Task 6（fetchCategories / getHomeData）+ Task 8（clearCache） |
| 第八节 迁移 | Task 2, 3 |
| 第九节 管理端（10 个） | Task 10 + Task 12（测试） |
| 第十节 测试 | Task 11, 12 |
| 附录 A 枚举 | Task 1（错误码）+ Task 10（validate 规则） |

无遗漏。

### 2. 占位符扫描

- ✅ 无 "TBD / TODO / 后续补充" 等字样
- ✅ 所有 Step 都给出了完整代码或命令
- ✅ 所有断言都有具体预期值（11、233、16 等）

### 3. 类型一致性

- `ToolService` 中定义的类型在 Controller（Task 9/10）和测试（Task 11/12）中保持一致：`HomeResult` 的 `mode` 字段为 `'aggregate' | 'paginated'` — 测试中以字符串判断 ✅
- `AccessResult.reason` 为 `'need_level' | 'need_paid' | 'paid_expired'`，文档中一致 ✅
- Model 属性 camelCase（`isFeature`、`requiredLevelCode`、`requirePaid`、`categoryCode`）在 Service / Controller / 测试中统一 ✅
- `batchPublish` 签名在 spec / Service / Controller / 测试中均为 `(ids: number[], status: 0|1) => { affected: number }` ✅

### 4. 注意事项

- **路由顺序**：`PUT /api/admin/tools/batch-publish` 必须在 `PUT /api/admin/tools/:id` 之前（已在 Task 10 注释中强调）
- **数据库部署（Task 3）**：HARD-GATE，必须等待用户确认
- **测试用户等级**：Task 11 假设 `TEST_USER` 为 free 等级用户。若实际非 free，需相应调整断言
- **categoryCode 中文 keyword 搜索**：MySQL 的 `utf8mb4` + `LIKE %黄金%` 原生支持，无需 FULLTEXT 索引

---

## 执行选择

计划已完成。请选择执行方式：

### 1. Subagent-Driven（推荐）
我为每个任务派发独立的 code-reviewer / 子代理执行并在任务间给你检查点，串行迭代、易控。

### 2. Inline Execution
由当前 session 顺序执行 Task 1-13，到关键检查点（Task 3 数据库部署、Task 11/12 测试、Task 13 文档）暂停让你 review。

**请回复 "1" 或 "2"** 选择执行方式，我将相应调用 `subagent-driven-development` 或 `executing-plans` 技能开始执行。
