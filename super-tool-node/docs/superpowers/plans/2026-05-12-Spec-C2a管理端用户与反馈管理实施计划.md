# Spec-C2a 管理端用户与反馈管理 Implementation Plan

> ⚠️ **部分内容已过时（2026-05-12 user_type 重构）**：本计划中涉及 `userType` 筛选/下拉选择、`USER_TYPE_LABELS` 标签映射等部分已废弃。当前实现改为 `registerSource`（注册来源平台）。详见 [user_type 重构设计文档](../specs/2026-05-12-user-type-refactor-design.md)。

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 super-tool-node + super-tools-admin 联合实现管理端的"用户管理"+"反馈管理"两个二级模块。后端补 4 接口（reset-password / status / admin-list-devices / admin-list-addresses）+ 1 SQL 迁移；前端补 2 个页面（含 3 Tab 详情抽屉、状态机白名单、列表+抽屉双入口状态切换）。

**Architecture:** egg.js Sequelize 后端 + UmiJS 3 + Antd 5 + dva 前端；services 薄封装；utils 抽离状态机白名单常量；pages/User + pages/Feedback 各自独立子目录；测试用 .tsx + babel-jest（避开 ts-jest 26 + TS 5.4 兼容性问题，C1 已验证）。

**Tech Stack:** TypeScript 5.4 / React 18 / UmiJS 3.5 / Antd 5.15 / dva 2.6 / Jest 26 / egg.js / Sequelize / MySQL

**Spec Reference:** `docs/superpowers/specs/2026-05-12-Spec-C2a管理端用户与反馈管理设计文档.md`

**Worktree:** `d:\Donough\Projects\super-tools-spec-c2a`（基于 master @ `ab0a127`，分支 `spec-c2a`）— 已创建（含 admin/node_modules + admin/src/.umi + super-tool-node/node_modules 三个 junction）

---

## 任务总览

| Task | 范围 | 文件数 | 估行数 |
|---|---|---|---|
| T0 | worktree + junction（**已完成**） | 0 | 0 |
| T1 | 后端契约联调 + 数据库迁移 SQL | 1 新 + 1 改 | +60 |
| T2 | 后端 audit action 词表扩展 + service 增量 | 1-2 改 | +50 |
| T3 | 后端 controller 增量 | 1 改 | +90 |
| T4 | 后端 router.ts 增量 | 1 改 | +5 |
| T5 | 后端 manual smoke test | - | - |
| T6 | admin 路由模块填充（user.ts / feedback.ts） | 2 改 | +20 |
| T7 | admin services 新建（user.ts / feedback.ts） | 2 新 | +180 |
| T8 | admin utils 新建（feedbackStatus.ts / userType.ts） | 2 新 | +50 |
| T9 | admin pages/User 列表 + Modal + ResetPwdModal + Drawer 占位 | 4 新 + 1 less | +400 |
| T10 | admin pages/User 详情抽屉 3 Tab 完整化 | 3 新 + 1 改 | +300 |
| T11 | admin pages/Feedback 列表 + Drawer | 4 新 + 1 less | +500 |
| T12 | admin 4 个测试文件 ≥ 19 case | 4 新 | +160 |
| T13 | type-check + lint + jest 全跑 | - | - |
| T14 | 前端 manual smoke test（与 9 项验收清单对齐） | - | - |
| T15 | spec + plan + 实施 commits 整合 push & 合并 master | - | - |

**预期总量**：20 新建 + 8 修改 ≈ **1865 行**（含 spec 1512 + plan ~1100）。

---

## Task 0: worktree + junction（已完成 ✅）

**Files:** 无（基础设施）

> 已在 brainstorming 收尾后创建：
> - worktree path: `d:\Donough\Projects\super-tools-spec-c2a`
> - branch: `spec-c2a`
> - 3 junctions: admin/node_modules + admin/src/.umi + super-tool-node/node_modules

跳过此 Task。如需重建，参考 spec § 1 命令段。

---

## Task 1: 后端契约联调 + 数据库迁移

**Spec:** §2 + §3

**Files:**
- New: `super-tool-node/database/009_add_user_admin_actions.sql`
- Modify: `super-tool-node/database/README.md`
- Verify: `GET /api/users/2`、`GET /api/admin/feedbacks?page=1`（curl）

### Step 1: 启动主仓库后端 + 验证现有接口契约

确保主仓库（master）的 super-tool-node 已启动：

```powershell
Set-Location 'd:\Donough\Projects\super-tools\super-tool-node'
npm run dev
```

获取 admin token + 验证现有 user/feedback list 返回结构：

```powershell
$body = '{"username":"admin","password":"Admin@123456","clientId":"admin_client","clientSecret":"ADMIN_SECRET"}'
$loginRes = Invoke-RestMethod -Uri "http://localhost:7001/api/auth/login" -Method POST -Body $body -ContentType "application/json"
$token = $loginRes.data.accessToken
$headers = @{ "Authorization" = "Bearer $token" }

# 验证 user list 返回结构（关注是 data.list 还是 data.rows）
$r = Invoke-RestMethod -Uri "http://localhost:7001/api/users?page=1&pageSize=5" -Headers $headers
$r.data | ConvertTo-Json -Depth 3 | Select-Object -First 30

# 验证 feedback list 返回结构
$f = Invoke-RestMethod -Uri "http://localhost:7001/api/admin/feedbacks?page=1&pageSize=5" -Headers $headers
$f.data | ConvertTo-Json -Depth 3 | Select-Object -First 30
```

**记录实际字段名**（spec § 2.6 约定 user=list、feedback=rows，如不符则 T7 调整）：

```
user list 返回结构 = data.____ (list / rows)
feedback list 返回结构 = data.____ (list / rows)
```

### Step 2: 创建 `database/009_add_user_admin_actions.sql`

写入 `d:\Donough\Projects\super-tools-spec-c2a\super-tool-node\database\009_add_user_admin_actions.sql`（完整 SQL 见 spec § 3.2，含幂等 INSERT + super_admin 关联 + 校验 SELECT + 末尾回滚注释）。

### Step 3: 执行迁移

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2a\super-tool-node'
mysql -u root -p superadmin_db < database/009_add_user_admin_actions.sql
```

预期：3 条 SELECT 输出（2 条 permission 行 + super_admin cnt=2）。

### Step 4: 更新 `database/README.md`

在迁移列表段追加 `009_add_user_admin_actions.sql` 行，并在迁移说明段末尾追加章节（见 spec § 3.4）。

### Step 5: type-check + commit

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2a\super-tool-node'
npm run tsc -- --noEmit
git add database/
git commit -m "feat(db): 009_add_user_admin_actions.sql 新增 user:device:list / user:address:list"
```

---

## Task 2: 后端 audit action 词表扩展 + service 增量

**Spec:** §2.2 + §2.3

**Files:**
- Modify: `super-tool-node/app/service/audit.ts`（如有 ACTIONS 常量）
- Modify: `super-tool-node/app/service/user.ts`（追加 2 个方法）

### Step 1: 检查 audit action 词表

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2a\super-tool-node'
Select-String -Path 'app/service/audit.ts' -Pattern 'reset-password|ACTIONS' -Encoding UTF8 | Select-Object -First 30
```

如有 `ACTIONS` 白名单且不含 `reset-password`，补入；如是字符串自由传入则跳过。

### Step 2: 在 `app/service/user.ts` 内追加 2 方法

在 `class UserService` 内（`generateReferralCode` 之前）追加 `adminResetPassword` + `adminChangeStatus`，完整实现见 spec § 2.2 / § 2.3。

要点：
- `adminResetPassword`：adminId === targetUserId 时抛 400；用 bcrypt.hash(newPassword, 12) 落库；清缓存
- `adminChangeStatus`：status 仅允 0/1；adminId === targetUserId 且 status===0 时抛 400；返回剔除 passwordHash 的对象

### Step 3: type-check + commit

```powershell
npm run tsc -- --noEmit
git add app/service/user.ts
# audit.ts 改动按需 add
git commit -m "feat(user-service): 新增 adminResetPassword / adminChangeStatus，自身保护硬校验"
```

---

## Task 3: 后端 controller 增量

**Spec:** §2.2 / §2.3 / §2.4 / §2.5

**Files:**
- Modify: `super-tool-node/app/controller/user.ts`（追加 4 个方法）

### Step 1: 在 `app/controller/user.ts` 内追加 4 方法

在 `_stripSensitive` 之前追加：`resetPassword` / `changeStatus` / `adminListDevices` / `adminListAddresses`。完整实现见 spec § 2.2 / § 2.3 / § 2.4 / § 2.5。

要点：
- `resetPassword`：validate `newPassword` min:8 max:50；adminId 来自 ctx.state.user.id；audit beforeData 用 _stripSensitive；不写 afterData（避免明文/hash 泄漏）
- `changeStatus`：validate `status: number`；audit module='user' action='update'，description 区分"禁用 #x" vs "启用 #x"
- `adminListDevices` / `adminListAddresses`：直接复用 `service.user.listDevices / listAddresses`，不写 audit（纯查询）

### Step 2: type-check + commit

```powershell
npm run tsc -- --noEmit
git add app/controller/user.ts
git commit -m "feat(user-ctrl): 新增 resetPassword / changeStatus / adminListDevices / adminListAddresses，audit 埋点齐全"
```

---

## Task 4: 后端 router.ts 增量

**Spec:** §2.1

**Files:**
- Modify: `super-tool-node/app/router.ts`

### Step 1: 在 router.ts 适当位置插入 4 条路由

在「用户管理（管理端）」段（line 45-51）之后、「角色管理」段（line 53）之前追加：

```typescript
  // ==================== 用户管理增强（Spec-C2a） ====================
  router.post('/api/admin/users/:id/reset-password',
    auth, perm('user:reset-password'),
    controller.user.resetPassword);
  router.put('/api/admin/users/:id/status',
    auth, perm('user:disable'),
    controller.user.changeStatus);
  router.get('/api/admin/users/:id/devices',
    auth, perm('user:device:list'),
    controller.user.adminListDevices);
  router.get('/api/admin/users/:id/addresses',
    auth, perm('user:address:list'),
    controller.user.adminListAddresses);
```

### Step 2: type-check + commit

```powershell
npm run tsc -- --noEmit
git add app/router.ts
git commit -m "feat(router): 挂载 Spec-C2a 用户管理增强 4 路由"
```

---

## Task 5: 后端 manual smoke test

**Spec:** §8.3 后端测试清单

**Files:** 无（仅运行）

### Step 1: 启动 worktree 后端（暂停主仓库 dev 避免端口冲突）

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2a\super-tool-node'
npm run dev
```

预期：启动无 error；router 加载日志可见 4 条新路由。

### Step 2: 跑 PowerShell smoke

```powershell
$body = '{"username":"admin","password":"Admin@123456","clientId":"admin_client","clientSecret":"ADMIN_SECRET"}'
$loginRes = Invoke-RestMethod -Uri "http://localhost:7001/api/auth/login" -Method POST -Body $body -ContentType "application/json"
$token = $loginRes.data.accessToken
$headers = @{ "Authorization" = "Bearer $token" }
$adminId = $loginRes.data.user.id

# (1) 重置非自身用户密码
Invoke-RestMethod -Uri "http://localhost:7001/api/admin/users/2/reset-password" -Method POST -Headers $headers -Body '{"newPassword":"test1234"}' -ContentType "application/json"
# 预期 code=200

# (2) 重置自身（应 400）
try { Invoke-RestMethod -Uri "http://localhost:7001/api/admin/users/$adminId/reset-password" -Method POST -Headers $headers -Body '{"newPassword":"test1234"}' -ContentType "application/json" } catch { $_.ErrorDetails.Message }
# 预期含 "请通过\"修改密码\"修改自己的密码"

# (3) 禁用 user 2
Invoke-RestMethod -Uri "http://localhost:7001/api/admin/users/2/status" -Method PUT -Headers $headers -Body '{"status":0}' -ContentType "application/json"
# 预期 data.status=0

# (4) 禁用自身（应 400）
try { Invoke-RestMethod -Uri "http://localhost:7001/api/admin/users/$adminId/status" -Method PUT -Headers $headers -Body '{"status":0}' -ContentType "application/json" } catch { $_.ErrorDetails.Message }
# 预期含 "不能禁用自己的账户"

# (5) 设备/地址查询
Invoke-RestMethod -Uri "http://localhost:7001/api/admin/users/2/devices" -Headers $headers
Invoke-RestMethod -Uri "http://localhost:7001/api/admin/users/2/addresses" -Headers $headers

# (6) 不带 token
try { Invoke-RestMethod -Uri "http://localhost:7001/api/admin/users/2/devices" } catch { $_.Exception.Response.StatusCode }
# 预期 401

# (7) 还原 user 2 状态
Invoke-RestMethod -Uri "http://localhost:7001/api/admin/users/2/status" -Method PUT -Headers $headers -Body '{"status":1}' -ContentType "application/json" | Out-Null
```

### Step 3: 检查 audit_logs 表

```sql
SELECT id, module, action, biz_type, biz_id, status, description, created_at
FROM audit_logs WHERE module='user' AND created_at >= NOW()-INTERVAL 10 MINUTE
ORDER BY id DESC LIMIT 20;
```

预期：见 reset-password 成功+失败 + update（禁用/启用）成功+失败 4-6 条记录。

### Step 4: 关闭 worktree dev

worktree 后端 ctrl-c。后续阶段可由主仓库 dev 接管，或维持 worktree dev 直到 T14 验收。

---

## Task 6: admin 路由模块填充

**Spec:** §4.1

**Files:**
- Modify: `super-tools-admin/config/routes/modules/user.ts`
- Modify: `super-tools-admin/config/routes/modules/feedback.ts`

### Step 1: 填充 `config/routes/modules/user.ts`

```typescript
import type { IRoute } from 'umi';

const userRoutes: IRoute[] = [
  {
    path: '/users',
    component: '@/pages/User/List',
    wrappers: ['@/components/AuthWrapper'],
  },
];

export default userRoutes;
```

### Step 2: 填充 `config/routes/modules/feedback.ts`

```typescript
import type { IRoute } from 'umi';

const feedbackRoutes: IRoute[] = [
  {
    path: '/feedback',
    component: '@/pages/Feedback/List',
    wrappers: ['@/components/AuthWrapper'],
  },
];

export default feedbackRoutes;
```

### Step 3: type-check + commit

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2a\super-tools-admin'
npx tsc --noEmit
git add config/routes/modules/
git commit -m "feat(admin-routes): 填充 /users + /feedback 路由占位"
```

---

## Task 7: admin services 新建

**Spec:** §7.1 + §7.2

**Files:**
- New: `super-tools-admin/src/services/user.ts`
- New: `super-tools-admin/src/services/feedback.ts`

### Step 1: 创建 `src/services/user.ts`

完整内容见 spec § 7.1（约 90 行）。9 个 API：listUsers / getUser / createUser / updateUser / deleteUser / resetUserPassword / changeUserStatus / listUserDevices / listUserAddresses。

⚠️ 类型 `User`、`UserDevice`、`UserAddress` 字段须与 T1 联调记录的真实响应对齐。

### Step 2: 创建 `src/services/feedback.ts`

完整内容见 spec § 7.2（约 90 行）。5 API：listFeedbacks / getFeedback / replyFeedback / updateFeedback / deleteFeedback。`FeedbackType` / `FeedbackStatus` 类型导出供 utils 复用。

### Step 3: type-check + commit

```powershell
npx tsc --noEmit
git add src/services/user.ts src/services/feedback.ts
git commit -m "feat(admin-svc): user/feedback service 封装（共 14 API + 类型）"
```

---

## Task 8: admin utils 新建

**Spec:** §7.3 + §7.4

**Files:**
- New: `super-tools-admin/src/utils/feedbackStatus.ts`
- New: `super-tools-admin/src/utils/userType.ts`

### Step 1: 创建 `src/utils/feedbackStatus.ts`

完整内容见 spec § 7.3。导出 `STATUS_TRANSITIONS` / `STATUS_LABELS` / `STATUS_COLORS` 常量 + `getAllowedTransitions(from)` / `isTransitionAllowed(from, to)` 函数。

### Step 2: 创建 `src/utils/userType.ts`

完整内容见 spec § 7.4。导出 `USER_TYPE_LABELS` / `USER_STATUS_LABELS` / `GENDER_LABELS` 常量 + `isSelf(targetUserId, currentUserId)` 函数。

### Step 3: 检查项目是否已有 formatDateTime

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2a\super-tools-admin'
Select-String -Path 'src/utils/*.ts' -Pattern 'formatDateTime|formatDate' -Encoding UTF8
```

- 已有：直接 import 复用
- 没有：在 `src/utils/format.ts` 新增（参考 spec § 7.5）

### Step 4: type-check + commit

```powershell
npx tsc --noEmit
git add src/utils/feedbackStatus.ts src/utils/userType.ts
# 如改了 format.ts 也 add
git commit -m "feat(admin-utils): feedbackStatus 状态机白名单 + userType 标签映射"
```

---

## Task 9: admin pages/User 列表 + Modal + ResetPwdModal + Drawer 占位

**Spec:** §5.2 / §5.3 / §5.4

**Files:**
- New: `super-tools-admin/src/pages/User/List/index.tsx`（约 250 行）
- New: `super-tools-admin/src/pages/User/List/UserModal.tsx`（约 90 行）
- New: `super-tools-admin/src/pages/User/List/ResetPwdModal.tsx`（约 60 行）
- New: `super-tools-admin/src/pages/User/List/DetailDrawer.tsx`（占位，T10 完整化）
- New: `super-tools-admin/src/pages/User/List/index.less`（约 10 行）

### Step 1: `index.tsx` 列表页

参考 `src/pages/System/Roles/index.tsx` 风格。要点：

- 5 字段筛选栏（keyword / status / userType / RangePicker / 重置）
  - RangePicker 转 startDate/endDate ISO 字符串
- Table 列：ID / 头像（Avatar） / 用户名 / 昵称 / 邮箱 / 手机 / 类型(Tag) / 状态(Switch 行内) / 注册时间 / 操作
- 状态 Switch：禁用 isSelf(row.id, currentUserId) 行
- 操作列 4 项（详情/编辑/重置密码/删除），均用 `<AuthButton permCode="...">` 包裹
- 自身保护：从 `localStorage.getItem('user')` JSON.parse 取 `currentUserId`
  - 重置密码自己 → Tooltip "请通过修改密码改自己" + a className="action-disabled"
  - 删除自己 → Tooltip "不能删除自己" + 同样禁用样式
- handleStatus / handleDelete 在调用前再次 isSelf 校验
- pagination + scroll x:1380

### Step 2: `UserModal.tsx`（创建/编辑共用）

- 创建模式：username + password + email + phone + nickname + userType
- 编辑模式：username 字段 disabled；隐藏 password 字段（密码改通过 ResetPwdModal）
- onOk：validateFields → 创建 createUser / 编辑 updateUser（剔除 username/password）
- 校验：username 3-50；password 8-50；email 格式；userType initialValue=1

### Step 3: `ResetPwdModal.tsx`

- 标题：`重置 ${target?.username} 的密码`
- Alert 警告 type="warning" "重置后用户需用新密码登录"
- newPassword 8-50 + confirmPassword（自定义 validator 校验两次一致）
- onOk：调 `resetUserPassword(target.id, newPassword)`
- afterClose：form.resetFields()

### Step 4: `DetailDrawer.tsx` 占位

```tsx
import React from 'react';
import { Drawer } from 'antd';
import { User } from '@/services/user';

interface Props { visible: boolean; target: User | null; onClose: () => void; }
const DetailDrawer: React.FC<Props> = ({ visible, onClose }) => (
  <Drawer open={visible} onClose={onClose} title="用户详情（构建中）" />
);
export default DetailDrawer;
```

T10 替换为完整版。

### Step 5: `index.less`

```less
.page-users {
  .action-disabled {
    color: rgba(0,0,0,.25);
    cursor: not-allowed;
  }
}
```

### Step 6: type-check + commit

```powershell
npx tsc --noEmit
git add src/pages/User/
git commit -m "feat(admin/users): 列表页 + UserModal + ResetPwdModal + DetailDrawer 占位"
```

---

## Task 10: admin pages/User 详情抽屉 3 Tab 完整化

**Spec:** §5.5

**Files:**
- Modify: `super-tools-admin/src/pages/User/List/DetailDrawer.tsx`（替换占位）
- New: `super-tools-admin/src/pages/User/List/BasicInfoTab.tsx`（约 70 行）
- New: `super-tools-admin/src/pages/User/List/DevicesTab.tsx`（约 60 行）
- New: `super-tools-admin/src/pages/User/List/AddressesTab.tsx`（约 45 行）

### Step 1: `DetailDrawer.tsx`（替换占位，约 80 行）

要点：
- props: `{ visible, target, onClose }`
- 内部 state: `tab` ('basic'/'devices'/'addresses') / `detail` / `loading`
- useEffect: visible 打开且 target 变化时调 `getUser(target.id)` 拉详情
- Drawer width=720 + destroyOnClose
- Tabs items 3 项

### Step 2: `BasicInfoTab.tsx`

要点：
- props: `{ user: User | null }`
- 用 `<Descriptions column={2} bordered size="small">`
- 字段：ID / UUID（slice 8 + ...）/ 头像（Avatar size 48）/ 用户名 / 昵称 / 邮箱 / 手机 / 类型(Tag) / 状态(Tag color 红绿) / 性别 / 生日 / 注册来源 / 注册 IP / 角色（Tag 数组） / 创建时间 / 更新时间
- user 为空时返回 `<Empty />`

### Step 3: `DevicesTab.tsx`

要点：
- props: `{ userId?: number }`
- 内部 state: `list` / `loading`
- useEffect 监听 userId 变化 → 调 `listUserDevices(userId)`
- Table size="small" pagination={false}
- 列：设备名 / 类型 / OS / App / 推送(Tag) / 最后活跃 / 状态(Tag)

### Step 4: `AddressesTab.tsx`

要点：
- props: `{ userId?: number }`
- 同 DevicesTab 模式
- 列：收件人 / 电话 / 省 / 市 / 区 / 详细(ellipsis) / 默认(Tag)

### Step 5: type-check + commit

```powershell
npx tsc --noEmit
git add src/pages/User/List/DetailDrawer.tsx src/pages/User/List/BasicInfoTab.tsx src/pages/User/List/DevicesTab.tsx src/pages/User/List/AddressesTab.tsx
git commit -m "feat(admin/users): 详情抽屉容器 + BasicInfo/Devices/Addresses 3 Tab"
```

---

## Task 11: admin pages/Feedback 列表 + Drawer

**Spec:** §6

**Files:**
- New: `super-tools-admin/src/pages/Feedback/List/index.tsx`（约 220 行）
- New: `super-tools-admin/src/pages/Feedback/List/DetailDrawer.tsx`（约 200 行）
- New: `super-tools-admin/src/pages/Feedback/List/StatusTag.tsx`（约 20 行）
- New: `super-tools-admin/src/pages/Feedback/List/index.less`（约 15 行）

### Step 1: `StatusTag.tsx`（共用组件）

```tsx
import React from 'react';
import { Tag } from 'antd';
import { STATUS_LABELS, STATUS_COLORS } from '@/utils/feedbackStatus';

const StatusTag: React.FC<{ status: number }> = ({ status }) => (
  <Tag color={STATUS_COLORS[status]}>{STATUS_LABELS[status]}</Tag>
);
export default StatusTag;
```

### Step 2: `index.tsx` 列表页

要点：
- 6 字段筛选栏：keyword / type / status / platform / userId / RangePicker
  - userId 用 InputNumber min:1
  - RangePicker showTime → startTime/endTime ISO
- TYPE_OPTIONS 常量：bug/suggestion/praise/other
- TYPE_COLOR：bug=red / suggestion=blue / praise=gold / other=default
- Table 列：ID / 类型(Tag) / 内容(ellipsis) / 用户 / 联系方式 / 平台 / 状态(行内 Select) / 提交时间 / 操作
  - 用户列：u 存在显示 username[(nickname)]，否则若 userId 显 #N，否则"匿名"
  - 状态列内联 Select：仅展示 transition 白名单 + 当前状态 disabled 项；onChange 调 updateFeedback
- 操作列：详情 / 回复 / 删除
  - 回复：status >= 2 时 disabled + Tooltip "已回复/已关闭，需先重新打开"
  - 详情/回复都打开同一 DetailDrawer（设 setTarget + setDrawerVisible(true)）
- pagination + scroll x:1500
- ⚠️ feedback list 返回 `data.rows`（非 `data.list`），fetch 内提取 `res.data.rows`

### Step 3: `DetailDrawer.tsx`（约 200 行）

要点：
- props: `{ visible, target, onClose, onSuccess }`
- 内部 state: `detail`（getFeedback 拉的完整对象）/ `loading` / `replyForm`
- useEffect: visible 打开且 target 变化时调 `getFeedback(target.id)`
- 4 段 Collapse defaultActiveKey={['basic','content','reply','actions']}：
  - **basic**：Descriptions column={2} 展示 类型(Tag)/状态(StatusTag)/用户/联系方式/平台/提交时间/IP/UA(ellipsis)
  - **content**：`<pre style={{whiteSpace:'pre-wrap',wordBreak:'break-word'}}>` 展示 detail.content
  - **reply**：
    - 已回复（detail.replyContent）：只读展示 + 回复人(replier.username) + 回复时间(repliedAt)
    - 未回复（status===0/1）：Form + TextArea rows=4 maxLength=2000 showCount + 提交按钮 → replyFeedback
    - status>=2 但 replyContent 空（边界）：禁用回复
  - **actions**：StatusChanger（Select 仅展示白名单）+ 删除按钮（Popconfirm）
- 提交回复后：refetch detail + onSuccess（通知列表刷新）
- StatusChanger 与列表内联 Select 同逻辑：transitions[detail.status] + 当前 disabled

### Step 4: `index.less`

```less
.page-feedback {
  .action-disabled {
    color: rgba(0,0,0,.25);
    cursor: not-allowed;
  }
  pre {
    background: #fafafa;
    padding: 12px;
    border-radius: 4px;
    margin: 0;
    max-height: 320px;
    overflow: auto;
  }
}
```

### Step 5: type-check + commit

```powershell
npx tsc --noEmit
git add src/pages/Feedback/
git commit -m "feat(admin/feedback): 列表 + DetailDrawer + StatusTag + 状态机白名单 UI"
```

---

## Task 12: admin 4 个测试文件

**Spec:** §8.2

**Files:**
- New: `super-tools-admin/src/services/__tests__/user.test.tsx`
- New: `super-tools-admin/src/services/__tests__/feedback.test.tsx`
- New: `super-tools-admin/src/utils/__tests__/feedbackStatus.test.tsx`
- New: `super-tools-admin/src/utils/__tests__/userType.test.tsx`

### Step 1: `user.test.tsx`（≥ 8 case）

完整内容见 spec § 8.2.1 模板。8 个 case 覆盖 9 API 中的 8 个（`listUserAddresses` 与 `listUserDevices` 同构，省略 1 个或加上凑 9 个）。

文件头注释固定（与 C1 一致）：

```typescript
/**
 * user service 测试
 *
 * 用 .tsx 扩展名走 babel-jest（jest config 已配置），
 * 避开 ts-jest 26 + TypeScript 5.4 的 hoist-jest `getMutableClone` bug。
 */
```

### Step 2: `feedback.test.tsx`（5 case）

5 个 case 对齐 5 API：listFeedbacks / getFeedback / replyFeedback / updateFeedback / deleteFeedback。

```typescript
import { listFeedbacks, getFeedback, replyFeedback, updateFeedback, deleteFeedback } from '@/services/feedback';
jest.mock('@/utils/request');
import request from '@/utils/request';

describe('feedback service', () => {
  beforeEach(() => jest.clearAllMocks());

  it('listFeedbacks → GET /api/admin/feedbacks + params', async () => {
    await listFeedbacks({ page: 1, status: 0 });
    expect(request).toHaveBeenCalledWith('/api/admin/feedbacks', { params: { page: 1, status: 0 } });
  });
  it('getFeedback → GET /api/admin/feedbacks/:id', async () => {
    await getFeedback(7);
    expect(request).toHaveBeenCalledWith('/api/admin/feedbacks/7');
  });
  it('replyFeedback → POST /api/admin/feedbacks/:id/reply + body', async () => {
    await replyFeedback(7, 'thanks');
    expect(request).toHaveBeenCalledWith('/api/admin/feedbacks/7/reply', { method: 'POST', data: { replyContent: 'thanks' } });
  });
  it('updateFeedback → PUT /api/admin/feedbacks/:id + body', async () => {
    await updateFeedback(7, { status: 1 });
    expect(request).toHaveBeenCalledWith('/api/admin/feedbacks/7', { method: 'PUT', data: { status: 1 } });
  });
  it('deleteFeedback → DELETE /api/admin/feedbacks/:id', async () => {
    await deleteFeedback(7);
    expect(request).toHaveBeenCalledWith('/api/admin/feedbacks/7', { method: 'DELETE' });
  });
});
```

### Step 3: `feedbackStatus.test.tsx`（4 case）

```typescript
import { getAllowedTransitions, isTransitionAllowed, STATUS_LABELS } from '@/utils/feedbackStatus';

describe('feedbackStatus', () => {
  it('getAllowedTransitions(0) returns [1,3]', () => {
    expect(getAllowedTransitions(0)).toEqual([1, 3]);
  });
  it('getAllowedTransitions on unknown returns []', () => {
    expect(getAllowedTransitions(99)).toEqual([]);
  });
  it('isTransitionAllowed(0,1) true; (0,2) false (reply 独占)', () => {
    expect(isTransitionAllowed(0, 1)).toBe(true);
    expect(isTransitionAllowed(0, 2)).toBe(false);
  });
  it('idempotent: from === to always allowed', () => {
    expect(isTransitionAllowed(2, 2)).toBe(true);
    expect(STATUS_LABELS[2]).toBe('已回复');
  });
});
```

### Step 4: `userType.test.tsx`（2 case）

```typescript
import { isSelf, USER_TYPE_LABELS, USER_STATUS_LABELS } from '@/utils/userType';

describe('userType utils', () => {
  it('isSelf returns true when target === current', () => {
    expect(isSelf(5, 5)).toBe(true);
    expect(isSelf(5, 6)).toBe(false);
    expect(isSelf(5, undefined)).toBe(false);
  });
  it('label maps cover full domain', () => {
    expect(USER_TYPE_LABELS[1]).toBe('普通用户');
    expect(USER_TYPE_LABELS[2]).toBe('管理员');
    expect(USER_STATUS_LABELS[0]).toBe('禁用');
    expect(USER_STATUS_LABELS[1]).toBe('正常');
  });
});
```

### Step 5: 跑测试

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2a\super-tools-admin'
npx jest src/services/__tests__/user.test.tsx src/services/__tests__/feedback.test.tsx src/utils/__tests__/feedbackStatus.test.tsx src/utils/__tests__/userType.test.tsx --no-coverage
```

预期：4 suites × 总 ≥ 19 case PASS。

### Step 6: commit

```powershell
git add src/services/__tests__/user.test.tsx src/services/__tests__/feedback.test.tsx src/utils/__tests__/feedbackStatus.test.tsx src/utils/__tests__/userType.test.tsx
git commit -m "test(admin/c2a): user/feedback service + feedbackStatus/userType utils 4 suites ≥ 19 case PASS"
```

---

## Task 13: type-check + lint + jest 全跑

**Spec:** §9.2 验收清单

**Files:** 无（仅运行）

### Step 1: 后端 tsc

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2a\super-tool-node'
npm run tsc -- --noEmit
```

预期：0 错。

### Step 2: 前端 tsc

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2a\super-tools-admin'
npx tsc --noEmit
```

预期：0 错。如出现 useSelector / useDispatch / history not found，重建 src/.umi junction（参考 T0）。

### Step 3: 前端 lint（如配置了）

```powershell
npx eslint 'src/**/*.{ts,tsx}' --max-warnings=0
# 或
npm run lint
```

如本项目无 lint 命令，跳过。

### Step 4: 前端 jest 全量

```powershell
npx jest --no-coverage
```

预期：与 C1 既有测试合并后全 PASS。

### Step 5: 如有任何失败，修到全绿

修复后回到 Step 1 重跑。

---

## Task 14: 前端 manual smoke test

**Spec:** §8.3 验收清单

**Files:** 无（仅运行）

### Step 1: 启动 worktree dev（admin + node）

```powershell
# 终端 1：worktree node 后端
Set-Location 'd:\Donough\Projects\super-tools-spec-c2a\super-tool-node'
npm run dev

# 终端 2：worktree admin
Set-Location 'd:\Donough\Projects\super-tools-spec-c2a\super-tools-admin'
npm run start:dev   # 或 npm start，按项目 package.json
```

⚠️ 主仓库 dev 需关闭避免端口冲突。

### Step 2: 浏览器访问 admin（默认 http://localhost:8000 或项目实际端口）

#### 用户管理

- [ ] 登录 super_admin → 左侧菜单见"用户管理"
- [ ] `/users` 列表加载，5 个筛选项可用
- [ ] 输入 keyword 查询 → URL params 正确，列表刷新
- [ ] 创建时间 RangePicker 改 → 列表变化
- [ ] 行内 Switch 改非自身用户状态 → 成功 + 刷新
- [ ] 行内 Switch 自身行 → disabled + Tooltip
- [ ] 详情按钮 → 抽屉打开，3 Tab 可切换
- [ ] BasicInfo Tab：所有字段显示正常
- [ ] Devices Tab：表格加载（数据可空）
- [ ] Addresses Tab：表格加载（数据可空）
- [ ] 编辑按钮 → Modal，username disabled
- [ ] 重置密码按钮（非自身）→ Modal，新密码+确认 → 提交成功
- [ ] 重置密码按钮（自身）→ 灰显 + Tooltip
- [ ] 删除按钮（非自身）→ Popconfirm → 成功
- [ ] 删除按钮（自身）→ 灰显
- [ ] 新建用户 → Modal → 成功

#### 反馈管理

- [ ] 左侧菜单见"反馈管理"
- [ ] `/feedback` 列表加载，6 个筛选项可用
- [ ] 行内状态 Select：仅显示 transition 白名单
- [ ] 列表 status=0 行 → 改 1 → 成功
- [ ] 列表 status=2 行 → 选项中无 0/2/3，仅 1（重新打开）
- [ ] 详情抽屉：4 段折叠可见
- [ ] 内容区 `<pre>` 保留换行
- [ ] 已回复反馈：回复区只读显示
- [ ] 未回复反馈：回复区 TextArea + 提交 → status 变 2 + 列表刷新
- [ ] 操作区状态 Select 同列表行内行为
- [ ] 操作区删除按钮 → Popconfirm → 抽屉关闭 + 列表刷新

### Step 3: 把任何遗漏问题记录为 follow-up backlog

如发现非阻断性体验问题（如 Tag 配色调整 / RangePicker 默认值），列入 spec § 9.2 加分项；阻断性问题立即修。

---

## Task 15: spec + plan + 实施 commits 整合 push & 合并 master

**Spec:** §9 收官

**Files:** 无（仅 git）

### Step 1: 在 worktree 内 commit spec + plan

```powershell
Set-Location 'd:\Donough\Projects\super-tools-spec-c2a'
git status --short
git add docs/superpowers/specs/2026-05-12-Spec-C2a管理端用户与反馈管理设计文档.md
git add docs/superpowers/plans/2026-05-12-Spec-C2a管理端用户与反馈管理实施计划.md
git commit -m "docs(spec-c2a): 设计文档 + 实施计划"
```

### Step 2: 检查 commit 链

```powershell
git log --oneline master..spec-c2a
```

预期：约 13-15 commits（docs + db + node 4 commits + admin routes + svc + utils + pages 3 commits + tests）。

### Step 3: 切回主仓库 + 拉取 worktree commits + 合并

```powershell
Set-Location 'd:\Donough\Projects\super-tools'
git checkout master
git merge spec-c2a --no-ff -m "Merge branch 'spec-c2a' into master

Spec-C2a 管理端用户与反馈管理（前后端联合）
- 后端 4 接口：reset-password / status / admin-list-devices / admin-list-addresses
- 数据库迁移 009_add_user_admin_actions.sql
- admin 用户管理页（列表 + Modal + ResetPwdModal + 详情抽屉 3 Tab）
- admin 反馈管理页（列表 + 详情抽屉 4 段折叠 + 状态机白名单）
- 4 个测试 suites ≥ 19 case PASS
"
```

### Step 4: 主仓库验证

```powershell
git log --oneline -10
git status --short
# tsc 跑一遍兜底
Set-Location 'super-tool-node'
npm run tsc -- --noEmit
Set-Location '../super-tools-admin'
npx tsc --noEmit
```

预期：全绿。

### Step 5: 清理 worktree

```powershell
Set-Location 'd:\Donough\Projects\super-tools'
git worktree remove ../super-tools-spec-c2a --force
git worktree list
git branch -d spec-c2a
git branch -a
```

预期：worktree list 仅剩主仓库；spec-c2a 分支已删。

⚠️ Windows 文件锁（与 A1/A2/C1 同款问题）可能导致 worktree 物理目录残留。如 `git worktree remove --force` 失败：

```powershell
# 手动清理：先 prune git 元数据，物理目录留待 IDE 重启后删
git worktree prune
# 关闭所有引用 worktree 路径的进程（VSCode/IDE/jest），重启后手动 rm
```

物理目录残留不影响功能，可忽略。

### Step 6: push 到远端

```powershell
git push origin master
# 如有远端 spec-c2a 分支：git push origin --delete spec-c2a
```

### Step 7: 最终核对

- [ ] master HEAD 含 Spec-C2a 全部 commits
- [ ] 主仓库 super-tools-admin 含 User/Feedback 页面 + service + utils + tests
- [ ] 主仓库 super-tool-node 含 4 接口 + 009 SQL
- [ ] 数据库 super_admin 已具备 user:device:list / user:address:list（如 dev DB 已迁过则 skip）
- [ ] 远端 master 已推送

---

## 收官检查清单

### 必达项

- [ ] T1：009_add_user_admin_actions.sql 迁移成功执行
- [ ] T2-T4：后端 service / controller / router 4 接口落地
- [ ] T5：后端 smoke test 8 case 全绿
- [ ] T6：admin 路由模块填充
- [ ] T7：services 14 API 封装
- [ ] T8：utils 状态机白名单 + 标签映射
- [ ] T9-T10：用户管理页 7 文件完整
- [ ] T11：反馈管理页 4 文件完整
- [ ] T12：4 测试 suites ≥ 19 case PASS
- [ ] T13：tsc 0 错（前后端）
- [ ] T14：14 项前端验收全过
- [ ] T15：spec-c2a 已合并 master

### 加分项（不阻断合并）

- [ ] feedback 详情抽屉显示 IP / UA 折叠
- [ ] 重置密码强复杂度建议（非强制）
- [ ] 列表搜索栏支持 reset 重置

### 已知非目标（明确不做）

- 用户角色绑定 UI（user:assign-roles）
- 用户级直接授权 UI（user:permission:grant/revoke）
- 富文本回复
- 反馈批量操作
- 反馈截图字段（model 中无 images）

---

## 风险与回滚

### 关键风险

| 风险 | 触发条件 | 缓解 |
|---|---|---|
| user list 实际返回 `data.rows` 而非 `data.list` | T1 联调验证发现差异 | T7 services 调整 + T9 列表 fetch 调整 |
| ts-jest 26 + TS 5.4 兼容问题 | 测试文件用 .ts 扩展名 | 严格用 .tsx + babel-jest（C1 已验证） |
| audit action 'reset-password' 不在白名单 | T2 检查后无 ACTIONS 常量约束 | 跳过；如有约束补入 |
| feedback list `data.rows` vs user `data.list` 混淆 | T9 / T11 fetch 取错字段 | code review 时严查；测试覆盖 |
| Windows 文件锁阻止 worktree 删除 | T15 git worktree remove 失败 | 等同 C1 处理：git prune + 物理目录手动清 |

### 回滚策略

- **后端**：`git revert <merge-commit>` + 跑 SQL 回滚段（spec § 3.3）
- **前端**：`git revert <merge-commit>`，无前端 DB 状态需回滚
- **半路撤回**：worktree 内 `git reset --hard master` 重来；DB 已迁的 009 不回滚（幂等无副作用）

---

**结尾**：plan 待 review 通过后即可进入 T1 执行。建议执行模式 = 主 agent 直跑（spec § Q11=2，C1 已验证）。
