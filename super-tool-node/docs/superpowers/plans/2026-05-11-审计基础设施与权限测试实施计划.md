# 审计基础设施与权限测试实施计划（Spec-A1）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 super-tool-node 项目中实现审计基础设施（model + service + 4 接口）+ 权限测试工具（3 mode），并回填 17 个高敏感写操作的审计调用。

**Architecture:** Egg.js + Sequelize；audit 通过 controller 层显式调用 `service.audit.log({...})` 实现（与现有代码风格一致，不引入装饰器）；permission-test 通过单接口 `mode` 参数路由到 3 个 service 方法。审计写入异常吞掉，永不影响主业务。

**Tech Stack:** Egg.js / Sequelize / TypeScript / mocha + egg-bin test + egg-mock

**Spec:** `docs/superpowers/specs/2026-05-11-审计基础设施与权限测试设计文档.md`

---

## 工作环境（Working Environment）

**主仓库**：`d:/Donough/Projects/super-tools/`（已合并 Spec-B 到 master）

**建议**：本 Spec-A1 的实施在新 git worktree 内进行，避免直接污染 master：

```bash
cd d:/Donough/Projects/super-tools
git worktree add .worktrees/spec-a1-audit -b feat/spec-a1-audit

# 共享主仓库 node_modules（避免重复安装）
cd .worktrees/spec-a1-audit/super-tool-node
cmd /c "mklink /J node_modules ..\..\..\super-tool-node\node_modules"
```

> 18 task 全部在 `feat/spec-a1-audit` 分支提交；完成后用 `finishing-a-development-branch` skill 合并回 master。

---

## Task 1: 新增 AuditLog Model + requestStartTime middleware

**Spec:** §4.1, §4.6

**Files:**
- Create: `super-tool-node/app/model/audit_log.ts`
- Create: `super-tool-node/app/middleware/requestStartTime.ts`
- Modify: `super-tool-node/config/config.default.ts`

- [ ] **Step 1: 创建 `app/model/audit_log.ts`**

```typescript
import { Application } from 'egg';
import { DataTypes } from 'sequelize';

export interface AuditLogAttributes {
  id: number;
  traceId: string | null;
  userId: number | null;
  username: string | null;
  platform: string | null;
  module: string;
  action: string;
  description: string | null;
  bizType: string | null;
  bizId: string | null;
  beforeData: any | null;
  afterData: any | null;
  ip: string | null;
  userAgent: string | null;
  requestUrl: string | null;
  requestMethod: string | null;
  requestParams: any | null;
  responseCode: number | null;
  costTime: number | null;
  status: number;
  failReason: string | null;
  createdAt?: Date;
}

export default (app: Application) => {
  const { STRING, BIGINT, INTEGER, TINYINT, JSON: JSON_TYPE } = DataTypes;
  const AuditLog = app.model.define('AuditLog', {
    id:             { type: BIGINT.UNSIGNED, autoIncrement: true, primaryKey: true },
    traceId:        { type: STRING(64),  allowNull: true,  field: 'trace_id' },
    userId:         { type: BIGINT.UNSIGNED, allowNull: true, field: 'user_id' },
    username:       { type: STRING(50),  allowNull: true },
    platform:       { type: STRING(30),  allowNull: true },
    module:         { type: STRING(50),  allowNull: false },
    action:         { type: STRING(50),  allowNull: false },
    description:    { type: STRING(500), allowNull: true },
    bizType:        { type: STRING(50),  allowNull: true,  field: 'biz_type' },
    bizId:          { type: STRING(64),  allowNull: true,  field: 'biz_id' },
    beforeData:     { type: JSON_TYPE,   allowNull: true,  field: 'before_data' },
    afterData:      { type: JSON_TYPE,   allowNull: true,  field: 'after_data' },
    ip:             { type: STRING(50),  allowNull: true },
    userAgent:      { type: STRING(500), allowNull: true,  field: 'user_agent' },
    requestUrl:     { type: STRING(500), allowNull: true,  field: 'request_url' },
    requestMethod:  { type: STRING(10),  allowNull: true,  field: 'request_method' },
    requestParams:  { type: JSON_TYPE,   allowNull: true,  field: 'request_params' },
    responseCode:   { type: INTEGER,     allowNull: true,  field: 'response_code' },
    costTime:       { type: INTEGER.UNSIGNED, allowNull: true, field: 'cost_time' },
    status:         { type: TINYINT.UNSIGNED, allowNull: false, defaultValue: 1 },
    failReason:     { type: STRING(500), allowNull: true,  field: 'fail_reason' },
  }, {
    tableName: 'audit_logs',
    timestamps: true,
    updatedAt: false,
    underscored: true,
  });
  return AuditLog;
};
```

- [ ] **Step 2: 创建 `app/middleware/requestStartTime.ts`**

```typescript
import { Context } from 'egg';

export default () => {
  return async (ctx: Context, next: () => Promise<any>) => {
    (ctx.state as any).requestStartTime = Date.now();
    await next();
  };
};
```

- [ ] **Step 3: 在 `config/config.default.ts` 全局 middleware 链最前面挂载**

读取现有 middleware 配置（搜索 `config.middleware`），在数组最前面插入 `'requestStartTime'`：

```typescript
// 修改前（示例 — 实际可能略有不同）
config.middleware = ['errorHandler', /* 其他 */];

// 修改后
config.middleware = ['requestStartTime', 'errorHandler', /* 其他 */];
```

> ⚠️ **如果原配置不是数组形式或在另一 config 文件**（如 plugin.ts / config.local.ts），请按现有结构追加。`requestStartTime` 必须放在所有可能写 audit 的 middleware **之前**。

- [ ] **Step 4: 类型检查**

```bash
cd super-tool-node && ./node_modules/.bin/tsc --noEmit
```

预期 exit=0，无类型错误。

- [ ] **Step 5: 启动服务冒烟**

```bash
cd super-tool-node && npm run dev
```

预期：服务正常启动；日志中无 model 加载错误；`AuditLog` 出现在 sequelize 注册的 model 列表中（可在 ctx.model.AuditLog 调用即正常）。

- [ ] **Step 6: Commit**

```bash
git add super-tool-node/app/model/audit_log.ts \
        super-tool-node/app/middleware/requestStartTime.ts \
        super-tool-node/config/config.default.ts
git commit -m "feat(audit): 新增 AuditLog model + requestStartTime middleware"
```

---

## Task 2: service/audit.ts 核心方法 — log / list / detail

**Spec:** §4.2, §4.3, §4.5, §4.7

**Files:**
- Create: `super-tool-node/app/service/audit.ts`

- [ ] **Step 1: 创建 `app/service/audit.ts` 完整内容**

```typescript
import { Service } from 'egg';
import { Op } from 'sequelize';

export interface AuditLogPayload {
  module: string;
  action: string;
  description?: string;
  bizType?: string;
  bizId?: string | number;
  beforeData?: any;
  afterData?: any;
  status?: 0 | 1;
  failReason?: string;
  /** 极少数情况下覆盖 ctx 注入字段 */
  override?: Record<string, any>;
}

export interface AuditLogQuery {
  page?: number;
  pageSize?: number;
  startTime?: string;
  endTime?: string;
  userId?: number;
  module?: string;
  action?: string;
  status?: 0 | 1;
  keyword?: string;
}

const SENSITIVE_FIELDS = [
  'password', 'newPassword', 'oldPassword', 'confirmPassword',
  'token', 'refreshToken', 'accessToken',
  'clientSecret', 'secret', 'apiKey',
];

export default class AuditService extends Service {
  /**
   * 写入一条审计日志
   * - 自动从 ctx 注入 user/ip/url/cost/trace 等
   * - 异常吞掉 + logger.warn（永不影响主业务）
   */
  async log(payload: AuditLogPayload): Promise<void> {
    try {
      const { ctx } = this;
      const user = (ctx.state as any).user;
      const startTime = (ctx.state as any).requestStartTime as number | undefined;

      await ctx.model.AuditLog.create({
        traceId: (ctx as any).tracer?.traceId || ctx.get('x-trace-id') || null,
        userId: user?.id ?? null,
        username: user?.username ?? null,
        platform: ctx.get('x-platform') || 'admin',
        module: payload.module,
        action: payload.action,
        description: payload.description ?? null,
        bizType: payload.bizType ?? null,
        bizId: payload.bizId != null ? String(payload.bizId) : null,
        beforeData: payload.beforeData ?? null,
        afterData: payload.afterData ?? null,
        ip: ctx.ip,
        userAgent: ctx.get('user-agent') || null,
        requestUrl: ctx.url,
        requestMethod: ctx.method,
        requestParams: this._sanitizeParams(ctx.request.body),
        responseCode: ctx.status || null,
        costTime: startTime ? Date.now() - startTime : null,
        status: payload.status ?? 1,
        failReason: payload.failReason ?? null,
        ...(payload.override || {}),
      } as any);
    } catch (err) {
      this.ctx.logger.warn('[audit] log failed: %s', (err as Error).message);
    }
  }

  /** 列表查询（7 维过滤 + 分页；列表不含 before/after 减少响应大小） */
  async list(q: AuditLogQuery) {
    const where = this._buildWhere(q);
    const page = Math.max(1, q.page || 1);
    const pageSize = Math.min(100, Math.max(1, q.pageSize || 20));

    const { count, rows } = await this.ctx.model.AuditLog.findAndCountAll({
      where,
      order: [['id', 'DESC']],
      offset: (page - 1) * pageSize,
      limit: pageSize,
      attributes: { exclude: ['beforeData', 'afterData', 'requestParams'] },
    });

    return { total: count, page, pageSize, rows };
  }

  /** 单条详情（含完整 JSON） */
  async detail(id: number) {
    return this.ctx.model.AuditLog.findByPk(id);
  }

  /** 流式 CSV 导出（在 Task 3 实现） */
  async exportCsv(_q: AuditLogQuery, _max = 10000): Promise<void> {
    throw new Error('not implemented yet — see Task 3');
  }

  /** 构造列表/导出共用的 where */
  protected _buildWhere(q: AuditLogQuery): any {
    const where: any = {};
    if (q.startTime || q.endTime) {
      where.createdAt = {};
      if (q.startTime) where.createdAt[Op.gte] = new Date(q.startTime);
      if (q.endTime) where.createdAt[Op.lte] = new Date(q.endTime);
    }
    if (q.userId) where.userId = q.userId;
    if (q.module) where.module = q.module;
    if (q.action) where.action = q.action;
    if (q.status !== undefined) where.status = q.status;
    if (q.keyword) where.description = { [Op.like]: `%${q.keyword}%` };
    return where;
  }

  /** 请求参数脱敏（深度遍历） */
  protected _sanitizeParams(body: any): any {
    if (!body || typeof body !== 'object') return body;
    let cloned: any;
    try { cloned = JSON.parse(JSON.stringify(body)); }
    catch { return null; }
    const walk = (obj: any) => {
      if (!obj || typeof obj !== 'object') return;
      for (const k of Object.keys(obj)) {
        if (SENSITIVE_FIELDS.includes(k)) {
          obj[k] = '***';
        } else if (typeof obj[k] === 'object') {
          walk(obj[k]);
        }
      }
    };
    walk(cloned);
    return cloned;
  }
}
```

- [ ] **Step 2: 类型检查**

```bash
cd super-tool-node && ./node_modules/.bin/tsc --noEmit
```

预期 exit=0。

- [ ] **Step 3: 启动 dev 冒烟（确认 service 注入成功）**

```bash
cd super-tool-node && npm run dev
```

预期：日志显示 `[egg-loader] load file ... app/service/audit.ts`；不报错即可。

- [ ] **Step 4: Commit**

```bash
git add super-tool-node/app/service/audit.ts
git commit -m "feat(service): 新增 audit service - log/list/detail + 参数脱敏"
```

---

## Task 3: service/audit.exportCsv 流式导出

**Spec:** §4.4

**Files:**
- Modify: `super-tool-node/app/service/audit.ts`

- [ ] **Step 1: 替换 exportCsv 方法 + 新增 _rowToCsv / _csvField 私有方法**

打开 `app/service/audit.ts`，把 `async exportCsv(_q, _max = 10000)` 整段替换为：

```typescript
  /**
   * 流式 CSV 导出
   * - PassThrough Stream 写入 ctx.body
   * - UTF-8 BOM + 中文表头
   * - 分批查询（500/批）避免内存峰值
   * - 超过 max 截断 + Header X-Audit-Truncated:true
   */
  async exportCsv(q: AuditLogQuery, max = 10000): Promise<void> {
    const { ctx } = this;
    const where = this._buildWhere(q);

    ctx.set('Content-Type', 'text/csv; charset=utf-8');
    ctx.set('Content-Disposition',
            `attachment; filename="audit-logs-${Date.now()}.csv"`);

    // eslint-disable-next-line @typescript-eslint/no-var-requires
    const { PassThrough } = require('stream');
    const stream = new PassThrough();
    ctx.body = stream;

    // UTF-8 BOM（Excel 识别中文）
    stream.write('\uFEFF');

    const HEADERS = ['ID', '时间', '用户', '模块', '动作', '业务类型', '业务ID',
                     '描述', 'IP', 'URL', '方法', '耗时(ms)', '状态', '失败原因'];
    stream.write(HEADERS.join(',') + '\n');

    const BATCH = 500;
    let offset = 0;
    let truncated = false;
    while (offset < max) {
      const limit = Math.min(BATCH, max - offset);
      const rows = await ctx.model.AuditLog.findAll({
        where,
        order: [['id', 'DESC']],
        offset,
        limit,
        attributes: { exclude: ['beforeData', 'afterData', 'requestParams'] },
      });
      if (rows.length === 0) break;
      for (const r of rows) {
        stream.write(this._rowToCsv(r as any) + '\n');
      }
      offset += rows.length;
      if (rows.length < limit) break;  // 数据已全部导出
    }

    // 检查是否还有更多数据未导出
    if (offset >= max) {
      const remain = await ctx.model.AuditLog.count({ where });
      if (remain > max) {
        truncated = true;
        ctx.set('X-Audit-Truncated', 'true');
      }
    }

    if (truncated) {
      stream.write(`\n[NOTE] 已截断，仅导出最新 ${max} 行；如需更多请缩小时间范围\n`);
    }

    stream.end();
  }

  /** 一行 CSV 输出（处理逗号 / 引号 / 换行转义） */
  protected _rowToCsv(r: any): string {
    return [
      r.id,
      r.createdAt ? new Date(r.createdAt).toISOString() : '',
      r.username || r.userId || '',
      r.module, r.action,
      r.bizType || '', r.bizId || '',
      r.description || '',
      r.ip || '', r.requestUrl || '', r.requestMethod || '',
      r.costTime ?? '',
      r.status === 1 ? '成功' : '失败',
      r.failReason || '',
    ].map((v) => this._csvField(v)).join(',');
  }

  /** CSV 字段转义 */
  protected _csvField(v: any): string {
    if (v == null) return '';
    const s = String(v);
    if (/[",\n\r]/.test(s)) return `"${s.replace(/"/g, '""')}"`;
    return s;
  }
```

> ⚠️ **导出后再 count 一次确认 truncated**：避免 max 恰好等于实际行数时误标 truncated；只有当 `count > max` 才标。

- [ ] **Step 2: 类型检查**

```bash
cd super-tool-node && ./node_modules/.bin/tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add super-tool-node/app/service/audit.ts
git commit -m "feat(service): audit.exportCsv 流式 CSV 导出 + UTF-8 BOM + 截断检测"
```

---

## Task 4: service/audit.ts 单元测试（4 case）

**Spec:** §8.2 audit.test.ts

**Files:**
- Create: `super-tool-node/test/service/audit.test.ts`

- [ ] **Step 1: 创建测试文件**

```typescript
import { app } from 'egg-mock/bootstrap';
import * as assert from 'assert';

describe('test/service/audit.test.ts', () => {
  let ctx: any;

  beforeEach(() => {
    ctx = app.mockContext({
      state: {
        user: { id: 1, username: 'admin' },
        requestStartTime: Date.now() - 50,
      },
      headers: { 'user-agent': 'jest', 'x-platform': 'admin' },
      ip: '127.0.0.1',
    } as any);
  });

  afterEach(() => app.mockRestore());

  it('成功路径写入完整字段（含 ctx 自动注入字段）', async () => {
    let captured: any;
    app.mockClassFunction(ctx.model.AuditLog, 'create', (data: any) => {
      captured = data;
      return Promise.resolve(data);
    });

    await ctx.service.audit.log({
      module: 'tool',
      action: 'delete',
      bizType: 'tool',
      bizId: 5,
      beforeData: { name: 'foo' },
      status: 1,
    });

    assert.strictEqual(captured.module, 'tool');
    assert.strictEqual(captured.action, 'delete');
    assert.strictEqual(captured.userId, 1);
    assert.strictEqual(captured.username, 'admin');
    assert.strictEqual(captured.bizId, '5');
    assert.strictEqual(captured.status, 1);
    assert.strictEqual(captured.ip, '127.0.0.1');
    assert.deepStrictEqual(captured.beforeData, { name: 'foo' });
    assert.ok(typeof captured.costTime === 'number' && captured.costTime >= 0);
  });

  it('失败路径写入 status=0 + failReason', async () => {
    let captured: any;
    app.mockClassFunction(ctx.model.AuditLog, 'create', (data: any) => {
      captured = data;
      return Promise.resolve(data);
    });

    await ctx.service.audit.log({
      module: 'tool',
      action: 'delete',
      status: 0,
      failReason: 'permission denied',
    });

    assert.strictEqual(captured.status, 0);
    assert.strictEqual(captured.failReason, 'permission denied');
  });

  it('beforeData 仅传时 afterData 为 null，反之亦然', async () => {
    const captures: any[] = [];
    app.mockClassFunction(ctx.model.AuditLog, 'create', (data: any) => {
      captures.push(data);
      return Promise.resolve(data);
    });

    await ctx.service.audit.log({ module: 'x', action: 'a', beforeData: { v: 1 } });
    await ctx.service.audit.log({ module: 'x', action: 'a', afterData: { v: 2 } });

    assert.deepStrictEqual(captures[0].beforeData, { v: 1 });
    assert.strictEqual(captures[0].afterData, null);
    assert.strictEqual(captures[1].beforeData, null);
    assert.deepStrictEqual(captures[1].afterData, { v: 2 });
  });

  it('AuditLog.create 抛异常时 service.log 内部吞异常 + logger.warn', async () => {
    app.mockClassFunction(ctx.model.AuditLog, 'create', () => {
      return Promise.reject(new Error('DB connection lost'));
    });
    let warned = false;
    app.mockClassFunction(ctx.logger, 'warn', () => { warned = true; });

    // 不应抛
    await ctx.service.audit.log({ module: 'x', action: 'a' });

    assert.ok(warned, 'logger.warn 应被调用');
  });
});
```

- [ ] **Step 2: 跑测试**

```bash
cd super-tool-node && npm test -- --grep "test/service/audit"
```

预期：4 case 全部 PASS。

> 如果 `--grep` 不工作，直接 `npm test test/service/audit.test.ts`。

- [ ] **Step 3: Commit**

```bash
git add super-tool-node/test/service/audit.test.ts
git commit -m "test(audit): 新增 audit service 单元测试 (4 case)"
```

---

## Task 5: controller/admin/audit-log.ts

**Spec:** §6.1

**Files:**
- Create: `super-tool-node/app/controller/admin/audit-log.ts`

- [ ] **Step 1: 创建 controller 文件**

```typescript
import BaseController from '../base';

export default class AuditLogController extends BaseController {
  /** GET /api/admin/audit-logs */
  async list() {
    const q = this.ctx.query as any;
    const result = await this.service.audit.list({
      page: q.page ? Number(q.page) : undefined,
      pageSize: q.pageSize ? Number(q.pageSize) : undefined,
      startTime: q.startTime,
      endTime: q.endTime,
      userId: q.userId ? Number(q.userId) : undefined,
      module: q.module,
      action: q.action,
      status: q.status !== undefined && q.status !== ''
              ? (Number(q.status) as 0 | 1)
              : undefined,
      keyword: q.keyword,
    });
    this.success(result);
  }

  /** GET /api/admin/audit-logs/:id */
  async detail() {
    const id = Number(this.ctx.params.id);
    if (!id || Number.isNaN(id)) this.ctx.throw(422, 'invalid id');
    const data = await this.service.audit.detail(id);
    if (!data) this.ctx.throw(404, 'audit log not found');
    this.success(data);
  }

  /** GET /api/admin/audit-logs/export */
  async exportCsv() {
    const q = this.ctx.query as any;
    const max = Math.min(10000, Number(q.max) || 10000);
    await this.service.audit.exportCsv({
      startTime: q.startTime,
      endTime: q.endTime,
      userId: q.userId ? Number(q.userId) : undefined,
      module: q.module,
      action: q.action,
      status: q.status !== undefined && q.status !== ''
              ? (Number(q.status) as 0 | 1)
              : undefined,
      keyword: q.keyword,
    }, max);
    // 不调 this.success — service 已经 set ctx.body = stream
  }
}
```

- [ ] **Step 2: 类型检查**

```bash
cd super-tool-node && ./node_modules/.bin/tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add super-tool-node/app/controller/admin/audit-log.ts
git commit -m "feat(controller): 新增 admin/audit-log - list/detail/export"
```

---

## Task 6: 路由注册 + 端到端 supertest 测试

**Spec:** §6.2, §8.2 audit-log.test.ts

**Files:**
- Modify: `super-tool-node/app/router.ts`
- Create: `super-tool-node/test/api/audit-log.test.ts`

- [ ] **Step 1: 在 router.ts 注册 3 条路由**

打开 `app/router.ts`，找到 Spec-B 已有的 admin auth 段（`router.get('/api/admin/auth/menus' ...)`），在其**之前**或之后插入新的「审计日志」段（位置选择：Spec-B 段之后、Dashboard 段之前，保持模块就近原则）：

```typescript
  // ==================== 审计日志 ====================
  // 注意：export 路由必须放在 :id 之前，否则 /export 被 :id 吞掉
  router.get('/api/admin/audit-logs',
    auth, perm('system:audit-log:list'),
    controller.admin.auditLog.list);
  router.get('/api/admin/audit-logs/export',
    auth, perm('system:audit-log:export'),
    controller.admin.auditLog.exportCsv);
  router.get('/api/admin/audit-logs/:id',
    auth, perm('system:audit-log:detail'),
    controller.admin.auditLog.detail);
```

> 注意 controller 类名 `AuditLogController` 在 egg 中按文件名 `audit-log.ts` 自动注册为 `controller.admin.auditLog`（连字符转驼峰）。

- [ ] **Step 2: 创建端到端测试**

```typescript
// test/api/audit-log.test.ts
import { app } from 'egg-mock/bootstrap';
import * as assert from 'assert';

describe('test/api/audit-log.test.ts', () => {
  afterEach(() => app.mockRestore());

  it('GET /api/admin/audit-logs 解析 query 参数并传给 service', async () => {
    let captured: any;
    app.mockClassFunction(app.serviceClasses.audit.prototype, 'list',
      function(q: any) { captured = q; return Promise.resolve({ total: 0, rows: [], page: 1, pageSize: 20 }); }
    );

    // mock auth + perm 中间件
    app.mockContext({ state: { user: { id: 1, username: 'admin' } } });
    app.httpRequest = (app as any).httpRequest;

    // 注：实际项目可能用 mockToken 或测试专用 admin token；这里以最小可用为准
    // 如果 perm 中间件查 super_admin 短路，请确保 mockUser 是 super_admin
    const res = await (app as any).httpRequest()
      .get('/api/admin/audit-logs?page=2&pageSize=50&module=tool&action=delete&userId=5&status=1&keyword=foo')
      .set('Authorization', 'Bearer test-super-admin-token');

    // 接口可能返回 401（如果 token 校验未 mock），此 case 验证 service.list 调用参数即可
    if (captured) {
      assert.strictEqual(captured.page, 2);
      assert.strictEqual(captured.pageSize, 50);
      assert.strictEqual(captured.module, 'tool');
      assert.strictEqual(captured.action, 'delete');
      assert.strictEqual(captured.userId, 5);
      assert.strictEqual(captured.status, 1);
      assert.strictEqual(captured.keyword, 'foo');
    } else {
      // 401/403 路径下也算 case 通过（auth 中间件正确拦截）
      assert.ok([200, 401, 403].includes(res.status));
    }
  });

  it('GET /api/admin/audit-logs/export 设置正确 Content-Type', async () => {
    let exportCalled = false;
    app.mockClassFunction(app.serviceClasses.audit.prototype, 'exportCsv',
      function(this: any, _q: any, _max: number) {
        exportCalled = true;
        this.ctx.set('Content-Type', 'text/csv; charset=utf-8');
        this.ctx.set('Content-Disposition', 'attachment; filename="audit-logs-test.csv"');
        // eslint-disable-next-line @typescript-eslint/no-var-requires
        const { PassThrough } = require('stream');
        const stream = new PassThrough();
        this.ctx.body = stream;
        stream.end();
        return Promise.resolve();
      }
    );

    const res = await (app as any).httpRequest()
      .get('/api/admin/audit-logs/export?module=tool')
      .set('Authorization', 'Bearer test-super-admin-token');

    if (exportCalled) {
      assert.ok(res.headers['content-type']?.includes('text/csv'));
      assert.ok(res.headers['content-disposition']?.includes('attachment'));
    } else {
      assert.ok([401, 403].includes(res.status));
    }
  });
});
```

> ⚠️ **关于 auth/perm 中间件 mock**：本项目使用基于 JWT 的 auth + RBAC 的 perm 中间件，supertest 完整流程需要 mock 用户 token。如果 `httpRequest()` 模式下 token 校验复杂，可改为**直接用 mockContext + 手动调 controller 方法**，避免走完整 HTTP 链路。**实施工程师可根据项目现有 test/api/ 中既有 .test.ts 的 token mock 模式参考调整。**

- [ ] **Step 3: 跑测试**

```bash
cd super-tool-node && npm test -- --grep "test/api/audit-log"
```

预期：2 case 全部 PASS（或对 401 路径也接受）。

- [ ] **Step 4: 启动 dev 手工冒烟**

```bash
cd super-tool-node && npm run dev
```

另起终端用 curl 验证（用前面 008 SQL 已迁移的 super_admin 账号 token）：

```bash
# 使用 admin 登录获取 token（参考已有 auth 接口）
curl -X POST http://localhost:7001/api/admin/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"<your-password>","clientId":"...","clientSecret":"..."}'

# 拿到 accessToken 后
TOKEN="<accessToken>"
curl http://localhost:7001/api/admin/audit-logs -H "Authorization: Bearer $TOKEN"
# 预期：{ code: 200, data: { total: 0, rows: [], ... }, ... }（数据为空因为还没回填）
```

- [ ] **Step 5: Commit**

```bash
git add super-tool-node/app/router.ts super-tool-node/test/api/audit-log.test.ts
git commit -m "feat(api): 注册 audit-logs 路由 + supertest 测试"
```

---

## Task 7: service/permission.ts 三个新方法

**Spec:** §5.1, §5.2, §5.3, §5.4

**Files:**
- Modify: `super-tool-node/app/service/permission.ts`

- [ ] **Step 1: 读现有 permission.ts 末尾位置**

打开 `app/service/permission.ts`，定位最后一个 `}` 之前（即 class 结束之前），新增三个方法。

- [ ] **Step 2: 新增 testForUser 方法**

在 class 末尾追加：

```typescript
  /**
   * Mode 1: 用户全景 — roles + codes + menus
   */
  async testForUser(userId: number, platform = 'admin') {
    const user = await this.ctx.model.User.findByPk(userId, {
      attributes: ['id', 'username', 'nickname', 'status'],
    });
    if (!user) {
      return {
        user: null,
        roles: [],
        isSuperAdmin: false,
        permissionCodes: [],
        menus: [],
        stats: { totalCodes: 0, totalMenus: 0, byModule: {} },
      };
    }

    const roles = await this.service.role.getUserRoles(userId);
    const isSuperAdmin = roles.some((r: any) => r.code === 'super_admin');
    const permissionCodes = await this.getUserPermissionCodes(userId);
    const menus = await this.getMenusForUser(userId, platform);

    // byModule 聚合：根据 codes 反查 permissions 表的 module 字段
    const byModule: Record<string, number> = {};
    if (permissionCodes.length > 0) {
      const perms = await this.ctx.model.Permission.findAll({
        where: { code: permissionCodes, platform },
        attributes: ['module'],
      });
      for (const p of perms) {
        const m = (p as any).module;
        byModule[m] = (byModule[m] || 0) + 1;
      }
    }

    return {
      user: (user as any).toJSON(),
      roles: roles.map((r: any) => ({
        id: r.id, code: r.code, name: r.name,
      })),
      isSuperAdmin,
      permissionCodes,
      menus,
      stats: {
        totalCodes: permissionCodes.length,
        totalMenus: this._countLeafMenus(menus),
        byModule,
      },
    };
  }

  /** 统计菜单树中的叶子节点数量（递归） */
  private _countLeafMenus(tree: any[]): number {
    let n = 0;
    for (const node of tree || []) {
      if (node.children && node.children.length > 0) {
        n += this._countLeafMenus(node.children);
      } else {
        n += 1;
      }
    }
    return n;
  }
```

- [ ] **Step 3: 新增 checkUserAccess 方法**

继续追加：

```typescript
  /**
   * Mode 2: 单接口/权限码命中检查
   * @param target 二选一：{code} 或 {path, method}
   */
  async checkUserAccess(
    userId: number,
    target: { code?: string; path?: string; method?: string },
  ) {
    // 1. 用户存在性
    const user = await this.ctx.model.User.findByPk(userId, {
      attributes: ['id', 'username', 'status'],
    });
    if (!user) {
      return this._buildDenyResult(null, target, 'user_not_found');
    }
    if ((user as any).status !== 1) {
      return this._buildDenyResult((user as any).toJSON(), target, 'user_disabled');
    }

    // 2. 角色绑定
    const roles = await this.service.role.getUserRoles(userId);
    if (roles.length === 0) {
      return this._buildDenyResult((user as any).toJSON(), target, 'no_roles');
    }
    const activeRoles = roles.filter((r: any) => r.status === 1);
    if (activeRoles.length === 0) {
      return this._buildDenyResult((user as any).toJSON(), target, 'roles_disabled');
    }

    // 3. super_admin 短路
    const isSuperAdmin = activeRoles.some((r: any) => r.code === 'super_admin');
    if (isSuperAdmin) {
      const resolved = await this._resolveTarget(target);
      return {
        user: { id: (user as any).id, username: (user as any).username },
        target: resolved.target,
        allowed: true,
        matchedRoles: [{
          id: activeRoles.find((r: any) => r.code === 'super_admin').id,
          code: 'super_admin',
          name: '超级管理员',
        }],
        matchedPermissions: [{ via: 'super_admin_short_circuit' }],
        denyReason: null,
      };
    }

    // 4. 解析 target → permission 记录
    const resolved = await this._resolveTarget(target);
    if (!resolved.permission) {
      return this._buildDenyResult((user as any).toJSON(), resolved.target, 'permission_not_exists');
    }
    if ((resolved.permission as any).status !== 1) {
      return this._buildDenyResult((user as any).toJSON(), resolved.target, 'permission_disabled');
    }

    // 5. 检查用户是否拥有该权限码
    const codes = await this.getUserPermissionCodes(userId);
    const targetCode = resolved.target.code!;
    if (!codes.includes(targetCode)) {
      return this._buildDenyResult((user as any).toJSON(), resolved.target, 'permission_not_granted');
    }

    // 6. 命中：回填 matchedRoles + matchedPermissions
    const matched = await this._findMatchedRolesAndPermissions(userId, targetCode);
    return {
      user: { id: (user as any).id, username: (user as any).username },
      target: resolved.target,
      allowed: true,
      matchedRoles: matched.roles,
      matchedPermissions: matched.permissions,
      denyReason: null,
    };
  }

  /** 解析 target → permission 记录 */
  private async _resolveTarget(target: { code?: string; path?: string; method?: string }) {
    let permission: any = null;
    let resolvedTarget: any = { ...target };

    if (target.code) {
      permission = await this.ctx.model.Permission.findOne({
        where: { code: target.code, platform: 'admin' },
      });
      resolvedTarget = {
        type: 'code',
        code: target.code,
        permissionExists: !!permission,
        permissionId: permission?.id,
        permissionName: permission?.name,
      };
    } else if (target.path && target.method) {
      permission = await this.ctx.model.Permission.findOne({
        where: {
          path: target.path,
          method: target.method.toUpperCase(),
          platform: 'admin',
        },
        order: [['sort', 'ASC'], ['id', 'ASC']],
      });
      resolvedTarget = {
        type: 'api',
        path: target.path,
        method: target.method.toUpperCase(),
        code: permission?.code,
        permissionExists: !!permission,
        permissionId: permission?.id,
        permissionName: permission?.name,
      };
    }

    return { target: resolvedTarget, permission };
  }

  /** 构造拒绝结果 */
  private _buildDenyResult(user: any, target: any, denyReason: string) {
    return {
      user: user ? { id: user.id, username: user.username } : null,
      target,
      allowed: false,
      matchedRoles: [],
      matchedPermissions: [],
      denyReason,
    };
  }

  /** 查询命中的 roles + permissions（用于回填 matchedXxx） */
  private async _findMatchedRolesAndPermissions(userId: number, code: string) {
    const sql = `
      SELECT r.id AS role_id, r.code AS role_code, r.name AS role_name,
             p.id AS perm_id, p.code AS perm_code
      FROM roles r
      INNER JOIN role_permissions rp ON r.id = rp.role_id
      INNER JOIN permissions p       ON p.id = rp.permission_id
      INNER JOIN user_roles ur       ON ur.role_id = r.id
      WHERE ur.user_id = :userId
        AND p.code = :code
        AND r.status = 1
        AND p.status = 1
    `;
    const rows: any[] = await this.app.model.query(sql, {
      replacements: { userId, code },
      type: (this.app.Sequelize as any).QueryTypes.SELECT,
    }) as any;

    const roleMap = new Map<number, any>();
    const permList: any[] = [];
    for (const row of rows) {
      if (!roleMap.has(row.role_id)) {
        roleMap.set(row.role_id, {
          id: row.role_id, code: row.role_code, name: row.role_name,
        });
      }
      permList.push({
        id: row.perm_id, code: row.perm_code, via: `role:${row.role_code}`,
      });
    }
    return {
      roles: Array.from(roleMap.values()),
      permissions: permList,
    };
  }
```

- [ ] **Step 4: 新增 testForRole 方法**

继续追加：

```typescript
  /**
   * Mode 3: 角色权限矩阵
   */
  async testForRole(input: { roleCode?: string; roleId?: number }) {
    const where: any = {};
    if (input.roleId) where.id = input.roleId;
    else if (input.roleCode) where.code = input.roleCode;
    else throw new Error('roleCode or roleId required');

    const role = await this.ctx.model.Role.findOne({ where });
    if (!role) {
      return {
        role: null, ownedCodes: [], permissionTree: [],
        stats: { total: 0, byModule: {}, byType: {} },
        boundUserCount: 0,
      };
    }

    const sql = `
      SELECT p.id, p.code, p.name, p.type, p.module, p.path, p.icon, p.parent_id, p.sort
      FROM permissions p
      INNER JOIN role_permissions rp ON p.id = rp.permission_id
      WHERE rp.role_id = :roleId AND p.platform = 'admin' AND p.status = 1
      ORDER BY p.sort ASC, p.id ASC
    `;
    const owned: any[] = await this.app.model.query(sql, {
      replacements: { roleId: (role as any).id },
      type: (this.app.Sequelize as any).QueryTypes.SELECT,
    }) as any;

    const ownedCodes = owned.map((p: any) => p.code);
    const byModule: Record<string, number> = {};
    const byType: Record<string, number> = {};
    for (const p of owned) {
      byModule[p.module] = (byModule[p.module] || 0) + 1;
      const typeName = p.type === 2 ? 'menu' : p.type === 4 ? 'api' : (p.type === 1 ? 'group' : `type${p.type}`);
      byType[typeName] = (byType[typeName] || 0) + 1;
    }

    // permissionTree: 复用 Spec-B 已有 buildTree（注意字段名映射 parent_id → parentId）
    const ownedNormalized = owned.map((p: any) => ({
      ...p, parentId: p.parent_id,
    }));
    const permissionTree = this.buildTree(ownedNormalized, 0);

    const boundUserCount = await this.ctx.model.UserRole.count({
      where: { roleId: (role as any).id },
    });

    return {
      role: {
        id: (role as any).id,
        code: (role as any).code,
        name: (role as any).name,
        status: (role as any).status,
      },
      ownedCodes,
      permissionTree,
      stats: {
        total: owned.length,
        byModule,
        byType,
      },
      boundUserCount,
    };
  }
```

- [ ] **Step 5: 类型检查**

```bash
cd super-tool-node && ./node_modules/.bin/tsc --noEmit
```

预期 exit=0；如果 `buildTree` 是 private 报错，把它改成 protected 或留作 plan 阶段确认。

- [ ] **Step 6: Commit**

```bash
git add super-tool-node/app/service/permission.ts
git commit -m "feat(service): permission 增 testForUser/checkUserAccess/testForRole 三方法"
```

---

## Task 8: controller.permission.test() 路由分发 + 路由注册

**Spec:** §5.5, §5.6

**Files:**
- Modify: `super-tool-node/app/controller/admin/permission.ts`
- Modify: `super-tool-node/app/router.ts`

- [ ] **Step 1: 在 controller/admin/permission.ts class 末尾追加 test 方法**

```typescript
  /** GET /api/admin/permissions/test — 权限测试综合入口 */
  async test() {
    const { mode } = this.ctx.query as any;
    switch (mode) {
      case 'user-overview':
        return this._handleUserOverview();
      case 'user-check':
        return this._handleUserCheck();
      case 'role-check':
        return this._handleRoleCheck();
      default:
        this.ctx.throw(422, `unknown mode: ${mode}`);
    }
  }

  private async _handleUserOverview() {
    const userId = Number(this.ctx.query.userId);
    if (!userId) this.ctx.throw(422, 'userId required');
    const data = await this.service.permission.testForUser(userId);
    this.success(data);
  }

  private async _handleUserCheck() {
    const q = this.ctx.query as any;
    const userId = Number(q.userId);
    if (!userId) this.ctx.throw(422, 'userId required');
    const target: { code?: string; path?: string; method?: string } = {};
    if (q.code) target.code = String(q.code);
    else if (q.path && q.method) {
      target.path = String(q.path);
      target.method = String(q.method).toUpperCase();
    } else {
      this.ctx.throw(422, 'either code or (path+method) required');
    }
    const data = await this.service.permission.checkUserAccess(userId, target);
    this.success(data);
  }

  private async _handleRoleCheck() {
    const q = this.ctx.query as any;
    if (!q.roleCode && !q.roleId) {
      this.ctx.throw(422, 'roleCode or roleId required');
    }
    const data = await this.service.permission.testForRole({
      roleCode: q.roleCode ? String(q.roleCode) : undefined,
      roleId: q.roleId ? Number(q.roleId) : undefined,
    });
    this.success(data);
  }
```

- [ ] **Step 2: 在 router.ts 注册新路由**

在 Task 6 已有的「审计日志」段之后追加「权限测试」段：

```typescript
  // ==================== 权限测试 ====================
  router.get('/api/admin/permissions/test',
    auth, perm('system:permission-test:run'),
    controller.admin.permission.test);
```

- [ ] **Step 3: 类型检查 + 启动 dev 冒烟**

```bash
cd super-tool-node && ./node_modules/.bin/tsc --noEmit
npm run dev
```

curl 验证：

```bash
TOKEN="<super-admin-token>"
curl "http://localhost:7001/api/admin/permissions/test?mode=user-overview&userId=1" \
  -H "Authorization: Bearer $TOKEN"
# 预期：{ code: 200, data: { user, roles, isSuperAdmin, permissionCodes, menus, stats } }

curl "http://localhost:7001/api/admin/permissions/test?mode=user-check&userId=1&code=tool:list" \
  -H "Authorization: Bearer $TOKEN"
# 预期：allowed=true（admin 用户拥有 tool:list）

curl "http://localhost:7001/api/admin/permissions/test?mode=role-check&roleCode=auditor" \
  -H "Authorization: Bearer $TOKEN"
# 预期：role/ownedCodes/permissionTree/stats/boundUserCount
```

- [ ] **Step 4: Commit**

```bash
git add super-tool-node/app/controller/admin/permission.ts super-tool-node/app/router.ts
git commit -m "feat(api): permission.test() 三 mode 路由分发 + 路由注册"
```

---

## Task 9: permission-test 单元测试（6 case）

**Spec:** §8.2 permission-test.test.ts

**Files:**
- Create: `super-tool-node/test/service/permission-test.test.ts`

- [ ] **Step 1: 创建测试文件**

```typescript
import { app } from 'egg-mock/bootstrap';
import * as assert from 'assert';

describe('test/service/permission-test.test.ts', () => {
  let ctx: any;

  beforeEach(() => {
    ctx = app.mockContext();
  });
  afterEach(() => app.mockRestore());

  // ============ testForUser ============
  describe('testForUser', () => {
    it('返回 user/roles/codes/menus/stats 完整字段（普通用户）', async () => {
      // mock User.findByPk → { id:2, username:'op', status:1 }
      app.mockClassFunction(ctx.model.User, 'findByPk', () =>
        Promise.resolve({ toJSON: () => ({ id: 2, username: 'op', nickname: 'Op', status: 1 }) }));

      // mock service.role.getUserRoles → [operator role]
      app.mockClassFunction(ctx.service.role, 'getUserRoles', () =>
        Promise.resolve([{ id: 3, code: 'operator', name: '运营管理员', status: 1 }]));

      // mock self.getUserPermissionCodes → ['tool:list','tool:menu']
      app.mockClassFunction(ctx.service.permission, 'getUserPermissionCodes', () =>
        Promise.resolve(['tool:list', 'tool:menu']));

      // mock self.getMenusForUser → 返回简化菜单
      app.mockClassFunction(ctx.service.permission, 'getMenusForUser', () =>
        Promise.resolve([{ id: 1, code: 'tool', children: [{ id: 2, code: 'tool:menu', children: [] }] }]));

      // mock Permission.findAll → 用于 byModule 聚合
      app.mockClassFunction(ctx.model.Permission, 'findAll', () =>
        Promise.resolve([{ module: 'tool' }, { module: 'tool' }]));

      const r = await ctx.service.permission.testForUser(2);
      assert.strictEqual(r.user.id, 2);
      assert.strictEqual(r.isSuperAdmin, false);
      assert.deepStrictEqual(r.permissionCodes, ['tool:list', 'tool:menu']);
      assert.strictEqual(r.stats.totalCodes, 2);
      assert.strictEqual(r.stats.byModule.tool, 2);
    });

    it('super_admin 用户 isSuperAdmin=true', async () => {
      app.mockClassFunction(ctx.model.User, 'findByPk', () =>
        Promise.resolve({ toJSON: () => ({ id: 1, username: 'sa', status: 1 }) }));
      app.mockClassFunction(ctx.service.role, 'getUserRoles', () =>
        Promise.resolve([{ id: 1, code: 'super_admin', name: '超管', status: 1 }]));
      app.mockClassFunction(ctx.service.permission, 'getUserPermissionCodes', () =>
        Promise.resolve(['ANY:CODE']));
      app.mockClassFunction(ctx.service.permission, 'getMenusForUser', () =>
        Promise.resolve([]));
      app.mockClassFunction(ctx.model.Permission, 'findAll', () => Promise.resolve([]));

      const r = await ctx.service.permission.testForUser(1);
      assert.strictEqual(r.isSuperAdmin, true);
    });
  });

  // ============ checkUserAccess ============
  describe('checkUserAccess', () => {
    it('用户拥有该 code → allowed=true + matchedRoles 非空', async () => {
      app.mockClassFunction(ctx.model.User, 'findByPk', () =>
        Promise.resolve({ id: 2, username: 'op', status: 1, toJSON() { return this; } }));
      app.mockClassFunction(ctx.service.role, 'getUserRoles', () =>
        Promise.resolve([{ id: 3, code: 'operator', status: 1 }]));
      app.mockClassFunction(ctx.model.Permission, 'findOne', () =>
        Promise.resolve({ id: 56, name: '查看工具列表', status: 1 }));
      app.mockClassFunction(ctx.service.permission, 'getUserPermissionCodes', () =>
        Promise.resolve(['tool:list']));
      // mock 内部 _findMatchedRolesAndPermissions 走 raw query → 直接 mock app.model.query
      app.mockClassFunction(app.model, 'query' as any, () =>
        Promise.resolve([{ role_id: 3, role_code: 'operator', role_name: '运营', perm_id: 56, perm_code: 'tool:list' }]));

      const r = await ctx.service.permission.checkUserAccess(2, { code: 'tool:list' });
      assert.strictEqual(r.allowed, true);
      assert.strictEqual(r.matchedRoles[0].code, 'operator');
      assert.strictEqual(r.denyReason, null);
    });

    it('用户不存在 → denyReason=user_not_found', async () => {
      app.mockClassFunction(ctx.model.User, 'findByPk', () => Promise.resolve(null));
      const r = await ctx.service.permission.checkUserAccess(999, { code: 'tool:list' });
      assert.strictEqual(r.allowed, false);
      assert.strictEqual(r.denyReason, 'user_not_found');
    });

    it('权限码不存在 → denyReason=permission_not_exists', async () => {
      app.mockClassFunction(ctx.model.User, 'findByPk', () =>
        Promise.resolve({ id: 2, username: 'op', status: 1, toJSON() { return this; } }));
      app.mockClassFunction(ctx.service.role, 'getUserRoles', () =>
        Promise.resolve([{ id: 3, code: 'operator', status: 1 }]));
      app.mockClassFunction(ctx.model.Permission, 'findOne', () => Promise.resolve(null));

      const r = await ctx.service.permission.checkUserAccess(2, { code: 'tool:foobar' });
      assert.strictEqual(r.allowed, false);
      assert.strictEqual(r.denyReason, 'permission_not_exists');
    });

    it('角色未授予 → denyReason=permission_not_granted', async () => {
      app.mockClassFunction(ctx.model.User, 'findByPk', () =>
        Promise.resolve({ id: 2, username: 'op', status: 1, toJSON() { return this; } }));
      app.mockClassFunction(ctx.service.role, 'getUserRoles', () =>
        Promise.resolve([{ id: 4, code: 'auditor', status: 1 }]));
      app.mockClassFunction(ctx.model.Permission, 'findOne', () =>
        Promise.resolve({ id: 99, name: '创建工具', status: 1 }));
      app.mockClassFunction(ctx.service.permission, 'getUserPermissionCodes', () =>
        Promise.resolve(['stats:overview']));  // 没有 tool:create

      const r = await ctx.service.permission.checkUserAccess(2, { code: 'tool:create' });
      assert.strictEqual(r.allowed, false);
      assert.strictEqual(r.denyReason, 'permission_not_granted');
    });
  });

  // ============ testForRole ============
  describe('testForRole', () => {
    it('返回 role/ownedCodes/permissionTree/stats/boundUserCount', async () => {
      app.mockClassFunction(ctx.model.Role, 'findOne', () =>
        Promise.resolve({ id: 4, code: 'auditor', name: '审计员', status: 1 }));
      app.mockClassFunction(app.model, 'query' as any, () =>
        Promise.resolve([
          { id: 10, code: 'system:audit-log', name: '审计日志', type: 2, module: 'system', path: '/system/audit-log', icon: null, parent_id: 0, sort: 30 },
          { id: 11, code: 'system:audit-log:list', name: '列表', type: 4, module: 'system', path: '/api/admin/audit-logs', icon: null, parent_id: 10, sort: 10 },
        ]));
      app.mockClassFunction(ctx.model.UserRole, 'count', () => Promise.resolve(7));

      const r = await ctx.service.permission.testForRole({ roleCode: 'auditor' });
      assert.strictEqual(r.role.code, 'auditor');
      assert.deepStrictEqual(r.ownedCodes.sort(), ['system:audit-log', 'system:audit-log:list'].sort());
      assert.strictEqual(r.stats.byType.menu, 1);
      assert.strictEqual(r.stats.byType.api, 1);
      assert.strictEqual(r.boundUserCount, 7);
    });
  });
});
```

> ⚠️ **关于 `app.model.query` mock**：egg-mock 对全局 `app.model.query` 的 mock 可能需要稍微调整签名（具体看 egg-mock 版本）；如有问题，可改为 stub `service.permission._findMatchedRolesAndPermissions`/拦截私有方法。本测试是"信号大于绝对正确"——重点验证 denyReason 等业务分支，private SQL 走的是真实 DB 也可接受（在 unittest 环境）。

- [ ] **Step 2: 跑测试**

```bash
cd super-tool-node && npm test -- --grep "test/service/permission-test"
```

预期：6 case 全部 PASS。失败 case 按报错信息调整 mock 或 stub 写法。

- [ ] **Step 3: Commit**

```bash
git add super-tool-node/test/service/permission-test.test.ts
git commit -m "test(permission): permission-test service 单元测试 (6 case)"
```

---

## Task 10: 回填 role + permission 模块审计（6 接口）

**Spec:** §7.1, §7.2, §7.4

**Files:**
- Modify: `super-tool-node/app/controller/admin/role.ts`
- Modify: `super-tool-node/app/controller/admin/permission.ts`

回填范围：
- `role.ts`: create / update / destroy（3 接口）
- `permission.ts`: create / update / destroy（3 接口）

- [ ] **Step 1: 读现有 role.ts**

```bash
cat super-tool-node/app/controller/admin/role.ts
```

记录现有方法签名、参数处理、调用的 service 方法名。

- [ ] **Step 2: 改造 role.ts 三个写方法**

按 §7.1 标准模板（destroy/update/create）改造。**示例**（实际方法名以现有为准）：

```typescript
async create() {
  const body = this.ctx.request.body;
  try {
    const created = await this.service.role.create(body);
    await this.service.audit.log({
      module: 'role', action: 'create',
      bizType: 'role', bizId: (created as any).id,
      afterData: created,
      description: `创建角色 ${(created as any).code}`,
      status: 1,
    });
    this.created(created);
  } catch (e: any) {
    await this.service.audit.log({
      module: 'role', action: 'create',
      description: `尝试创建角色 ${body?.code || '(未知)'}`,
      status: 0, failReason: e.message,
    });
    throw e;
  }
}

async update() {
  const id = Number(this.ctx.params.id);
  let beforeData: any = null;
  try { beforeData = await this.service.role.findById(id); } catch {}

  try {
    const updated = await this.service.role.update(id, this.ctx.request.body);
    await this.service.audit.log({
      module: 'role', action: 'update', bizType: 'role', bizId: id,
      beforeData, afterData: updated,
      description: `更新角色 #${id}`, status: 1,
    });
    this.success(updated);
  } catch (e: any) {
    await this.service.audit.log({
      module: 'role', action: 'update', bizType: 'role', bizId: id,
      beforeData,
      description: `尝试更新角色 #${id}`,
      status: 0, failReason: e.message,
    });
    throw e;
  }
}

async destroy() {
  const id = Number(this.ctx.params.id);
  let beforeData: any = null;
  try { beforeData = await this.service.role.findById(id); } catch {}

  try {
    await this.service.role.destroy(id);
    await this.service.audit.log({
      module: 'role', action: 'delete', bizType: 'role', bizId: id,
      beforeData,
      description: `删除角色 #${id}`,
      status: 1,
    });
    this.success();
  } catch (e: any) {
    await this.service.audit.log({
      module: 'role', action: 'delete', bizType: 'role', bizId: id,
      beforeData,
      description: `尝试删除角色 #${id}`,
      status: 0, failReason: e.message,
    });
    throw e;
  }
}
```

> ⚠️ **如果 service 没有 `findById` 而是 `findByPk` 或 `getById`**，按现有命名调整。`beforeData` 查询失败用空 try/catch 吞掉。

- [ ] **Step 3: 同样模式改造 permission.ts 的 create/update/destroy**

复制 role 的三个模板，把 `module:'role'`、`bizType:'role'`、`description` 中的"角色"替换为"权限"，service 调用换为 `this.service.permission.xxx`。

- [ ] **Step 4: 类型检查 + dev 冒烟**

```bash
cd super-tool-node && ./node_modules/.bin/tsc --noEmit
npm run dev
```

curl 实测一次 role 更新：

```bash
TOKEN="<super-admin-token>"
curl -X PUT http://localhost:7001/api/admin/roles/3 \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name":"运营管理员（更新测试）"}'

# 直查 audit_logs
mysql -u <user> -p superadmin_db -e \
  "SELECT id,module,action,biz_id,status,LEFT(description,40) FROM audit_logs ORDER BY id DESC LIMIT 3"
# 预期：最新一行 module=role action=update biz_id=3 status=1
```

- [ ] **Step 5: Commit**

```bash
git add super-tool-node/app/controller/admin/role.ts \
        super-tool-node/app/controller/admin/permission.ts
git commit -m "feat(audit): 回填 role + permission 模块 6 个写操作的审计调用"
```

---

## Task 11: 回填 tool + category 模块审计（7 接口）

**Spec:** §7.1, §7.2, §7.3-A

**Files:**
- Modify: `super-tool-node/app/controller/admin/tool.ts`

回填范围：
- tool: create / update / destroy / batchUpdate（4 接口）
- category: createCategory / updateCategory / destroyCategory（3 接口）

> 实际方法名以现有 tool.ts 为准；如果 category 在另一个 controller，请相应调整文件。

- [ ] **Step 1: 读现有 tool.ts**

```bash
cat super-tool-node/app/controller/admin/tool.ts
```

记录所有写方法的签名（特别是 batchUpdate 的输入参数格式）。

- [ ] **Step 2: 改造 tool 三个标准写方法**

按 §7.1 标准模板套用，`module:'tool'`、`bizType:'tool'`：
- `create`: action='create' (Create 模板)
- `update`: action='update' (Update 模板)
- `destroy`: action='delete' (Destroy 模板)

代码同 Task 10 模板，将 `role` 替换为 `tool`。

- [ ] **Step 3: 改造 batchUpdate（按 §7.3-A 扩展模板）**

```typescript
async batchUpdate() {
  const { ids, ...changes } = this.ctx.request.body;
  if (!Array.isArray(ids) || ids.length === 0) {
    this.ctx.throw(422, 'ids required');
  }

  let beforeList: any[] = [];
  try { beforeList = await this.service.tool.findByIds(ids); } catch {}

  try {
    await this.service.tool.batchUpdate(ids, changes);
    await this.service.audit.log({
      module: 'tool', action: 'batch_update',
      bizType: 'tool',
      bizId: ids.slice(0, 10).join(','),  // 防止超长（bizId 字段 64 字节）
      beforeData: beforeList,
      afterData: changes,
      description: `批量更新 ${ids.length} 个工具`,
      status: 1,
    });
    this.success();
  } catch (e: any) {
    await this.service.audit.log({
      module: 'tool', action: 'batch_update',
      bizType: 'tool', bizId: ids.slice(0, 10).join(','),
      beforeData: beforeList,
      description: `尝试批量更新 ${ids.length} 个工具`,
      status: 0, failReason: e.message,
    });
    throw e;
  }
}
```

> **bizId 截断处理**：`ids.slice(0, 10).join(',')` 取前 10 个；这是对 §9.2 O2 的回应。如果 ids 长度更大，前端通常分批调用，10 个足以追溯。

- [ ] **Step 4: 改造 category 三个写方法**

同 Task 10 标准模板，`module:'category'`、`bizType:'category'`，service 调用换为 `this.service.tool.createCategory` 等（实际方法名以现有为准）。

- [ ] **Step 5: 类型检查 + dev 冒烟**

```bash
cd super-tool-node && ./node_modules/.bin/tsc --noEmit
npm run dev
```

实测一次 tool 创建 + 一次 batch update：

```bash
# 创建工具
curl -X POST http://localhost:7001/api/admin/tools -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" -d '{...}'
# 直查 audit_logs 确认 action=create

# 批量更新
curl -X POST http://localhost:7001/api/admin/tools/batch-update -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" -d '{"ids":[1,2,3],"status":0}'
# 直查 audit_logs 确认 action=batch_update biz_id="1,2,3"
```

- [ ] **Step 6: Commit**

```bash
git add super-tool-node/app/controller/admin/tool.ts
git commit -m "feat(audit): 回填 tool + category 模块 7 个写操作的审计调用"
```

---

## Task 12: 回填 user 模块审计（3 接口）

**Spec:** §7.1, §7.2, §7.3-B

**Files:**
- Modify: `super-tool-node/app/controller/admin/user.ts`

回填范围：
- update / destroy（标准模板）
- resetPassword（特殊：password 不入 audit）

- [ ] **Step 1: 读现有 user.ts**

```bash
cat super-tool-node/app/controller/admin/user.ts
```

- [ ] **Step 2: 改造 update / destroy（标准模板）**

同 Task 10 模板，`module:'user'`、`bizType:'user'`。

> 注意：update 时 beforeData 中的 password 字段（如果存在）必须剔除：

```typescript
let beforeData: any = null;
try {
  const raw = await this.service.user.findById(id);
  if (raw) {
    const cloned = JSON.parse(JSON.stringify(raw));
    delete cloned.password;
    delete cloned.passwordHash;
    beforeData = cloned;
  }
} catch {}
```

> service.audit 内部 _sanitizeParams 只脱敏 request.body 不会动 beforeData/afterData，所以**调用方必须显式删除**。

- [ ] **Step 3: 改造 resetPassword（按 §7.3-B 扩展模板）**

```typescript
async resetPassword() {
  const userId = Number(this.ctx.params.id);

  try {
    await this.service.user.resetPassword(userId);
    await this.service.audit.log({
      module: 'user', action: 'reset_password',
      bizType: 'user', bizId: userId,
      description: `重置用户 #${userId} 的密码`,
      // 故意不传 beforeData/afterData
      status: 1,
    });
    this.success();
  } catch (e: any) {
    await this.service.audit.log({
      module: 'user', action: 'reset_password',
      bizType: 'user', bizId: userId,
      description: `尝试重置用户 #${userId} 的密码`,
      status: 0, failReason: e.message,
    });
    throw e;
  }
}
```

- [ ] **Step 4: 类型检查 + dev 冒烟**

```bash
cd super-tool-node && ./node_modules/.bin/tsc --noEmit
npm run dev
```

实测 reset password 后查 audit_logs：

```bash
curl -X POST http://localhost:7001/api/admin/users/2/reset-password \
  -H "Authorization: Bearer $TOKEN"

mysql -u <user> -p superadmin_db -e \
  "SELECT request_params FROM audit_logs ORDER BY id DESC LIMIT 1"
# 预期：request_params 中 password 字段值为 '***' 或不存在
```

- [ ] **Step 5: Commit**

```bash
git add super-tool-node/app/controller/admin/user.ts
git commit -m "feat(audit): 回填 user 模块 3 个写操作的审计调用（含 password 脱敏）"
```

---

## Task 13: 回填 member 模块审计（2 接口）

**Spec:** §7.1, §7.2, §7.3-C

**Files:**
- Modify: `super-tool-node/app/controller/admin/member.ts`

回填范围：
- update（标准模板）
- adjustPoints（特殊：含 delta + reason）

- [ ] **Step 1: 读现有 member.ts**

```bash
cat super-tool-node/app/controller/admin/member.ts
```

- [ ] **Step 2: 改造 update（标准模板）**

同 Task 10 模板，`module:'member'`、`bizType:'member'`。

- [ ] **Step 3: 改造 adjustPoints（按 §7.3-C 扩展模板）**

```typescript
async adjustPoints() {
  const userId = Number(this.ctx.params.id);
  const { delta, reason } = this.ctx.request.body;

  let before: any = null;
  try { before = await this.service.member.getPoints(userId); } catch {}

  try {
    const after = await this.service.member.adjustPoints(userId, delta, reason);
    await this.service.audit.log({
      module: 'member', action: 'adjust_points',
      bizType: 'member', bizId: userId,
      beforeData: { points: before },
      afterData: { points: after, delta, reason },
      description: `调整用户 #${userId} 积分 ${delta > 0 ? '+' : ''}${delta}（${reason || '无原因'}）`,
      status: 1,
    });
    this.success({ points: after });
  } catch (e: any) {
    await this.service.audit.log({
      module: 'member', action: 'adjust_points',
      bizType: 'member', bizId: userId,
      beforeData: { points: before },
      description: `尝试调整用户 #${userId} 积分 ${delta}`,
      status: 0, failReason: e.message,
    });
    throw e;
  }
}
```

> **如果项目里 member 模块的方法名不是 `getPoints` / `adjustPoints`**（可能是 `getMember` / `addPoints` 等），按现有命名调整。如果当前 member controller **没有** adjustPoints 方法，跳过此步并在 commit message 注明。

- [ ] **Step 4: 类型检查 + dev 冒烟**

```bash
cd super-tool-node && ./node_modules/.bin/tsc --noEmit
npm run dev
```

实测：

```bash
curl -X POST http://localhost:7001/api/admin/members/2/points \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"delta":100,"reason":"测试奖励"}'

mysql -u <user> -p superadmin_db -e \
  "SELECT description FROM audit_logs WHERE action='adjust_points' ORDER BY id DESC LIMIT 1"
# 预期：description 含 "+100（测试奖励）"
```

- [ ] **Step 5: Commit**

```bash
git add super-tool-node/app/controller/admin/member.ts
git commit -m "feat(audit): 回填 member 模块 2 个写操作的审计调用"
```

---

## Task 14: docs/api/audit-log API 文档

**Spec:** §3.1（文件清单中的 docs/api/audit-log/README.md）

**Files:**
- Create: `super-tool-node/docs/api/audit-log/README.md`
- Modify: `super-tool-node/docs/api/README.md`
- Modify: `super-tool-node/docs/README.md`

- [ ] **Step 1: 创建 docs/api/audit-log/README.md**

```markdown
# 审计日志接口文档

> 模块：`system:audit-log`  |  接口数：3  |  Spec-A1（v2.8）

## 一、概述

提供后台审计日志的查询、详情、CSV 导出能力。所有需要追溯的高敏感写操作（角色 / 权限 / 工具 / 用户 / 会员）已在 controller 层调用 `service.audit.log()` 写入 `audit_logs` 表。

## 二、接口清单

| 方法 | 路径 | 权限码 | 说明 |
|------|------|--------|------|
| GET  | `/api/admin/audit-logs` | `system:audit-log:list` | 列表查询（7 维过滤 + 分页） |
| GET  | `/api/admin/audit-logs/:id` | `system:audit-log:detail` | 单条详情（含完整 before/after JSON） |
| GET  | `/api/admin/audit-logs/export` | `system:audit-log:export` | 同步 CSV 导出（≤10000 行） |

## 三、接口详情

### 3.1 列表查询

**GET** `/api/admin/audit-logs`

**Query 参数：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `page` | int | 页码（默认 1） |
| `pageSize` | int | 每页数量（默认 20，max 100） |
| `startTime` | ISO 8601 | 时间范围起 |
| `endTime` | ISO 8601 | 时间范围止 |
| `userId` | int | 操作用户 |
| `module` | string | 模块（如 tool / role / user） |
| `action` | string | 动作（如 create / update / delete / batch_update） |
| `status` | 0 \| 1 | 0=失败 1=成功 |
| `keyword` | string | description LIKE 模糊搜索 |

**Response（成功）：**
```json
{
  "code": 200,
  "data": {
    "total": 152,
    "page": 1,
    "pageSize": 20,
    "rows": [
      { "id": 152, "userId": 1, "username": "admin",
        "module": "tool", "action": "delete", "bizType": "tool", "bizId": "5",
        "description": "删除 tool #5", "ip": "127.0.0.1",
        "requestUrl": "/api/admin/tools/5", "requestMethod": "DELETE",
        "responseCode": 200, "costTime": 35, "status": 1, "failReason": null,
        "createdAt": "2026-05-11T03:14:22.000Z" }
    ]
  }
}
```

> **列表响应不含** `beforeData / afterData / requestParams`（避免响应过大），需要 JSON 详情请用 3.2。

### 3.2 单条详情

**GET** `/api/admin/audit-logs/:id`

**Response：** 同 3.1 的 row 字段，**额外含** `beforeData / afterData / requestParams` 三个 JSON 字段。

### 3.3 CSV 导出

**GET** `/api/admin/audit-logs/export`

**Query：** 同 3.1（不含 page/pageSize），可选 `max`（默认 10000，硬上限 10000）。

**Response：** CSV 文件流。
- `Content-Type: text/csv; charset=utf-8`
- `Content-Disposition: attachment; filename="audit-logs-<timestamp>.csv"`
- UTF-8 BOM 开头（Excel 识别中文）
- 14 列：`ID, 时间, 用户, 模块, 动作, 业务类型, 业务ID, 描述, IP, URL, 方法, 耗时(ms), 状态, 失败原因`
- 超过 max 行时截断，响应头额外含 `X-Audit-Truncated: true`

## 四、写入规范

后端在 controller 层显式调用 `service.audit.log({...})` 写入；详见 [Spec-A1 §7](../../superpowers/specs/2026-05-11-审计基础设施与权限测试设计文档.md#七高敏感接口回填17-个写操作)。

**Action 词表**：`create / update / delete / batch_update / reset_password / adjust_points / export / login / logout`（禁止临时新词）。

## 五、相关文档

- [Spec-A1 设计文档](../../superpowers/specs/2026-05-11-审计基础设施与权限测试设计文档.md)
- [Spec-A1 实施计划](../../superpowers/plans/2026-05-11-审计基础设施与权限测试实施计划.md)
- [audit_logs 表 schema](../../database/init.sql) line 628-657
```

- [ ] **Step 2: 更新 docs/api/README.md**

参照 Spec-B 加 admin-auth 章节的形式，在「权限管理」后追加「审计日志」一行：

```markdown
## 审计日志 (Audit Log) [查看文档](audit-log/README.md)

| 方法 | 路径 | 认证 |
|------|------|------|
| `GET` | `/api/admin/audit-logs` | ✅ |
| `GET` | `/api/admin/audit-logs/:id` | ✅ |
| `GET` | `/api/admin/audit-logs/export` | ✅ |
```

同时更新顶部接口总数：从 84 → 87（+3）。日期更新为 2026/5/11。

- [ ] **Step 3: 更新 docs/README.md**

在「管理端用户自查」一行下方插入「审计日志」一行：

```markdown
| [审计日志](api/audit-log/README.md)                     | 列表 + 详情 + CSV 导出（3个）       | 🆕 已发布 |
```

- [ ] **Step 4: Commit**

```bash
git add super-tool-node/docs/api/audit-log/ \
        super-tool-node/docs/api/README.md \
        super-tool-node/docs/README.md
git commit -m "docs(api): 新增 audit-log 接口文档"
```

---

## Task 15: docs/api/permission-test API 文档

**Spec:** §3.1（文件清单中的 docs/api/permission-test/README.md）

**Files:**
- Create: `super-tool-node/docs/api/permission-test/README.md`
- Modify: `super-tool-node/docs/api/README.md`
- Modify: `super-tool-node/docs/README.md`

- [ ] **Step 1: 创建 docs/api/permission-test/README.md**

```markdown
# 权限测试接口文档

> 模块：`system:permission-test`  |  接口数：1（3 mode）  |  Spec-A1（v2.8）

## 一、概述

提供权限调试综合工具。一个接口三种模式：
- **user-overview** — 用户全景（角色/权限码/菜单）
- **user-check** — 单接口/权限码命中检查（含 7 种 denyReason）
- **role-check** — 角色权限矩阵 + 影响面（绑定用户数）

适用场景：
- 用户报错"我点不动按钮" → user-check
- 给新用户分配角色后审视 → user-overview
- 设计/调整角色后回归验收 → role-check

## 二、接口清单

| 方法 | 路径 | 权限码 |
|------|------|--------|
| GET | `/api/admin/permissions/test` | `system:permission-test:run` |

## 三、Mode 1: user-overview

**Query：** `mode=user-overview&userId=<id>`

**Response：**
```json
{
  "code": 200,
  "data": {
    "user": { "id": 2, "username": "operator", "nickname": "Op", "status": 1 },
    "roles": [{ "id": 3, "code": "operator", "name": "运营管理员" }],
    "isSuperAdmin": false,
    "permissionCodes": ["dashboard","tool","tool:menu","tool:list", "..."],
    "menus": [/* MenuNode[] */],
    "stats": {
      "totalCodes": 31,
      "totalMenus": 7,
      "byModule": { "tool": 8, "category": 4, "feedback": 5, "stats": 5, "user": 6, "member": 3 }
    }
  }
}
```

## 四、Mode 2: user-check

**Query 形态 A**（按权限码）：`mode=user-check&userId=<id>&code=<perm-code>`

**Query 形态 B**（按 API）：`mode=user-check&userId=<id>&path=<api-path>&method=<HTTP-method>`

**Response（命中）：**
```json
{
  "code": 200,
  "data": {
    "user": { "id": 1, "username": "admin" },
    "target": {
      "type": "code", "code": "tool:create",
      "permissionExists": true, "permissionId": 56, "permissionName": "创建工具"
    },
    "allowed": true,
    "matchedRoles": [{ "id": 1, "code": "admin", "name": "管理员" }],
    "matchedPermissions": [{ "id": 56, "code": "tool:create", "via": "role:admin" }],
    "denyReason": null
  }
}
```

**Response（拒绝）：** `allowed: false`、`matchedRoles/matchedPermissions: []`、`denyReason` 取下表枚举：

| denyReason | 触发条件 |
|---|---|
| `user_not_found` | userId 对应用户不存在 |
| `user_disabled` | 用户 status≠1 |
| `no_roles` | 用户未绑定任何角色 |
| `roles_disabled` | 所有角色 status≠1 |
| `permission_not_exists` | code/api 在 permissions 表不存在 |
| `permission_disabled` | 权限码 status≠1 |
| `permission_not_granted` | 角色未授予该权限码 |

> **super_admin 短路**：直接返回 `allowed=true`、`matchedPermissions=[{via:'super_admin_short_circuit'}]`。

## 五、Mode 3: role-check

**Query：** `mode=role-check&roleCode=<code>` 或 `&roleId=<id>`

**Response：**
```json
{
  "code": 200,
  "data": {
    "role": { "id": 4, "code": "auditor", "name": "审计员", "status": 1 },
    "ownedCodes": ["dashboard","stats:overview","system:audit-log",...],
    "permissionTree": [/* 仅含 owned 节点的树 */],
    "stats": {
      "total": 24,
      "byModule": { "stats": 5, "system": 8, "tool": 5, "..." : "..." },
      "byType": { "menu": 9, "api": 15 }
    },
    "boundUserCount": 3
  }
}
```

## 六、错误码

| HTTP | code | message |
|---|---|---|
| 422 | 422 | `unknown mode: <xxx>` / `userId required` / `code or (path+method) required` / `roleCode or roleId required` |
| 401 | 401 | 未登录 |
| 403 | 403 | 缺 `system:permission-test:run` 权限 |

## 七、相关文档

- [Spec-A1 设计文档 §5](../../superpowers/specs/2026-05-11-审计基础设施与权限测试设计文档.md#五permission-test-service-详细设计)
```

- [ ] **Step 2: 更新 docs/api/README.md**

在「审计日志」段后追加：

```markdown
## 权限测试 (Permission Test) [查看文档](permission-test/README.md)

| 方法 | 路径 | 认证 |
|------|------|------|
| `GET` | `/api/admin/permissions/test` | ✅ |
```

更新顶部接口总数：从 87 → 88（+1）。

- [ ] **Step 3: 更新 docs/README.md**

在「审计日志」一行下方插入：

```markdown
| [权限测试](api/permission-test/README.md)               | 综合权限调试工具（3 mode）          | 🆕 已发布 |
```

- [ ] **Step 4: Commit**

```bash
git add super-tool-node/docs/api/permission-test/ \
        super-tool-node/docs/api/README.md \
        super-tool-node/docs/README.md
git commit -m "docs(api): 新增 permission-test 接口文档"
```

---

## Task 16: 全量测试 + 14 项手工验收清单

**Spec:** §8.3 手工验收清单

**Files:** 无修改（验收 + 修 bug）

- [ ] **Step 1: 跑全量测试**

```bash
cd super-tool-node && npm test
```

预期：
- 新增 12 case（audit 4 + permission-test 6 + audit-log api 2）全部 PASS
- 现有测试不受影响

如失败：定位失败 case，回到对应 Task 修 bug 后重跑。

- [ ] **Step 2: 启动后端 + 用 admin 账号准备 token**

```bash
cd super-tool-node && npm run dev
# 另起终端：
TOKEN=$(curl -sX POST http://localhost:7001/api/admin/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"<admin-pw>","clientId":"...","clientSecret":"..."}' \
  | python -c "import sys,json; print(json.load(sys.stdin)['data']['accessToken'])")
echo $TOKEN
```

- [ ] **Step 3: 跑 14 项手工验收清单**

按 Spec §8.3 表格逐项执行。**每项失败则回到对应 Task 修复并 `git commit -m "fix(audit): xxx"`**。

| # | 验证 | 命令 | 预期 |
|---|------|------|------|
| 1 | 审计基础链路 | curl PUT /roles/3 修改名字 → mysql 查 audit_logs | status=1 + before/after + cost_time>0 |
| 2 | 失败也写 | curl POST /roles 提交重名 code → mysql 查 | status=0 + failReason 非空 |
| 3 | password 脱敏 | curl POST /users/2/reset-password → mysql 查 request_params | password=`***` 或不存在 |
| 4 | 列表过滤 | curl GET `/audit-logs?module=role&action=update` | 仅 role update 记录 + 无 before/after 字段 |
| 5 | 详情完整 JSON | curl GET `/audit-logs/<id>` | 含 beforeData/afterData |
| 6 | CSV 导出 | curl `/audit-logs/export?module=role` -o test.csv，Excel 打开 | UTF-8 + 中文表头正常 |
| 7 | 导出截断 | （需提前模拟 15000 条数据）curl `/export` | 仅 10000 行 + Header `X-Audit-Truncated:true` |
| 8 | user-overview | curl `?mode=user-overview&userId=2` | 完整 user/roles/codes/menus/stats |
| 9 | user-check 命中 | curl `?mode=user-check&userId=1&code=tool:list` | allowed=true |
| 10 | user-check 拒绝（不存在）| curl `?code=tool:foobar_xxx` | denyReason='permission_not_exists' |
| 11 | path 反查 | curl `?path=/api/admin/tools&method=POST` | 反查到 code='tool:create' |
| 12 | role-check | curl `?mode=role-check&roleCode=auditor` | ownedCodes/permissionTree/boundUserCount |
| 13 | 权限校验 | 用 operator token 调 user-overview | 401/403 |
| 14 | super_admin 短路 | super_admin token 调 `?code=tool:foobar` | allowed=true（不查 permission_not_exists） |

> ⚠️ #7 需要先用 SQL 批量插入测试数据：
> ```sql
> INSERT INTO audit_logs (module, action, status) SELECT 'tool','create',1 FROM
>   (SELECT 1 UNION ALL SELECT 2) a, (SELECT 1 UNION ALL SELECT 2 UNION ALL SELECT 3) b,
>   /* ... 凑出 15000 条 */;
> ```
> 或者临时把 max=10000 改为 max=10 跑测试再改回。

- [ ] **Step 4: 修复手工验收发现的 bug**

如发现 bug，回到对应 Task 修复并 commit（`fix(audit): xxx`）。

- [ ] **Step 5（无修复时跳过）：Commit fix**

```bash
git add <修改文件>
git commit -m "fix(audit): 修复手工验收 #N 发现的 xxx"
```

---

## Task 17: 文档同步 + 收尾整理

**Files:**
- Modify: `super-tool-node/database/README.md`（如有需要补充 audit_logs 使用记录）
- Modify: `super-tool-node/CHANGELOG.md`（如项目有此文件）

- [ ] **Step 1: 检查 docs/superpowers/README.md（如存在）**

如果该目录有索引 README，把本 Spec-A1 的 spec + plan 加入：

```markdown
| [Spec-A1 审计基础设施与权限测试](specs/2026-05-11-审计基础设施与权限测试设计文档.md) | [实施计划](plans/2026-05-11-审计基础设施与权限测试实施计划.md) | ✅ 已完成 |
```

- [ ] **Step 2: 检查 database/README.md 是否需补 audit_logs 使用记录**

```bash
grep -n "audit_logs" super-tool-node/database/README.md
```

如果完全没提，在 README 末尾追加一节：

```markdown
## v2.8 audit_logs 表正式启用（Spec-A1）

`audit_logs` 表自 init.sql 起即定义，但直到 Spec-A1 才在代码层面写入数据。
- Service: `service.audit.log({...})`
- Model: `app/model/audit_log.ts`
- Controller: `app/controller/admin/audit-log.ts`
- API: `GET /api/admin/audit-logs[*]`

详见 [Spec-A1 设计文档](../docs/superpowers/specs/2026-05-11-审计基础设施与权限测试设计文档.md)。
```

- [ ] **Step 3: 跑一次 commit 数量与改动统计**

```bash
git log --oneline master..HEAD | wc -l
# 预期：~16-18 个 commit

git diff --stat master..HEAD | tail -5
# 预期：~17 文件 +1500/-? 行
```

- [ ] **Step 4: Commit（如有文档改动）**

```bash
git add super-tool-node/database/README.md super-tool-node/docs/superpowers/README.md
git commit -m "docs: 同步 Spec-A1 完成状态"
```

---

## Task 18: 合并到 master（finishing-a-development-branch）

**前提：** Task 16 的 14 项手工验收全部通过；全量测试 npm test 12 个新 case 全部 PASS。

- [ ] **Step 1: 跑最后一次全量测试**

```bash
cd super-tool-node && npm test
```

确认 0 新失败（master 预先存在的 failure 不算）。

- [ ] **Step 2: 切回主仓库 master 合并**

```bash
cd d:/Donough/Projects/super-tools
git status   # 主仓库应在 master，working tree clean
git merge --ff-only feat/spec-a1-audit
git log --oneline -5
```

如果不能 fast-forward（master 有新 commit），改为 rebase or merge commit（按团队规范）。

- [ ] **Step 3: 合并后再跑测试**

```bash
cd super-tool-node && npm test
```

预期：12 个新 case + 现有测试都 PASS。

- [ ] **Step 4: 清理 worktree + 本地分支**

```bash
cd d:/Donough/Projects/super-tools

# 移除 junction（避免 worktree remove 失败）
cmd /c "rmdir .worktrees\spec-a1-audit\super-tool-node\node_modules"

git worktree remove .worktrees/spec-a1-audit
git branch -d feat/spec-a1-audit
git worktree list
git branch
```

> 如果 Windows 文件锁阻止 worktree remove，关闭 IDE 后手动 `Remove-Item .worktrees -Recurse -Force`。

- [ ] **Step 5: 最终汇报**

```bash
git log master --oneline | head -20
```

输出本 Spec-A1 引入的 ~17 commit；准备进入 Spec-A2（feedback + stats）的 brainstorming。

---

## 实施完成检查

完成全部 18 个 Task 后：

- [ ] **检查 1：commit 数量**

```bash
git log master..feat/spec-a1-audit --oneline | wc -l
```

预期：16-18 个 commit。

- [ ] **检查 2：全量测试通过**

```bash
cd super-tool-node && npm test
```

预期：12 个新 case 全部 PASS + 现有测试不受影响。

- [ ] **检查 3：DB 数据完整性**

```sql
-- audit_logs 已有真实数据
SELECT COUNT(*) FROM audit_logs;
-- 预期：> 0（手工验收过程产生的记录）

-- 模块覆盖
SELECT module, action, COUNT(*) FROM audit_logs GROUP BY module, action;
-- 预期：至少 role/permission/tool/category/user/member 6 个模块都有记录

-- 失败记录也有
SELECT COUNT(*) FROM audit_logs WHERE status = 0;
-- 预期：>= 1（手工验收 #2 验证过）
```

- [ ] **检查 4：手工验收 14 项全过**（Task 16 Step 3）

任何未通过项必须先修复再视为 Spec-A1 完成。

- [ ] **检查 5：服务正常运行**

```bash
npm run dev
```

后端启动 0 报错，所有路由正确注册。

---

## Spec-A1 收尾（与 Spec-A2 / Spec-C 衔接）

完成本 plan 后：

1. **Spec-A1 已合并到 master** — 全栈审计能力 + 权限测试工具就绪
2. **进入 Spec-A2**（feedback + stats）的 brainstorming：
   - feedback controller 的 `reply` / `delete` 等写操作可直接复用 `service.audit.log({ module:'feedback', action:'reply' })`
   - stats 各 GET 接口不挂审计
3. **Spec-C 管理端**：
   - 「审计日志页」直接消费 `GET /api/admin/audit-logs*` 4 接口
   - 「权限测试页」直接消费 `GET /api/admin/permissions/test`，3 个 mode 用 Tab 切换

---

**END of Plan**
