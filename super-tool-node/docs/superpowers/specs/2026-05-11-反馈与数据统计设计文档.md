# Spec-A2 · 反馈与数据统计 设计文档

> 创建：2026-05-11 · 版本 v1.0 · 状态：✅ 设计完成
> 上游：[Spec-A 整体规划](2026-05-11-审计基础设施与权限测试设计文档.md#一背景与目标)（已拆 A1 + A2，A1 已完成）
> 下游：Spec-C 管理端业务页 / Spec-D（C 端反馈入口）

---

## 一、背景与目标

### 1.1 上下文

`Spec-A1（审计基础设施与权限测试）`已合并至 master，提供了：
- `service.audit.log({...})` 通用审计写入能力
- `audit_logs` 表已挂 22 个高敏感写操作
- `service.permission.testForUser/checkUserAccess/testForRole` 调试工具
- 流式 CSV 导出范式（`PassThrough` + UTF-8 BOM）

本 Spec-A2 在此基础上补齐 **Spec-A 的剩余两块业务后端**：

1. **反馈系统**：建表 + C 端提交 + 管理端 5 接口（list/detail/reply/update/delete）
2. **数据统计**：5 个聚合接口（overview / tool-usage / user-active / trend / export）

完成后 `Spec-A` 整体闭环（C 端反馈 + 后台运营所需后端能力齐备），Spec-C 管理端 9 业务页前端可直接消费。

### 1.2 目标

| 模块 | 目标 |
|---|---|
| **feedbacks** | 用户能匿名/登录提交反馈；管理员能查看、按状态筛选、回复、变更状态、软删；所有写操作走 audit |
| **stats** | 5 个 SQL 聚合接口提供 dashboard 扩展指标、工具使用 TOP N、DAU/WAU/MAU、4×3 趋势矩阵、CSV 导出 |
| **复用 A1** | 不重新发明 audit / CSV 框架；不引入新中间件；不引入新 service 抽象 |

### 1.3 非目标（YAGNI）

| 不做的事 | 原因 |
|---|---|
| feedback 附件上传 | 当前无文件上传服务；附件字段沦为装饰 |
| feedback 优先级 / 标签 | 当前管理员不会用，UI 暂不支持 |
| feedback 通知用户（邮件/站内信）| 通知服务本身需要单独 spec（SMTP/短信网关）|
| stats 同比 / 环比 | Spec-C 暂不展示 |
| stats 按角色 / 等级切片 | 同上 |
| stats 预聚合（stats_daily 表）| 数据量级 <100 万实时聚合够用；过早优化 |
| 工具使用专属表（tool_usage_logs）| api_logs 已包含同等信息，重复存储无价值 |
| 登录用户限流差异化 | middleware 改动超出 scope；统一 10/h 已够用 |

---

## 二、决策快照

| # | 决策 | 选择 |
|---|---|---|
| Q1 | Spec-A2 总体 scope | **A：最小闭环** — feedbacks 表 + C 端 + 管理端 4 接口 + stats 5 实时聚合 |
| Q2 | feedbacks 表字段 | **B：标准** — type/contact/platform/reply_*/软删 |
| Q3 | C 端鉴权 | **B：可登录可不登录 + IP 限流** |
| Q4 | reply 流程 | **B：严格状态机 0/1→2，2/3 时 409** |
| Q4.1 | status 变更实现 | **b：009 SQL 新增 `feedback:update` + 开 PUT 接口** |
| Q5 | stats 聚合口径 | **B：标准** — overview 8 字段 / tool-usage TOP N / DAU-WAU-MAU / trend 4×3 / export 3 类 |
| Q6 | C 端 controller 实现 | **B：手动解析 token + `rateLimit({ max:10, window:3600 })`** |
| Q7 | 测试覆盖 | **A：最小集** — feedback 7 case + stats 5 case = 12 case |

---

## 三、整体架构

### 3.1 交付物清单（13 文件）

```
super-tool-node/
├── database/
│   └── 009_add_feedback_module.sql         ← 新增（建表 + 权限码 + 角色授权）
├── app/model/
│   └── feedback.ts                         ← 新增
├── app/service/
│   ├── feedback.ts                         ← 新增（create/list/detail/reply/update/destroy）
│   └── stats.ts                            ← 新增（overview/getToolUsage/getUserActive/getTrend/exportCsv）
├── app/controller/
│   ├── feedback.ts                         ← 新增（C 端 POST /api/feedback）
│   └── admin/
│       ├── feedback.ts                     ← 新增（5 方法）
│       └── stats.ts                        ← 新增（5 方法）
├── app/router.ts                           ← 修改（注册 11 条路由）
├── docs/api/feedback/README.md             ← 新增
├── docs/api/stats/README.md                ← 新增
├── docs/api/README.md                      ← 修改（接口 88 → 99）
├── docs/README.md                          ← 修改
├── test/api/feedback.test.ts               ← 新增（7 case）
└── test/api/stats.test.ts                  ← 新增（5 case）
```

### 3.2 数据流 · feedback reply 审计场景

```
┌─ 管理员点"回复" ─┐
│  AuthButton 校验 feedback:reply │
└────────┬───────────────────────┘
         │ POST /api/admin/feedbacks/123/reply { replyContent }
         ▼
┌─────────────────────────────────────────────────┐
│ admin/feedback.ts  reply()                      │
│  1) before = await service.feedback.detail(id)  │ ← before snapshot
│  2) try {                                       │
│       updated = await service.feedback.reply(   │
│         id, replyContent, replyUserId)          │
│       await service.audit.log({                 │ ← 复用 Spec-A1
│         module:'feedback', action:'reply',      │
│         bizType:'feedback', bizId:id,           │
│         beforeData: before, afterData: updated, │
│         status:1 })                             │
│       this.success(updated)                     │
│     } catch (e) {                               │
│       // 失败路径同 Spec-A1 模板                  │
│     }                                           │
└─────────────────────────────────────────────────┘
         │
         ▼ service 内部状态机校验
┌─────────────────────────────────────────────────┐
│ service/feedback.ts  reply()                    │
│   if (status NOT IN {0,1}) throw 409            │
│   UPDATE feedbacks SET status=2,                │
│     reply_content=?, reply_user_id=?,           │
│     replied_at=NOW()                            │
│     WHERE id=?                                  │
└─────────────────────────────────────────────────┘
```

### 3.3 数据流 · stats tool-usage 聚合

```
GET /api/admin/stats/tool-usage?startTime=&endTime=&limit=20
  ▼
service.stats.getToolUsage(query)
  ▼
SELECT SUBSTRING_INDEX(SUBSTRING_INDEX(path, '/', 4), '/', -1) AS tool_code,
       COUNT(*) AS count
FROM api_logs
WHERE path LIKE '/api/tools/%/access'
  AND created_at BETWEEN :start AND :end
GROUP BY tool_code
ORDER BY count DESC
LIMIT :limit
  ▼
SELECT code, name FROM tools WHERE code IN (...)   ← 名称回填
  ▼
返回 [{ toolCode, toolName, count }]
```

### 3.4 模块边界

| 单元 | 职责 | 关键依赖 |
|---|---|---|
| `model/feedback.ts` | Sequelize model（**paranoid=true** 软删） | egg/sequelize |
| `service/feedback.ts::create(payload)` | C 端/管理端共用 | model |
| `service/feedback.ts::list(query)` | 分页 + 7 维过滤 + JOIN user | model + User |
| `service/feedback.ts::detail(id)` | 含 user + replier JOIN | model + User |
| `service/feedback.ts::reply(id, content, userId)` | **状态机 0/1→2**；其他状态抛 409 | model |
| `service/feedback.ts::update(id, payload)` | status 变更（含 transition 白名单）| model |
| `service/feedback.ts::destroy(id)` | 软删（paranoid 自动）| model |
| `service/stats.ts::overview()` | 8 字段聚合 | User/LoginLog/UserSession/Tool/Feedback |
| `service/stats.ts::getToolUsage(q)` | api_logs 聚合 + tools 名称回填 | raw SQL + Tool |
| `service/stats.ts::getUserActive(q)` | DAU/WAU/MAU + 新增用户趋势 | LoginLog/User |
| `service/stats.ts::getTrend(q)` | 4 metric × 3 granularity；switch 路由表 | 多表 raw SQL |
| `service/stats.ts::exportCsv(type, q)` | 复用 A1 PassThrough 模板 | 自身其他方法 |
| `controller/feedback.ts` | C 端 create（手动解析 token）| service/feedback + jwt |
| `controller/admin/feedback.ts` | 5 方法；reply/update/destroy 挂 audit | service/feedback + service/audit |
| `controller/admin/stats.ts` | 5 方法；全 GET 不挂 audit | service/stats |

### 3.5 关键不变量

1. **`service.feedback.reply` 内部必须做状态机检查**：controller 检查可被绕过（直调 service），service 层再加一道
2. **所有 list 自动过滤 `deleted_at IS NULL`**（通过 paranoid，**禁止手动加** WHERE）
3. **stats 时间范围有默认值**：startTime 默认 -30 天，endTime 默认 now；防止全表扫
4. **stats trend granularity 仅 3 枚举**：`day/week/month`；其他 422
5. **stats export 复用 A1 PassThrough**：不抽公共 CsvWriter（YAGNI）
6. **reply/update/destroy audit 必含 beforeData**：审计依赖"前→后"语义
7. **C 端 create 不挂 audit**：审计只针对管理端写操作（沿用 A1 §7.4 #5）

### 3.6 009 SQL 概要

```sql
-- 009_add_feedback_module.sql

-- 1. 建表
CREATE TABLE IF NOT EXISTS `feedbacks` (
  `id`             BIGINT UNSIGNED   NOT NULL AUTO_INCREMENT,
  `user_id`        BIGINT UNSIGNED   DEFAULT NULL COMMENT '提交用户(匿名为 NULL)',
  `type`           VARCHAR(20)       NOT NULL DEFAULT 'other'
                   COMMENT '分类: bug/suggestion/praise/other',
  `content`        TEXT              NOT NULL COMMENT '反馈内容',
  `contact`        VARCHAR(100)      DEFAULT NULL COMMENT '联系方式',
  `platform`       VARCHAR(30)       DEFAULT NULL COMMENT '来源',
  `ip`             VARCHAR(50)       DEFAULT NULL,
  `user_agent`     VARCHAR(500)      DEFAULT NULL,
  `status`         TINYINT UNSIGNED  NOT NULL DEFAULT 0
                   COMMENT '0待处理 1处理中 2已回复 3已关闭',
  `reply_content`  TEXT              DEFAULT NULL,
  `reply_user_id`  BIGINT UNSIGNED   DEFAULT NULL,
  `replied_at`     DATETIME          DEFAULT NULL,
  `created_at`     DATETIME          NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at`     DATETIME          NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted_at`     DATETIME          DEFAULT NULL,
  PRIMARY KEY (`id`),
  INDEX `idx_user_id`    (`user_id`),
  INDEX `idx_type`       (`type`),
  INDEX `idx_status`     (`status`),
  INDEX `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户反馈表';

-- 2. 新增 feedback:update 权限码
INSERT INTO `permissions` (code, name, type, module, platform, path, method, parent_id, sort)
SELECT 'feedback:update', '更新反馈状态', 4, 'feedback', 'admin',
       '/api/admin/feedbacks/:id', 'PUT',
       (SELECT id FROM (SELECT id FROM `permissions` WHERE code = 'feedback') t), 15
WHERE NOT EXISTS (SELECT 1 FROM permissions WHERE code = 'feedback:update');

-- 3. 给 admin/operator 角色授权
INSERT INTO `role_permissions` (role_id, permission_id)
SELECT r.id, p.id
FROM roles r, permissions p
WHERE r.code IN ('admin','operator') AND p.code='feedback:update'
  AND NOT EXISTS (
    SELECT 1 FROM role_permissions rp
    WHERE rp.role_id = r.id AND rp.permission_id = p.id
  );
```

> super_admin 用短路逻辑无需显式授予；auditor 只读不需要。

---

## 四、Service · feedback 详细设计

### 4.1 Model（paranoid 软删）

```typescript
// app/model/feedback.ts
import { Application } from 'egg';
import { DataTypes } from 'sequelize';

export interface FeedbackAttributes {
  id: number;
  userId: number | null;
  type: 'bug' | 'suggestion' | 'praise' | 'other';
  content: string;
  contact: string | null;
  platform: string | null;
  ip: string | null;
  userAgent: string | null;
  status: 0 | 1 | 2 | 3;
  replyContent: string | null;
  replyUserId: number | null;
  repliedAt: Date | null;
  createdAt?: Date;
  updatedAt?: Date;
  deletedAt?: Date | null;
}

export default (app: Application) => {
  const { STRING, TEXT, BIGINT, TINYINT, DATE } = DataTypes;
  const Feedback = app.model.define('Feedback', {
    id:           { type: BIGINT.UNSIGNED, autoIncrement: true, primaryKey: true },
    userId:       { type: BIGINT.UNSIGNED, allowNull: true,  field: 'user_id' },
    type:         { type: STRING(20), allowNull: false, defaultValue: 'other' },
    content:      { type: TEXT, allowNull: false },
    contact:      { type: STRING(100), allowNull: true },
    platform:     { type: STRING(30), allowNull: true },
    ip:           { type: STRING(50), allowNull: true },
    userAgent:    { type: STRING(500), allowNull: true,  field: 'user_agent' },
    status:       { type: TINYINT.UNSIGNED, allowNull: false, defaultValue: 0 },
    replyContent: { type: TEXT, allowNull: true,  field: 'reply_content' },
    replyUserId:  { type: BIGINT.UNSIGNED, allowNull: true,  field: 'reply_user_id' },
    repliedAt:    { type: DATE, allowNull: true,  field: 'replied_at' },
    deletedAt:    { type: DATE, allowNull: true,  field: 'deleted_at' },
  }, {
    tableName: 'feedbacks',
    timestamps: true,
    underscored: true,
    paranoid: true,    // 自动 deleted_at IS NULL 过滤；destroy() 自动软删
  });

  (Feedback as any).associate = () => {
    Feedback.belongsTo(app.model.User, { foreignKey: 'user_id', as: 'user' });
    Feedback.belongsTo(app.model.User, { foreignKey: 'reply_user_id', as: 'replier' });
  };

  return Feedback;
};
```

### 4.2 service/feedback.ts 完整接口

```typescript
import { Service } from 'egg';
import { Op } from 'sequelize';

export interface FeedbackCreatePayload {
  userId?: number | null;
  type: 'bug' | 'suggestion' | 'praise' | 'other';
  content: string;
  contact?: string | null;
  platform?: string | null;
  ip?: string | null;
  userAgent?: string | null;
}

export interface FeedbackListQuery {
  page?: number;
  pageSize?: number;
  type?: string;
  status?: 0 | 1 | 2 | 3;
  platform?: string;
  userId?: number;
  keyword?: string;
  startTime?: string;
  endTime?: string;
}

export interface FeedbackUpdatePayload {
  status?: 0 | 1 | 2 | 3;
}

const VALID_STATUS = new Set([0, 1, 2, 3]);

export default class FeedbackService extends Service {
  async create(payload: FeedbackCreatePayload) {
    return this.ctx.model.Feedback.create({
      userId: payload.userId ?? null,
      type: payload.type,
      content: payload.content,
      contact: payload.contact ?? null,
      platform: payload.platform ?? null,
      ip: payload.ip ?? null,
      userAgent: payload.userAgent ?? null,
      status: 0,
    });
  }

  async list(q: FeedbackListQuery) {
    const where: any = {};
    if (q.type) where.type = q.type;
    if (q.status !== undefined) where.status = q.status;
    if (q.platform) where.platform = q.platform;
    if (q.userId) where.userId = q.userId;
    if (q.keyword) where.content = { [Op.like]: `%${q.keyword}%` };
    if (q.startTime || q.endTime) {
      where.createdAt = {};
      if (q.startTime) where.createdAt[Op.gte] = new Date(q.startTime);
      if (q.endTime)   where.createdAt[Op.lte] = new Date(q.endTime);
    }

    const page = Math.max(1, q.page || 1);
    const pageSize = Math.min(100, Math.max(1, q.pageSize || 20));

    const { count, rows } = await this.ctx.model.Feedback.findAndCountAll({
      where,
      include: [{
        association: 'user',
        attributes: ['id', 'username', 'nickname'],
        required: false,
      }],
      order: [['id', 'DESC']],
      offset: (page - 1) * pageSize,
      limit: pageSize,
    });
    return { total: count, page, pageSize, rows };
  }

  async detail(id: number) {
    return this.ctx.model.Feedback.findByPk(id, {
      include: [
        { association: 'user',    attributes: ['id', 'username', 'nickname'], required: false },
        { association: 'replier', attributes: ['id', 'username', 'nickname'], required: false },
      ],
    });
  }

  /** 严格状态机 0/1→2 */
  async reply(id: number, replyContent: string, replyUserId: number) {
    const fb = await this.ctx.model.Feedback.findByPk(id);
    if (!fb) this.ctx.throw(404, 'feedback not found');

    const status = (fb as any).status;
    if (status !== 0 && status !== 1) {
      this.ctx.throw(409,
        `反馈当前状态不允许回复（status=${status}），请先重新打开`);
    }

    await fb.update({
      status: 2,
      replyContent,
      replyUserId,
      repliedAt: new Date(),
    });
    return fb;
  }

  async update(id: number, payload: FeedbackUpdatePayload) {
    const fb = await this.ctx.model.Feedback.findByPk(id);
    if (!fb) this.ctx.throw(404, 'feedback not found');

    if (payload.status !== undefined) {
      if (!VALID_STATUS.has(payload.status)) {
        this.ctx.throw(422, `invalid status: ${payload.status}`);
      }
      const allowed = this._isStatusTransitionAllowed(
        (fb as any).status, payload.status,
      );
      if (!allowed) {
        this.ctx.throw(422,
          `不允许的状态转移：${(fb as any).status} → ${payload.status}`);
      }
      await fb.update({ status: payload.status });
    }
    return fb;
  }

  /** 状态转移白名单 */
  private _isStatusTransitionAllowed(from: number, to: number): boolean {
    if (from === to) return true;   // 幂等
    const TRANSITIONS: Record<number, number[]> = {
      0: [1, 3],     // 待处理 → 处理中 / 直接关闭
      1: [0, 3],     // 处理中 → 取消 / 关闭（→2 由 reply 独占）
      2: [1],        // 已回复 → 重新打开（处理中）
      3: [1],        // 已关闭 → 重新打开（处理中）
    };
    return (TRANSITIONS[from] || []).includes(to);
  }

  async destroy(id: number) {
    const fb = await this.ctx.model.Feedback.findByPk(id);
    if (!fb) this.ctx.throw(404, 'feedback not found');
    await fb.destroy();   // paranoid 自动软删
  }
}
```

### 4.3 状态机完整转移图

```
           ┌── reply (独占) ──┐
           │                  ▼
  ┌────┐     ┌────┐     ┌────┐     ┌────┐
  │ 0  │───→│ 1  │───→│ 2  │───→│ 3  │
  └────┘     └────┘     └────┘     └────┘
  待处理      处理中      已回复      已关闭
    │          │            │          │
    │          │            │          │
    │          └─┐  ┌───────┘          │
    │            ▼  ▼                   │
    │        重新打开 (status=1)         │
    │            │                       │
    └─→ 3 (直接关闭) ──────────────────→
```

**Allowed transitions**：
- `0 → 0/1/3`（幂等 / 开始处理 / 直接关闭）
- `1 → 0/1/3`（取消 / 幂等 / 关闭）
- `0/1 → 2` **仅通过 reply()**
- `2 → 1/2`（重新打开 / 幂等）
- `3 → 1/3`（重新打开 / 幂等）

**禁止 transitions**：`2→0`、`2→3`、`3→0`、`3→2`（避免跳过"重新打开"语义）

### 4.4 错误处理矩阵

| 场景 | 方法 | 行为 |
|---|---|---|
| id 不存在 | reply/update/destroy | 404 |
| 已是 2/3 调 reply | reply | 409 |
| status 非法值 | update | 422 |
| 禁止 transition | update | 422 |
| type 非四枚举 | create（controller validate）| 422 |
| content 过短/过长 | create（controller validate）| 422 |
| 未登录 + contact 空 | create（C 端 controller）| 422 |
| DB 失败 | 所有 | 冒泡 500 |

### 4.5 audit 联动（controller 层）

```typescript
async reply() {
  const id = Number(this.ctx.params.id);
  this.validate({ replyContent: { type: 'string', min: 1, max: 2000 } });
  const { replyContent } = this.ctx.request.body as any;
  const replyUserId = (this.ctx.state as any).user.id;

  let beforeData: any = null;
  try { beforeData = await this.service.feedback.detail(id); } catch {}

  try {
    const updated = await this.service.feedback.reply(id, replyContent, replyUserId);
    await this.service.audit.log({
      module: 'feedback', action: 'reply',
      bizType: 'feedback', bizId: id,
      beforeData, afterData: updated,
      description: `回复反馈 #${id}`, status: 1,
    });
    this.success(updated, '回复成功');
  } catch (e: any) {
    await this.service.audit.log({
      module: 'feedback', action: 'reply',
      bizType: 'feedback', bizId: id,
      beforeData,
      description: `尝试回复反馈 #${id}`,
      status: 0, failReason: e.message,
    });
    throw e;
  }
}
```

### 4.6 audit action 词表扩展（v1.1）

Spec-A1 §7.4 不变量 #5 锁定的词表：
`create / update / delete / batch_update / reset_password / adjust_points / export / login / logout`

Spec-A2 新增：
- **`reply`** — 回复反馈（feedback 模块独占动作）

新词表 v1.1（A1 + A2）：
`create / update / delete / batch_update / reset_password / adjust_points / reply / export / login / logout`

---

## 五、Service · stats 详细设计

### 5.1 完整接口

```typescript
import { Service } from 'egg';
import { Op } from 'sequelize';

export type TrendMetric = 'user-register' | 'user-login' | 'feedback-submit' | 'tool-access';
export type Granularity = 'day' | 'week' | 'month';

export interface TrendQuery {
  metric: TrendMetric;
  granularity?: Granularity;
  startTime?: string;
  endTime?: string;
}

export interface ToolUsageQuery {
  startTime?: string;
  endTime?: string;
  limit?: number;
}

export interface UserActiveQuery {
  startTime?: string;
  endTime?: string;
}

export type ExportType = 'tool-usage' | 'user-active' | 'trend';

const VALID_METRICS = new Set<TrendMetric>([
  'user-register', 'user-login', 'feedback-submit', 'tool-access',
]);
const VALID_GRANULARITIES = new Set<Granularity>(['day', 'week', 'month']);
const VALID_EXPORT_TYPES = new Set<ExportType>(['tool-usage', 'user-active', 'trend']);

export default class StatsService extends Service {
  async overview() { /* 见 5.2 */ }
  async getToolUsage(q: ToolUsageQuery) { /* 见 5.3 */ }
  async getUserActive(q: UserActiveQuery) { /* 见 5.4 */ }
  async getTrend(q: TrendQuery) { /* 见 5.5 */ }
  async exportCsv(type: ExportType, q: any) { /* 见 5.6 */ }

  private _parseTimeRange(q: { startTime?: string; endTime?: string }) {
    const now = new Date();
    const defaultStart = new Date(now.getTime() - 30 * 86400000);
    const startTime = q.startTime ? new Date(q.startTime) : defaultStart;
    const endTime = q.endTime ? new Date(q.endTime) : now;
    return { startTime, endTime };
  }

  /** granularity → MySQL DATE_FORMAT 模式（白名单 switch，避免字符串注入） */
  private _bucketFormat(g: Granularity): string {
    switch (g) {
      case 'day':   return '%Y-%m-%d';
      case 'week':  return '%x-W%v';   // ISO 周
      case 'month': return '%Y-%m';
    }
  }

  private async _countActiveUsers(sinceDate: Date): Promise<number> {
    const sql = `
      SELECT COUNT(DISTINCT user_id) AS cnt
      FROM login_logs
      WHERE status = 1 AND created_at >= :sinceDate
    `;
    const rows: any[] = (await (this.app as any).model.query(sql, {
      replacements: { sinceDate },
      type: (this.app as any).Sequelize.QueryTypes.SELECT,
    })) as any[];
    return Number(rows[0]?.cnt || 0);
  }
}
```

### 5.2 `overview()` — 8 字段聚合

```typescript
async overview() {
  const todayStart = new Date(); todayStart.setHours(0, 0, 0, 0);
  const sevenDaysAgo = new Date(Date.now() - 7 * 86400000);

  const [
    userCount, activeUserCount, todayLoginCount, activeSessionCount,
    toolCount, feedbackCount, pendingFeedbackCount, todayNewUserCount,
  ] = await Promise.all([
    this.ctx.model.User.count(),
    this._countActiveUsers(sevenDaysAgo),
    this.ctx.model.LoginLog.count({
      where: { status: 1, created_at: { [Op.gte]: todayStart } },
    }),
    this.ctx.model.UserSession.count({ where: { isActive: 1 } }),
    this.ctx.model.Tool.count({ where: { status: 1 } }),
    this.ctx.model.Feedback.count(),
    this.ctx.model.Feedback.count({ where: { status: 0 } }),
    this.ctx.model.User.count({
      where: { created_at: { [Op.gte]: todayStart } },
    }),
  ]);

  return {
    userCount, activeUserCount, todayLoginCount, activeSessionCount,
    toolCount, feedbackCount, pendingFeedbackCount, todayNewUserCount,
  };
}
```

### 5.3 `getToolUsage()` — TOP N

```typescript
async getToolUsage(q: ToolUsageQuery) {
  const { startTime, endTime } = this._parseTimeRange(q);
  const limit = Math.min(100, Math.max(1, q.limit || 20));

  const sql = `
    SELECT SUBSTRING_INDEX(SUBSTRING_INDEX(path, '/', 4), '/', -1) AS tool_code,
           COUNT(*) AS count
    FROM api_logs
    WHERE path LIKE '/api/tools/%/access'
      AND created_at BETWEEN :start AND :end
    GROUP BY tool_code
    ORDER BY count DESC
    LIMIT :limit
  `;
  const rows: any[] = (await (this.app as any).model.query(sql, {
    replacements: { start: startTime, end: endTime, limit },
    type: (this.app as any).Sequelize.QueryTypes.SELECT,
  })) as any[];

  // 名称回填（避免 N+1）
  const codes = rows.map((r: any) => r.tool_code).filter(Boolean);
  const tools = codes.length > 0
    ? await this.ctx.model.Tool.findAll({
        where: { code: codes }, attributes: ['code', 'name'],
      })
    : [];
  const nameMap = new Map((tools as any[]).map(t => [t.code, t.name]));

  return rows.map((r: any) => ({
    toolCode: r.tool_code,
    toolName: nameMap.get(r.tool_code) || '(已删除/未知)',
    count: Number(r.count),
  }));
}
```

### 5.4 `getUserActive()` — DAU/WAU/MAU + 新增用户趋势

```typescript
async getUserActive(q: UserActiveQuery) {
  const { startTime, endTime } = this._parseTimeRange(q);
  const now = new Date();
  const dayAgo   = new Date(now.getTime() - 1  * 86400000);
  const weekAgo  = new Date(now.getTime() - 7  * 86400000);
  const monthAgo = new Date(now.getTime() - 30 * 86400000);

  const [dau, wau, mau, newUserPoints] = await Promise.all([
    this._countActiveUsers(dayAgo),
    this._countActiveUsers(weekAgo),
    this._countActiveUsers(monthAgo),
    this._getNewUserTrend(startTime, endTime),
  ]);

  return { dau, wau, mau, newUserTrend: newUserPoints };
}

private async _getNewUserTrend(start: Date, end: Date) {
  const sql = `
    SELECT DATE_FORMAT(created_at, '%Y-%m-%d') AS bucket,
           COUNT(*) AS count
    FROM users
    WHERE created_at BETWEEN :start AND :end
    GROUP BY bucket
    ORDER BY bucket ASC
  `;
  const rows: any[] = (await (this.app as any).model.query(sql, {
    replacements: { start, end },
    type: (this.app as any).Sequelize.QueryTypes.SELECT,
  })) as any[];
  return rows.map((r: any) => ({ date: r.bucket, count: Number(r.count) }));
}
```

### 5.5 `getTrend()` — 4 metric × 3 granularity

```typescript
async getTrend(q: TrendQuery) {
  const { metric, granularity = 'day' } = q;
  const { startTime, endTime } = this._parseTimeRange(q);

  if (!VALID_METRICS.has(metric)) {
    this.ctx.throw(422, `invalid metric: ${metric}`);
  }
  if (!VALID_GRANULARITIES.has(granularity)) {
    this.ctx.throw(422, `invalid granularity: ${granularity}`);
  }

  const fmt = this._bucketFormat(granularity);   // 白名单 switch，无注入风险
  const config = this._trendSqlConfig(metric);

  // ⚠️ fmt 来自白名单常量，不来自用户输入；可以安全地拼接到 SQL
  const sql = `
    SELECT DATE_FORMAT(${config.timeColumn}, '${fmt}') AS bucket,
           COUNT(*) AS count
    FROM ${config.table}
    WHERE ${config.timeColumn} BETWEEN :start AND :end
      ${config.extraWhere || ''}
    GROUP BY bucket
    ORDER BY bucket ASC
  `;
  const rows: any[] = (await (this.app as any).model.query(sql, {
    replacements: { start: startTime, end: endTime },
    type: (this.app as any).Sequelize.QueryTypes.SELECT,
  })) as any[];

  return {
    metric, granularity,
    points: rows.map((r: any) => ({ date: r.bucket, count: Number(r.count) })),
  };
}

private _trendSqlConfig(metric: TrendMetric) {
  switch (metric) {
    case 'user-register':
      return { table: 'users',      timeColumn: 'created_at' };
    case 'user-login':
      return { table: 'login_logs', timeColumn: 'created_at',
               extraWhere: 'AND status = 1' };
    case 'feedback-submit':
      return { table: 'feedbacks',  timeColumn: 'created_at',
               extraWhere: 'AND deleted_at IS NULL' };
    case 'tool-access':
      return { table: 'api_logs',   timeColumn: 'created_at',
               extraWhere: "AND path LIKE '/api/tools/%/access'" };
  }
}
```

> **关于 `fmt` 字符串注入**：`_bucketFormat()` 是白名单 switch，仅返回 3 个固定常量字符串，不来自用户输入；直接拼接到 SQL 是安全的。但 `metric` 和 `table` 也都来自白名单，同理安全。**replacements 不能传 DATE_FORMAT 第二参数**（sequelize 会双 quote），所以采用拼接是必然选择。

> **零值不补**：缺失日期不返回 row；前端用 ECharts `xAxis.type='time'` 自动处理空隙。

### 5.6 `exportCsv()` — 复用 A1 PassThrough 模板

```typescript
async exportCsv(type: ExportType, q: any) {
  const { ctx } = this;
  if (!VALID_EXPORT_TYPES.has(type)) {
    ctx.throw(422, `invalid export type: ${type}`);
  }

  ctx.set('Content-Type', 'text/csv; charset=utf-8');
  ctx.set('Content-Disposition',
    `attachment; filename="stats-${type}-${Date.now()}.csv"`);

  // eslint-disable-next-line @typescript-eslint/no-var-requires
  const { PassThrough } = require('stream');
  const stream = new PassThrough();
  ctx.body = stream;
  stream.write('\uFEFF');

  if (type === 'tool-usage') {
    await this._exportToolUsage(stream, q);
  } else if (type === 'user-active') {
    await this._exportUserActive(stream, q);
  } else {
    await this._exportTrend(stream, q);
  }
  stream.end();
}

private async _exportToolUsage(stream: any, q: any) {
  stream.write(['工具编码', '工具名称', '使用次数'].join(',') + '\n');
  const data = await this.getToolUsage({ ...q, limit: 10000 });
  for (const row of data) {
    stream.write([row.toolCode, row.toolName, row.count]
      .map(v => this._csvField(v)).join(',') + '\n');
  }
}

private async _exportUserActive(stream: any, q: any) {
  // 多段格式：[总览段] + [新增用户趋势段]
  const data = await this.getUserActive(q);
  stream.write('指标,值\n');
  stream.write(`DAU,${data.dau}\nWAU,${data.wau}\nMAU,${data.mau}\n\n`);
  stream.write('日期,新增数\n');
  for (const p of data.newUserTrend) {
    stream.write([p.date, p.count].map(v => this._csvField(v)).join(',') + '\n');
  }
}

private async _exportTrend(stream: any, q: any) {
  const data = await this.getTrend(q);
  stream.write(`指标,${data.metric}\n粒度,${data.granularity}\n\n`);
  stream.write('日期,数量\n');
  for (const p of data.points) {
    stream.write([p.date, p.count].map(v => this._csvField(v)).join(',') + '\n');
  }
}

private _csvField(v: any): string {
  if (v == null) return '';
  const s = String(v);
  if (/[",\n\r]/.test(s)) return `"${s.replace(/"/g, '""')}"`;
  return s;
}
```

### 5.7 SQL 索引覆盖检查

| SQL | 索引命中 | 说明 |
|---|---|---|
| `User.count()` | 行数缓存 | OK |
| `_countActiveUsers` | `login_logs.idx_created_at` | OK |
| `LoginLog.count(today)` | `idx_created_at` + `idx_status` | OK |
| `Tool.count(status=1)` | tools 表小（<1000）扫描可接受 | OK |
| `getToolUsage` | `api_logs.idx_path(prefix)` + `idx_created_at` | OK，前缀匹配部分命中 |
| `_getNewUserTrend` | `users.idx_created_at`（init.sql line 54）| ✅ |
| `getTrend` | 各表 created_at 索引：login_logs/feedbacks/users/api_logs 全部已建 | ✅ |

**结论**：009 SQL **无需追加额外索引**。

### 5.8 错误处理矩阵

| 场景 | 行为 |
|---|---|
| metric/granularity/type 不在枚举 | 422 |
| endTime < startTime | 返回空数组 |
| limit > 100 | clamp 到 100 |
| tool_code 解析失败 | name='(已删除/未知)' |

---

## 六、Controller + Router

### 6.1 C 端 controller/feedback.ts

```typescript
import BaseController from './base';

const VALID_TYPES = ['bug', 'suggestion', 'praise', 'other'];

export default class FeedbackController extends BaseController {
  /**
   * POST /api/feedback
   * 可登录可不登录；限流 10/h/IP（路由层挂）
   */
  async create() {
    // 1. 可选 token 解析（宽松模式：JWT 验签通过即接受 userId，不查 session）
    let userId: number | null = null;
    const auth = this.ctx.get('authorization');
    if (auth) {
      const token = auth.replace(/^Bearer\s+/i, '');
      if (token) {
        try {
          const decoded = (this.app as any).jwt.verify(
            token, ((this.app.config as any).jwt as any).secret,
          ) as any;
          userId = decoded?.userId ?? null;
        } catch { /* 按匿名 */ }
      }
    }

    // 2. 参数校验
    this.validate({
      type: { type: 'enum', values: VALID_TYPES },
      content: { type: 'string', min: 5, max: 2000 },
      contact: { type: 'string', required: false, max: 100 },
      platform: { type: 'string', required: false, max: 30 },
    });

    const body = this.ctx.request.body as any;

    // 3. 未登录强制留联系方式
    if (!userId && !body.contact) {
      this.ctx.throw(422, '未登录用户必须填写联系方式');
    }

    // 4. 写入
    const fb = await this.service.feedback.create({
      userId,
      type: body.type,
      content: body.content,
      contact: body.contact || null,
      platform: body.platform || null,
      ip: this.ctx.ip,
      userAgent: this.ctx.get('user-agent'),
    });
    this.created({ id: (fb as any).id });
  }
}
```

### 6.2 管理端 controller/admin/feedback.ts

```typescript
import BaseController from '../base';

export default class AdminFeedbackController extends BaseController {
  /** GET /api/admin/feedbacks */
  async list() {
    const q = this.ctx.query as any;
    const result = await this.service.feedback.list({
      page: q.page ? Number(q.page) : undefined,
      pageSize: q.pageSize ? Number(q.pageSize) : undefined,
      type: q.type,
      status: q.status !== undefined ? Number(q.status) as 0|1|2|3 : undefined,
      platform: q.platform,
      userId: q.userId ? Number(q.userId) : undefined,
      keyword: q.keyword,
      startTime: q.startTime,
      endTime: q.endTime,
    });
    this.success(result);
  }

  /** GET /api/admin/feedbacks/:id */
  async detail() {
    const id = Number(this.ctx.params.id);
    const data = await this.service.feedback.detail(id);
    if (!data) this.ctx.throw(404, 'feedback not found');
    this.success(data);
  }

  /** POST /api/admin/feedbacks/:id/reply */
  async reply() {
    const id = Number(this.ctx.params.id);
    this.validate({ replyContent: { type: 'string', min: 1, max: 2000 } });
    const { replyContent } = this.ctx.request.body as any;
    const replyUserId = (this.ctx.state as any).user.id;

    let beforeData: any = null;
    try { beforeData = await this.service.feedback.detail(id); } catch {}

    try {
      const updated = await this.service.feedback.reply(id, replyContent, replyUserId);
      await this.service.audit.log({
        module: 'feedback', action: 'reply',
        bizType: 'feedback', bizId: id,
        beforeData, afterData: updated,
        description: `回复反馈 #${id}`, status: 1,
      });
      this.success(updated, '回复成功');
    } catch (e: any) {
      await this.service.audit.log({
        module: 'feedback', action: 'reply',
        bizType: 'feedback', bizId: id,
        beforeData,
        description: `尝试回复反馈 #${id}`,
        status: 0, failReason: e.message,
      });
      throw e;
    }
  }

  /** PUT /api/admin/feedbacks/:id */
  async update() {
    const id = Number(this.ctx.params.id);
    let beforeData: any = null;
    try { beforeData = await this.service.feedback.detail(id); } catch {}

    try {
      const updated = await this.service.feedback.update(id, this.ctx.request.body as any);
      await this.service.audit.log({
        module: 'feedback', action: 'update',
        bizType: 'feedback', bizId: id,
        beforeData, afterData: updated,
        description: `更新反馈 #${id} 状态`, status: 1,
      });
      this.success(updated, '更新成功');
    } catch (e: any) {
      await this.service.audit.log({
        module: 'feedback', action: 'update',
        bizType: 'feedback', bizId: id,
        beforeData,
        description: `尝试更新反馈 #${id} 状态`,
        status: 0, failReason: e.message,
      });
      throw e;
    }
  }

  /** DELETE /api/admin/feedbacks/:id */
  async destroy() {
    const id = Number(this.ctx.params.id);
    let beforeData: any = null;
    try { beforeData = await this.service.feedback.detail(id); } catch {}

    try {
      await this.service.feedback.destroy(id);
      await this.service.audit.log({
        module: 'feedback', action: 'delete',
        bizType: 'feedback', bizId: id,
        beforeData,
        description: `删除反馈 #${id}`, status: 1,
      });
      this.success(null, '删除成功');
    } catch (e: any) {
      await this.service.audit.log({
        module: 'feedback', action: 'delete',
        bizType: 'feedback', bizId: id,
        beforeData,
        description: `尝试删除反馈 #${id}`,
        status: 0, failReason: e.message,
      });
      throw e;
    }
  }
}
```

### 6.3 管理端 controller/admin/stats.ts

```typescript
import BaseController from '../base';

export default class AdminStatsController extends BaseController {
  async overview() {
    const data = await this.service.stats.overview();
    this.success(data);
  }

  async toolUsage() {
    const q = this.ctx.query as any;
    const data = await this.service.stats.getToolUsage({
      startTime: q.startTime, endTime: q.endTime,
      limit: q.limit ? Number(q.limit) : undefined,
    });
    this.success(data);
  }

  async userActive() {
    const q = this.ctx.query as any;
    const data = await this.service.stats.getUserActive({
      startTime: q.startTime, endTime: q.endTime,
    });
    this.success(data);
  }

  async trend() {
    const q = this.ctx.query as any;
    if (!q.metric) this.ctx.throw(422, 'metric required');
    const data = await this.service.stats.getTrend({
      metric: q.metric,
      granularity: q.granularity || 'day',
      startTime: q.startTime, endTime: q.endTime,
    });
    this.success(data);
  }

  async exportCsv() {
    const q = this.ctx.query as any;
    if (!q.type) this.ctx.throw(422, 'type required');
    await this.service.stats.exportCsv(q.type, q);
    // 不调 success — service 已 set ctx.body = stream
  }
}
```

### 6.4 路由注册（11 条）

```typescript
// app/router.ts — 在 Spec-A1 路由段后追加

// ==================== 反馈（C 端） ====================
router.post('/api/feedback',
  app.middleware.rateLimit({ max: 10, window: 3600 }, app),
  controller.feedback.create);

// ==================== 反馈（管理端） ====================
router.get('/api/admin/feedbacks',
  auth, perm('feedback:list'),
  adminCtrl.feedback.list);
router.post('/api/admin/feedbacks/:id/reply',
  auth, perm('feedback:reply'),
  adminCtrl.feedback.reply);
router.put('/api/admin/feedbacks/:id',
  auth, perm('feedback:update'),
  adminCtrl.feedback.update);
router.delete('/api/admin/feedbacks/:id',
  auth, perm('feedback:delete'),
  adminCtrl.feedback.destroy);
router.get('/api/admin/feedbacks/:id',
  auth, perm('feedback:detail'),
  adminCtrl.feedback.detail);

// ==================== 数据统计 ====================
router.get('/api/admin/stats/overview',
  auth, perm('stats:overview'),
  adminCtrl.stats.overview);
router.get('/api/admin/stats/tool-usage',
  auth, perm('stats:tool-usage'),
  adminCtrl.stats.toolUsage);
router.get('/api/admin/stats/user-active',
  auth, perm('stats:user-active'),
  adminCtrl.stats.userActive);
router.get('/api/admin/stats/trend',
  auth, perm('stats:trend'),
  adminCtrl.stats.trend);
router.get('/api/admin/stats/export',
  auth, perm('stats:export'),
  adminCtrl.stats.exportCsv);
```

> `adminCtrl` 是 Spec-A1 在 router.ts 已定义的 `const adminCtrl = controller.admin as any` 别名（绕 ets typings 滞后），本 spec 直接复用。

### 6.5 限流策略

`rateLimit({ max: 10, window: 3600 }, app)`：
- 单 IP 每小时最多 10 次 POST `/api/feedback`
- 超出 429
- Redis 不可用时自动降级放行（middleware 行为）
- **不区分登录/匿名**：YAGNI；正常用户每小时 10 条反馈足够

### 6.6 与 Spec-A1 不变量延续

| Spec-A1 §7.4 | A2 是否遵守 |
|---|---|
| #1 不重写业务逻辑（仅加 audit）| ✅ service 是新建；audit 调用全在 controller |
| #2 不动 service（audit 调用在 controller）| ✅ |
| #3 password/token/secret 不进 audit | ✅ feedback 无敏感字段；stats 是 GET |
| #4 bizId 必须 string | ✅ 由 service.audit 内部 String() 转 |
| #5 action 词表锁定 | ⚠️ 扩展词表 v1.1（新增 reply）|

---

## 七、测试

### 7.1 12 case 单测清单

#### `test/api/feedback.test.ts`（7 case）

| # | case | 验证 |
|---|---|---|
| 1 | 匿名 create | userId=null + status=0 入库 |
| 2 | 登录 create | userId=actual 入库 |
| 3 | list 按 status 过滤 + 软删过滤 | 软删条目不在结果 |
| 4 | reply 0→2 | reply_* 三字段写入 |
| 5 | reply 状态 2 时再 reply | 抛 409 |
| 6 | update 0→1 OK，2→3 抛 422 | transition 白名单生效 |
| 7 | C 端 controller：无 token + 无 contact | 返回 422 |

#### `test/api/stats.test.ts`（5 case）

| # | case | 验证 |
|---|---|---|
| 1 | overview | 8 字段全为 number |
| 2 | getToolUsage | 数组 + count DESC |
| 3 | getUserActive | dau/wau/mau + newUserTrend 数组 |
| 4 | getTrend(user-register, day) | metric/granularity/points 三字段 |
| 5 | getTrend invalid metric | 422 |

### 7.2 测试基础设施

- 使用 egg-bin test (mocha + egg-mock + supertest)
- 测试文件位于 `test/api/`
- 复用 `app.mockContext` / `app.httpRequest` / `mm.app` —— 与 Spec-A1 完全一致

### 7.3 手工验收清单（22 项）

| # | 场景 | 命令 / 预期 |
|---|---|---|
| 1 | 009 SQL 落地 | `SELECT * FROM permissions WHERE code='feedback:update'` 1 行 |
| 2 | 角色授权 | admin/operator 各有 feedback:update |
| 3 | C 端匿名提交 | POST /api/feedback（无 token）→ 201 + userId=null |
| 4 | C 端登录提交 | 带 token → 201 + userId=actual |
| 5 | C 端未登录无 contact | → 422 |
| 6 | 限流触发 | 同 IP 1 小时 11 次 → 第 11 次 429 |
| 7 | 管理端 list 过滤 | ?type=bug&status=0 |
| 8 | 管理端 reply 0→2 | POST /:id/reply → 200 + audit_logs 有记录 |
| 9 | reply 409 | 状态 2 再 reply → 409 + audit status=0 |
| 10 | update 重新打开 2→1 | PUT /:id {status:1} → 200 |
| 11 | update 禁止 transition 2→3 | → 422 |
| 12 | 软删 destroy | DELETE → list 不包含；DB deleted_at IS NOT NULL |
| 13 | stats overview | 8 字段全 number |
| 14 | stats tool-usage | 数组 + count 降序 |
| 15 | stats user-active | dau/wau/mau + newUserTrend |
| 16 | stats trend × 4 metric | 4 metric 各返回 points |
| 17 | stats trend × 3 granularity | day/week/month 各 bucket 格式 |
| 18 | stats export tool-usage | UTF-8 BOM CSV + Excel 正常 |
| 19 | stats export user-active 多段 | 总览段 + 趋势段 |
| 20 | RBAC 通过 | operator 调 feedback:update → 200 |
| 21 | RBAC 拒绝 | auditor 调 feedback:reply → 403 |
| 22 | 审计可查 | GET /audit-logs?module=feedback 含本 spec 操作 |

---

## 八、风险与缓解

| ID | 风险 | 缓解 |
|---|---|---|
| **R1** | 状态机 transition 多，手工测易漏 | TC #6 + 手工 #10/#11 三重覆盖；service 用 dict 配置便于 review |
| **R2** | C 端限流 10/h 是否过紧？ | 监控 429 响应率；超 1% 则放宽至 30/h（改一行配置） |
| **R3** | stats SQL 大数据量慢查询 | 全索引命中；时间范围默认 30 天；Spec-C 默认窗口 7-30 天 |
| **R4** | tool-usage 解析依赖 path 格式 | 实施时 dry-run 验证；Spec-C 显示"未知"占比 >5% 告警 |
| **R5** | feedback 无 user_id FK 约束（支持匿名）| LEFT JOIN required:false；前端显示"(匿名/已注销)" |
| **R6** | C 端 token 宽松解析（不查 session）→ 已登出 token 仍可署名 | 评估业务风险低（token 仍在有效期；登出后 5-30 分钟内匿名提交被署名问题不大）|

## 九、开放问题

| ID | 问题 | 决策 |
|---|---|---|
| **O1** | 是否补零（缺失日期）| 本 spec 不补，前端 ECharts 自动处理；Spec-C 反馈再说 |
| **O2** | 软删 audit 是否记 bizId | 记（沿用 Spec-A1 §7.1） |
| **O3** | user-active CSV 多段是否让用户困惑 | 文档说明；Spec-C 反馈不爽再拆 |

---

## 十、与上下游 Spec 衔接

| Spec | 关系 |
|---|---|
| **Spec-A1** ✅ 已合并 master | 复用 `service.audit.log()` 11 个写操作埋点 + 复用 PassThrough CSV 模板 |
| **Spec-C 管理端反馈页** | 直接消费 5 个 `/api/admin/feedbacks*`；状态机 UI 按 §4.3 转移图实现 |
| **Spec-C 管理端统计页** | 直接消费 5 个 `/api/admin/stats/*`；trend chart 用 ECharts + metric/granularity 下拉 |
| **Spec-C C 端反馈入口** | tool-box / micro-tools 各加"意见反馈" → POST /api/feedback；登录态自动取 token |

---

## 十一、附录

### 11.1 决策溯源

| Q | 决策 | 备选拒因 |
|---|---|---|
| Q1 | A 最小闭环 | B 加 tool_usage_logs 表与 api_logs 信息重复；C 预聚合过早优化；E 拆 spec 后 brainstorming 开销翻倍 |
| Q2 | B 标准字段 | A 简到失去筛选能力；C 的 attachments/priority/tags 当前无人用 |
| Q3 | B 可选 auth + IP 限流 | A 强制登录堵用户；C 完全开放失去用户上下文 |
| Q4 | B 严格状态机 | A 可重复回复丢历史；C 通知服务超 scope |
| Q4.1 | b 新增 update 权限码 | a 合并到 reply 限制重新打开 + 立即回复；c"自动重开"行为隐晦 |
| Q5 | B 标准 | A user-active 缺 WAU/MAU；C 同比环比当前 UI 不展示 |
| Q6 | B 手动解析 token | A 新增 optionalAuth middleware 长期会膨胀；C 双接口违反 REST 单一资源 |
| Q7 | A 最小集 | B/C 边际价值低；E2E 已被手工验收覆盖 |

### 11.2 改动统计预估

| 类别 | 文件数 | 行数 |
|---|---|---|
| SQL | 1 | ~50 |
| Model | 1 | ~50 |
| Service | 2 | ~610 |
| Controller | 3 | ~280 |
| Router | 1 | ~25 |
| Test | 2 | ~250 |
| Doc | 4 | ~250 |
| **合计** | **~13** | **~1500 行净增** |

### 11.3 工作量估算

| 阶段 | 任务数 | 估时 |
|---|---|---|
| brainstorming（已完成） | — | 1.5h |
| writing-plans | — | 0.5h |
| 实施 | **15 task** | 4-5h |
| 总计 | — | ~7h |

### 11.4 与 Spec-A1 横向对照

| 维度 | Spec-A1 | Spec-A2 |
|---|---|---|
| 文件数 | 18 | 13 |
| 代码行 | +1849 / -82 | ~+1500 净增 |
| 任务数 | 14 | 15 |
| 单测 | 10 | 12 |
| 手工验收 | 14 | 22 |
| 决策数 | 9 | 7 |
| 引入新基础设施 | audit / requestStartTime middleware | **无（全复用 A1）** |

---

**END of Spec-A2**
