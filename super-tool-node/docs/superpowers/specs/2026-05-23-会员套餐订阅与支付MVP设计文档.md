# 会员套餐订阅与支付 MVP 设计文档

> 创建日期：2026-05-23
> 分支：`member-subscription-mvp`（基于 master @ `e6ce79d`）
> 上游 spec：Spec-C2b（管理端会员管理，已收官）/ 003 会员体系设计（已收官）/ 018 通知系统（已收官）
> 状态：设计阶段（brainstorming 收尾，待 review → 写实施计划）

---

## 1. 概述与范围

### 1.1 目标

为已实装的会员等级 + 积分体系补齐**订阅 / 支付 / 订单 / 到期闭环**：用户可在 H5 端选择套餐、走完支付流程、收到通知、套餐到期前后被自动提醒。系统侧建立标准化的"订单 1:N 支付流水"模型与可插拔的 PaymentProvider 适配器，**为后续接入真实微信支付/支付宝零业务代码改动留位**。

### 1.2 brainstorming 决策汇总

| # | 决策点 | 选择 |
|---|---|---|
| Q1 | 范围边界 | **B**：MVP 闭环（订单 + 支付下单 + 仅微信支付架构 + 回调 + 到期定时 + H5 4 页 + 管理端订单只读） |
| Q2 | 数据模型 | **B**：双表分离 `member_orders` + `member_payments` |
| Q3 | 微信支付集成 | **D**：Mock Provider + 适配器架构（PaymentProvider 接口 + MockProvider 实装 + Wechat 占位） |
| Q4 | 续费逻辑 | **A**：同套餐叠加 + 跨套餐禁止下单 |
| Q5 | 到期定时 | **B**：每天 09:00 扫描 7d/1d/已过期，幂等键防重 |
| Q6 | H5 重构粒度 | **B**：4 页（member 重写 + cashier + orders + orders/:id）+ mine 加入口 |

### 1.3 不在范围（明确排除）

| 排除项 | 原因/归属 |
|---|---|
| 真实微信支付 SDK 接入（JSAPI/Native/H5） | 留 PaymentProvider 接口占位，单独 spec 接入（涉及商户号、证书、IP 白名单、对账） |
| 支付宝集成 | 同上，接口已留位，单独 spec |
| 退款功能 | MVP 不做（业务约定：模拟支付成功后无法回退；未来用 `member_refunds` 单独建表） |
| 升降级差价计算 | MVP 用 Q4=A 规则禁止跨套餐购买（前端禁用 + 后端 400 校验）；差价方案单独 spec |
| 订单优惠/优惠券/促销活动 | 非 MVP 范围 |
| 自动续费（订阅制） | 非 MVP 范围（需要授权扣款） |
| 对账文件/财务报表 | 非 MVP 范围 |
| 管理端订单退款/手动改价 | 仅做只读列表 + 详情；写操作单独 spec |

### 1.4 与现有模块的关系

| 维度 | 003 会员体系 | 018 通知系统 | C2b 管理端会员 | **本 spec** |
|---|---|---|---|---|
| DB 迁移 | 003 | 018 | 007（RBAC） | **021（订单/支付）+ 022（订单 RBAC）+ 023（通知种子增量）** |
| Service 复用 | `member.activatePaidPlan` `member.addPoints` | `notification.core.send` | `audit.log` | 全部复用 |
| 通知类型 | 已有 `BUSINESS_PLAN_ACTIVATED` `BUSINESS_POINTS_CHANGE` `BUSINESS_MEMBER_UPGRADE` `BUSINESS_MEMBER_EXPIRE_SOON` `BUSINESS_MEMBER_EXPIRED` | 通知发送基础设施 | - | **新增 `BUSINESS_PAYMENT_SUCCESS` `BUSINESS_PAYMENT_FAIL` 模板** |

---

## 2. 架构总览

### 2.1 端到端数据流

```
H5 用户                后端                                      联动模块
─────                ────                                      ────────

[1] 立即订阅 ───────► POST /api/orders                          
                     OrderService.create(planCode)             
                     ├── 校验：is_paid=1 且 planCode 不同 → 400
                     ├── 创建 member_orders（status=0 待支付）  
                     └── 返回 { orderId, orderNo, amount, expireAt }

[2] 选支付 ────────► POST /api/payments                         
                     PaymentService.create(orderId, 'mock')    
                     ├── 创建 member_payments（status=0 pending）
                     ├── PaymentProvider('mock').createPrepay()
                     └── 返回 { paymentNo, prepayData, cashierUrl }

[3] 跳收银台 ───────► H5 /member/cashier?paymentNo=xxx          
                     用户点 [模拟成功] / [模拟失败] / [等待回调] 

[4] 模拟回调 ───────► POST /api/payments/mock/notify           ◄─── 内部接口（不挂 auth）
                     PaymentService.handleCallback(rawBody)    
                     ├── verifyCallback（mock 透传）            
                     ├── 事务（行级锁 SELECT FOR UPDATE）：     
                     │   ├── payment.status=1 success           
                     │   ├── order.status=1 paid                
                     │   └── member.activatePaidPlan() ────────► [积分+通知联动]
                     │                                          │   ├── addPoints (giftPoints)
                     │                                          │   │   └─ BUSINESS_POINTS_CHANGE
                     │                                          │   └─ BUSINESS_PLAN_ACTIVATED
                     ├── 通知 BUSINESS_PAYMENT_SUCCESS          
                     └── 返回 { success: true }

[5] 失败回调 ───────► POST /api/payments/mock/notify (failed)   
                     ├── payment.status=2 failed                
                     ├── order.status 保持 0（用户可重新支付）  
                     └── 通知 BUSINESS_PAYMENT_FAIL             

[6] 每天 09:00 ────► schedule.memberExpireCheck                
                     ├── 扫 paid_expire_at ∈ +7d → BUSINESS_MEMBER_EXPIRE_SOON {stage:'7d'}
                     ├── 扫 paid_expire_at ∈ +1d → BUSINESS_MEMBER_EXPIRE_SOON {stage:'1d'}
                     └── 扫 paid_expire_at ∈ -1d～now → UPDATE is_paid=0 + BUSINESS_MEMBER_EXPIRED

[7] 订单过期 ───────► schedule.orderExpireCheck (每 5 分钟)    
                     扫 expire_at < now 且 status=0     
                     UPDATE status=3 expired                   
```

### 2.2 模块边界

```
super-tool-node/
├── app/
│   ├── lib/payment/                    ← 新增：支付适配器层
│   │   ├── provider.ts                 接口定义（PaymentProvider）
│   │   ├── factory.ts                  工厂（按 code 返回实现）
│   │   ├── mock.ts                     MockProvider（本次实装）
│   │   ├── wechat-jsapi.ts             占位（throw NotImplemented）
│   │   ├── wechat-native.ts            占位
│   │   └── index.ts                    re-export
│   ├── model/
│   │   ├── member_order.ts             新增
│   │   └── member_payment.ts           新增
│   ├── service/
│   │   ├── order.ts                    新增（订单 CRUD + 状态机）
│   │   ├── payment.ts                  新增（支付下单 + 回调处理）
│   │   └── member.ts                   微改（activatePaidPlan 接 orderId 参数）
│   ├── controller/
│   │   ├── order.ts                    新增（C 端 4 接口）
│   │   ├── payment.ts                  新增（C 端 2 接口 + Mock 回调）
│   │   └── admin/order.ts              新增（管理端 3 接口）
│   ├── schedule/
│   │   ├── memberExpireCheck.ts        新增（cron: 0 9 * * *）
│   │   └── orderExpireCheck.ts         新增（interval: 5min）
│   └── router.ts                       改（增 8 + 2 内部 路由）
├── database/
│   ├── 021_add_order_payment.sql       新增（2 张表 + 系统配置）
│   ├── 022_add_order_module.sql        新增（3 RBAC 权限码）
│   └── 023_add_payment_notification.sql 新增（2 通知类型 + 模板）
└── docs/superpowers/specs/2026-05-23-...md  本文件

super-tools-web/packages/h5/micro-tools/
├── pages/member/
│   ├── index.tsx                       重写（从 39 行 → ~250 行）
│   ├── index.less                      改
│   ├── cashier/
│   │   ├── index.tsx                   新（Mock 收银台）
│   │   └── index.less
│   └── orders/
│       ├── index.tsx                   新（订单列表）
│       ├── index.less
│       ├── [id]/
│       │   ├── index.tsx               新（订单详情）
│       │   └── index.less
├── pages/mine/index.tsx                改（菜单加"我的订单"入口）
├── service/
│   ├── member.ts                       扩展
│   └── payment.ts                      新（订单 + 支付 API）
├── store/
│   └── order.ts                        新（订单状态管理）
├── types/
│   └── order.ts                        新（订单/支付类型）
└── routes.config.ts                    改（增 4 路由）

super-tools-admin/
├── src/
│   ├── pages/Member/Orders/            新（管理端订单页）
│   │   ├── index.tsx                   订单列表
│   │   ├── DetailDrawer.tsx            订单详情抽屉
│   │   └── index.less
│   ├── services/
│   │   └── order.ts                    新（管理端订单 API）
│   └── services/__tests__/
│       └── order.test.tsx              新（≥ 5 case）
└── config/routes/modules/member.ts     改（加 /member/orders 路由）
```

### 2.3 PaymentProvider 适配器接口（Q3=D 落地）

```typescript
// app/lib/payment/provider.ts
export type ProviderCode = 'mock' | 'wechat_jsapi' | 'wechat_native' | 'alipay';

export interface PrepayInput {
  paymentNo: string;        // 内部支付流水号
  amount: number;           // 金额（元，DECIMAL）
  subject: string;          // 订单标题（例如 "月度会员"）
  userId: number;
  notifyUrl: string;        // 完整回调 URL
  extra?: Record<string, any>;
}

export interface PrepayResult {
  prepayData: Record<string, any>;  // provider 特定的预支付参数（JSAPI 用，前端调起需要）
  cashierUrl?: string;              // 可选：跳转收银台 URL（mock/Native 用）
}

export interface VerifyResult {
  success: boolean;
  paymentNo: string;                  // 从回调中解出的内部 paymentNo
  providerTradeNo: string;            // provider 的交易号
  amount: number;
  rawPayload: Record<string, any>;    // 原始回调体（持久化用）
  error?: string;                     // 验签失败原因
}

export interface PaymentProvider {
  readonly code: ProviderCode;
  createPrepay(input: PrepayInput): Promise<PrepayResult>;
  verifyCallback(headers: Record<string, string>, rawBody: string): Promise<VerifyResult>;
  queryStatus(providerTradeNo: string): Promise<'pending' | 'success' | 'failed'>;
}
```

**MockProvider 行为**：
- `createPrepay`：返回 `{ prepayData: { mockToken: 'mock_xxx' }, cashierUrl: '/member/cashier?paymentNo=xxx' }`
- `verifyCallback`：直接信任 `rawBody`（解 JSON），不做签名校验，但**校验 `paymentNo` 在 `member_payments` 表中存在**
- `queryStatus`：返回 'pending'（service 层直接读 DB 状态）

**WechatJsapiProvider / WechatNativeProvider 占位**：均 `throw new Error('NotImplemented: ...')`，附带接入 TODO 提示。

### 2.4 订单状态机

```
                  [createOrder]
                       │
                       ▼
             ┌─── 0 待支付 ───────────────┐
             │         │                  │
   [createPayment]     │           [orderExpireCheck]
             │         │                  │
             ▼         ▼                  ▼
     [回调成功]   [用户取消]          [30min 未支付]
             │         │                  │
             ▼         ▼                  ▼
       1 已支付     2 已取消            3 已过期
       (终态)       (终态)              (终态)

注：4 已退款（终态，本 MVP 不实现，仅在 status enum 留位）
```

订单 status 转移规则：
- `0 → 1`：支付回调成功（PaymentService.handleCallback）
- `0 → 2`：用户主动取消（OrderService.cancel，仅 0 状态可取消）
- `0 → 3`：调度器扫描 `expire_at < now AND status = 0` 自动置过期
- 其他转移**禁止**（service 层强制校验）

### 2.5 支付状态机

```
        [createPayment]
              │
              ▼
       0 pending ──────────┐
              │            │
      [回调成功]    [回调失败/超时]
              │            │
              ▼            ▼
         1 success     2 failed
         (终态)        (非终态：用户可基于同 order 创建新 payment)
```

**关键约束**：同一 `orderId` 可以有 N 条 `member_payments`（用户付款失败 → 创建新支付重试）；但只能有 **0 或 1 条 `status=1 success`**（service 层用 `findOne(orderId, status=1)` 校验防重）。

---

## 3. 数据库设计

### 3.1 迁移脚本 `021_add_order_payment.sql`

```sql
-- ============================================================
-- 迁移脚本: 021_add_order_payment.sql
-- 版本: 2.7.0
-- 创建时间: 2026-05-23
-- 说明: 会员套餐订阅支付 —— 订单表 + 支付流水表
-- 设计文档: docs/superpowers/specs/2026-05-23-会员套餐订阅与支付MVP设计文档.md
-- 前置依赖: 003_add_member_system.sql
-- ============================================================

USE `superadmin_db`;

SET NAMES utf8mb4;
SET FOREIGN_KEY_CHECKS = 0;

-- ------------------------------------------------------------
-- 1. 会员订单表
-- ------------------------------------------------------------
CREATE TABLE IF NOT EXISTS `member_orders` (
  `id`              BIGINT UNSIGNED   NOT NULL AUTO_INCREMENT,
  `order_no`        VARCHAR(32)       NOT NULL COMMENT '订单号（业务流水号，对外展示）',
  `user_id`         BIGINT UNSIGNED   NOT NULL COMMENT '用户ID',
  `plan_id`         INT UNSIGNED      NOT NULL COMMENT '套餐ID（快照）',
  `plan_code`       VARCHAR(30)       NOT NULL COMMENT '套餐编码（快照）',
  `plan_snapshot`   JSON              NOT NULL COMMENT '下单时套餐完整快照',
  `amount`          DECIMAL(10,2)     NOT NULL COMMENT '应付金额',
  `status`          TINYINT UNSIGNED  NOT NULL DEFAULT 0 COMMENT '0待支付/1已支付/2已取消/3已过期/4已退款',
  `scene`           TINYINT UNSIGNED  NOT NULL DEFAULT 1 COMMENT '场景:1新购/2续费',
  `paid_at`         DATETIME          DEFAULT NULL COMMENT '支付完成时间',
  `cancelled_at`    DATETIME          DEFAULT NULL COMMENT '取消时间',
  `expire_at`       DATETIME          NOT NULL COMMENT '订单过期时间',
  `remark`          VARCHAR(200)      DEFAULT NULL,
  `created_at`      DATETIME          NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at`      DATETIME          NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_order_no`     (`order_no`),
  INDEX `idx_user_id`          (`user_id`),
  INDEX `idx_status`           (`status`),
  INDEX `idx_user_status`      (`user_id`, `status`),
  INDEX `idx_expire_status`    (`expire_at`, `status`),
  INDEX `idx_created_at`       (`created_at`),
  CONSTRAINT `fk_order_user` FOREIGN KEY (`user_id`)
    REFERENCES `users` (`id`) ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='会员订单表';

-- ------------------------------------------------------------
-- 2. 支付流水表（1 订单 N 流水）
-- ------------------------------------------------------------
CREATE TABLE IF NOT EXISTS `member_payments` (
  `id`                  BIGINT UNSIGNED   NOT NULL AUTO_INCREMENT,
  `payment_no`          VARCHAR(32)       NOT NULL COMMENT '支付流水号（内部）',
  `order_id`            BIGINT UNSIGNED   NOT NULL,
  `user_id`             BIGINT UNSIGNED   NOT NULL,
  `provider`            VARCHAR(20)       NOT NULL COMMENT 'mock/wechat_jsapi/wechat_native/alipay',
  `provider_trade_no`   VARCHAR(64)       DEFAULT NULL COMMENT '第三方流水号',
  `amount`              DECIMAL(10,2)     NOT NULL,
  `status`              TINYINT UNSIGNED  NOT NULL DEFAULT 0 COMMENT '0pending/1success/2failed/3refunded',
  `prepay_data`         JSON              DEFAULT NULL COMMENT '预支付参数',
  `callback_payload`    JSON              DEFAULT NULL COMMENT '回调原始内容',
  `paid_at`             DATETIME          DEFAULT NULL,
  `failed_reason`       VARCHAR(500)      DEFAULT NULL,
  `created_at`          DATETIME          NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at`          DATETIME          NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_payment_no` (`payment_no`),
  INDEX `idx_order_id`       (`order_id`),
  INDEX `idx_user_id`        (`user_id`),
  INDEX `idx_status`         (`status`),
  INDEX `idx_provider_trade` (`provider`, `provider_trade_no`),
  CONSTRAINT `fk_payment_order` FOREIGN KEY (`order_id`)
    REFERENCES `member_orders` (`id`) ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='会员支付流水表';

-- 系统配置
INSERT IGNORE INTO `system_configs` (`group`, `key`, `value`, `type`, `is_secret`, `is_public`, `description`) VALUES
('payment', 'order_expire_minutes',  '30',       'number',  0, 1, '订单超时未支付的过期时长（分钟）'),
('payment', 'mock_auto_success',     'false',    'boolean', 0, 0, '开发期：用户停留收银台 30s 后自动模拟支付成功'),
('payment', 'mock_auto_delay_sec',   '30',       'number',  0, 0, 'mock_auto_success 的延时秒数'),
('payment', 'enabled_providers',     '["mock"]', 'json',    0, 1, '当前启用的支付方式');

SET FOREIGN_KEY_CHECKS = 1;
```

### 3.2 迁移脚本 `022_add_order_module.sql`（RBAC）

```sql
-- ============================================================
-- 迁移脚本: 022_add_order_module.sql
-- 版本: 2.7.1
-- 说明: 订单模块 RBAC 权限码（管理端只读）
-- ============================================================

USE `superadmin_db`;
SET NAMES utf8mb4;
SET FOREIGN_KEY_CHECKS = 0;

-- 幂等清理
DELETE rp FROM `role_permissions` rp
  INNER JOIN `permissions` p ON rp.permission_id = p.id
  WHERE p.code IN ('member:order:list', 'member:order:detail', 'member:order:stats');
DELETE FROM `permissions` WHERE code IN ('member:order:list', 'member:order:detail', 'member:order:stats');

-- 在 member 顶级菜单下增加 3 个 API 权限
INSERT INTO `permissions`
  (`code`, `name`, `type`, `module`, `platform`, `path`, `method`, `parent_id`, `sort`)
SELECT 'member:order:list', '会员订单列表', 4, 'member', 'admin',
       '/api/admin/member/orders', 'GET',
       (SELECT id FROM (SELECT id FROM `permissions` WHERE code = 'member') t), 110
UNION ALL
SELECT 'member:order:detail', '订单详情', 4, 'member', 'admin',
       '/api/admin/member/orders/:id', 'GET',
       (SELECT id FROM (SELECT id FROM `permissions` WHERE code = 'member') t), 120
UNION ALL
SELECT 'member:order:stats', '订单统计', 4, 'member', 'admin',
       '/api/admin/member/orders/stats', 'GET',
       (SELECT id FROM (SELECT id FROM `permissions` WHERE code = 'member') t), 130;

-- 三个角色都授权（只读权限可全开）
INSERT INTO `role_permissions` (`role_id`, `permission_id`)
SELECT r.id, p.id
FROM `roles` r CROSS JOIN `permissions` p
WHERE r.code IN ('admin', 'operator', 'auditor')
  AND p.code IN ('member:order:list', 'member:order:detail', 'member:order:stats');

SET FOREIGN_KEY_CHECKS = 1;
```

### 3.3 迁移脚本 `023_add_payment_notification.sql`（通知种子）

```sql
-- ============================================================
-- 迁移脚本: 023_add_payment_notification.sql
-- 版本: 2.7.2
-- 说明: 新增 2 个通知类型（支付成功/失败）+ 模板
-- 前置依赖: 018_add_notification_system.sql
-- ============================================================

USE `superadmin_db`;
SET NAMES utf8mb4;

-- 新增通知类型
INSERT IGNORE INTO `notification_types`
  (`code`, `name`, `description`, `category`, `default_channels`, `enabled`, `priority`, `policy`, `icon`, `color`, `system`, `sort`)
VALUES
  ('BUSINESS_PAYMENT_SUCCESS', '支付成功', '订单支付成功通知', 'business', '["in_app","email"]', 1, 1, 'respect', 'check-circle', '#52c41a', 1, 30),
  ('BUSINESS_PAYMENT_FAIL',    '支付失败', '订单支付失败通知', 'business', '["in_app"]',          1, 1, 'respect', 'close-circle', '#ff4d4f', 1, 31);

-- 模板（站内信）
INSERT IGNORE INTO `notification_templates`
  (`type_id`, `code`, `name`, `channel`, `language`, `title_tpl`, `content_tpl`, `version`, `enabled`, `system`)
SELECT t.id, 'BUSINESS_PAYMENT_SUCCESS_INAPP', '支付成功-站内信', 'in_app', 'zh-CN',
       '支付成功',
       '订单 {{orderNo}} 已支付成功，{{planName}} 已开通至 {{expireAt}}。',
       1, 1, 1
FROM `notification_types` t
WHERE t.code = 'BUSINESS_PAYMENT_SUCCESS'
  AND NOT EXISTS (SELECT 1 FROM `notification_templates` WHERE code = 'BUSINESS_PAYMENT_SUCCESS_INAPP' AND channel = 'in_app');

INSERT IGNORE INTO `notification_templates`
  (`type_id`, `code`, `name`, `channel`, `language`, `title_tpl`, `content_tpl`, `version`, `enabled`, `system`)
SELECT t.id, 'BUSINESS_PAYMENT_FAIL_INAPP', '支付失败-站内信', 'in_app', 'zh-CN',
       '支付失败',
       '订单 {{orderNo}} 支付失败，原因：{{reason}}。您可前往「我的订单」重新支付。',
       1, 1, 1
FROM `notification_types` t
WHERE t.code = 'BUSINESS_PAYMENT_FAIL'
  AND NOT EXISTS (SELECT 1 FROM `notification_templates` WHERE code = 'BUSINESS_PAYMENT_FAIL_INAPP' AND channel = 'in_app');

-- 邮件模板（仅成功推送邮件）
INSERT IGNORE INTO `notification_templates`
  (`type_id`, `code`, `name`, `channel`, `language`, `title_tpl`, `content_tpl`, `version`, `enabled`, `system`)
SELECT t.id, 'BUSINESS_PAYMENT_SUCCESS_EMAIL', '支付成功-邮件', 'email', 'zh-CN',
       '【订单 {{orderNo}}】支付成功',
       '<p>您好，</p><p>您的订单 <strong>{{orderNo}}</strong> 已支付成功！</p><p>套餐：{{planName}}<br>有效期至：{{expireAt}}<br>订单金额：¥{{amount}}</p><p>感谢您的支持。</p>',
       1, 1, 1
FROM `notification_types` t
WHERE t.code = 'BUSINESS_PAYMENT_SUCCESS'
  AND NOT EXISTS (SELECT 1 FROM `notification_templates` WHERE code = 'BUSINESS_PAYMENT_SUCCESS_EMAIL' AND channel = 'email');

-- 到期提醒模板增量（如果 018 已存在则跳过）
INSERT IGNORE INTO `notification_templates`
  (`type_id`, `code`, `name`, `channel`, `language`, `title_tpl`, `content_tpl`, `version`, `enabled`, `system`)
SELECT t.id, 'BUSINESS_MEMBER_EXPIRE_SOON_INAPP', '会员到期提醒-站内信', 'in_app', 'zh-CN',
       '会员即将到期',
       '您的{{planName}}将于 {{expireAt}} 到期（剩 {{daysLeft}} 天），续费可享原价优惠。',
       1, 1, 1
FROM `notification_types` t
WHERE t.code = 'BUSINESS_MEMBER_EXPIRE_SOON'
  AND NOT EXISTS (SELECT 1 FROM `notification_templates` WHERE code = 'BUSINESS_MEMBER_EXPIRE_SOON_INAPP' AND channel = 'in_app');

INSERT IGNORE INTO `notification_templates`
  (`type_id`, `code`, `name`, `channel`, `language`, `title_tpl`, `content_tpl`, `version`, `enabled`, `system`)
SELECT t.id, 'BUSINESS_MEMBER_EXPIRED_INAPP', '会员已过期-站内信', 'in_app', 'zh-CN',
       '会员已过期',
       '您的{{planName}}已于 {{expireAt}} 过期，欢迎续费继续享受会员权益。',
       1, 1, 1
FROM `notification_types` t
WHERE t.code = 'BUSINESS_MEMBER_EXPIRED'
  AND NOT EXISTS (SELECT 1 FROM `notification_templates` WHERE code = 'BUSINESS_MEMBER_EXPIRED_INAPP' AND channel = 'in_app');
```

### 3.4 索引设计要点

| 表 | 索引 | 查询场景 |
|---|---|---|
| member_orders | `idx_user_status` | 用户"我的订单"按状态筛选（H5 列表） |
| member_orders | `idx_expire_status` | `orderExpireCheck` 扫描过期订单 |
| member_payments | `idx_order_id` | 订单详情查所有支付流水（重试历史） |
| member_payments | `idx_provider_trade` | 第三方回调按 trade_no 查（未来真实支付用） |
| user_members（已有） | `idx_paid_expire` | `memberExpireCheck` 扫描即将到期 |

---

## 4. 后端 Service 设计

### 4.1 `app/service/order.ts`（新增）

```typescript
import BaseService, { PaginationResult } from './base';

interface CreateOrderInput {
  userId: number;
  planCode: string;
  remark?: string;
}

export interface OrderListQuery {
  page?: number;
  pageSize?: number;
  userId?: number;
  status?: number;
  startDate?: string;
  endDate?: string;
}

export default class OrderService extends BaseService {
  /**
   * 创建订单（C 端入口）
   * 校验：
   *   1. 套餐存在且 status=1
   *   2. 当前用户会员状态：
   *      - 未付费 → 新购（scene=1）
   *      - 付费但同 planCode → 续费（scene=2）
   *      - 付费但不同 planCode → 抛 400（Q4=A 规则）
   *   3. 当前用户存在未支付订单 → 抛 400
   */
  async create(input: CreateOrderInput) {
    const { userId, planCode } = input;

    const plan = await this.ctx.model.PaidPlan.findOne({
      where: { code: planCode, status: 1 },
    });
    if (!plan) this.ctx.throw(404, '套餐不存在或已下架');
    const planData = (plan as any).toJSON();

    const member = await this.ctx.model.UserMember.findOne({ where: { userId } });
    if (!member) this.ctx.throw(404, '会员记录不存在');
    const memberData = (member as any).toJSON();

    let scene: 1 | 2 = 1;
    const now = new Date();
    const expireAt = memberData.paidExpireAt ? new Date(memberData.paidExpireAt) : null;
    const isActive = memberData.isPaid === 1 && (!expireAt || expireAt > now);
    if (isActive) {
      if (memberData.paidPlanCode !== planCode) {
        this.ctx.throw(400, `您当前为「${memberData.paidPlanCode}」套餐，请先到期或选择当前套餐续费（MVP 不支持升降级）`);
      }
      scene = 2;
    }

    const pending = await this.ctx.model.MemberOrder.findOne({
      where: { userId, status: 0 },
    });
    if (pending) {
      this.ctx.throw(400, `您有未完成订单 ${(pending as any).orderNo}，请先处理`);
    }

    const orderNo = this._genOrderNo();
    const expireMinutes = await this._getConfig('order_expire_minutes', 30);
    const orderExpireAt = new Date(Date.now() + expireMinutes * 60 * 1000);

    const order = await this.ctx.model.MemberOrder.create({
      orderNo, userId,
      planId: planData.id, planCode: planData.code,
      planSnapshot: planData,
      amount: planData.price,
      status: 0, scene,
      expireAt: orderExpireAt,
      remark: input.remark,
    });

    return {
      orderId: (order as any).id,
      orderNo,
      amount: planData.price,
      planName: planData.name,
      expireAt: orderExpireAt,
      scene,
    };
  }

  /** 取消订单（仅 status=0 可取消） */
  async cancel(orderId: number, userId: number) {
    const order = await this.ctx.model.MemberOrder.findOne({
      where: { id: orderId, userId },
    });
    if (!order) this.ctx.throw(404, '订单不存在');
    if ((order as any).status !== 0) this.ctx.throw(400, '订单状态不允许取消');

    await (order as any).update({ status: 2, cancelledAt: new Date() });
    return { orderId, status: 2 };
  }

  /** 订单详情（含 payments 流水） */
  async detail(orderId: number, userId?: number) {
    const where: any = { id: orderId };
    if (userId) where.userId = userId;
    const order = await this.ctx.model.MemberOrder.findOne({
      where,
      include: [{ model: this.ctx.model.MemberPayment, as: 'payments' }],
    });
    if (!order) this.ctx.throw(404, '订单不存在');
    return (order as any).toJSON();
  }

  /** 用户订单列表 */
  async listByUser(userId: number, query: OrderListQuery): Promise<PaginationResult<any>> {
    const { status, ...pagination } = query;
    const where: any = { userId };
    if (status !== undefined) where.status = Number(status);
    return this.paginate(this.ctx.model.MemberOrder, { where }, pagination);
  }

  /** 管理端：全局订单列表 */
  async listAll(query: OrderListQuery): Promise<PaginationResult<any>> {
    const { Op } = require('sequelize');
    const { userId, status, startDate, endDate, ...pagination } = query;
    const where: any = {};
    if (userId) where.userId = Number(userId);
    if (status !== undefined) where.status = Number(status);
    if (startDate || endDate) {
      where.createdAt = {};
      if (startDate) where.createdAt[Op.gte] = new Date(startDate);
      if (endDate) where.createdAt[Op.lte] = new Date(endDate + 'T23:59:59');
    }
    return this.paginate(this.ctx.model.MemberOrder, {
      where,
      include: [{
        model: this.ctx.model.User, as: 'user',
        attributes: ['id', 'username', 'nickname', 'phone', 'email'],
      }],
    }, pagination);
  }

  /** 管理端：订单统计 */
  async stats(query: { startDate?: string; endDate?: string }) {
    const { Op } = require('sequelize');
    const where: any = {};
    if (query.startDate || query.endDate) {
      where.createdAt = {};
      if (query.startDate) where.createdAt[Op.gte] = new Date(query.startDate);
      if (query.endDate) where.createdAt[Op.lte] = new Date(query.endDate + 'T23:59:59');
    }

    const totalOrders = await this.ctx.model.MemberOrder.count({ where });
    const paidOrders = await this.ctx.model.MemberOrder.count({ where: { ...where, status: 1 } });
    const totalRevenue = await this.ctx.model.MemberOrder.sum('amount', {
      where: { ...where, status: 1 },
    }) || 0;

    return {
      totalOrders,
      paidOrders,
      payRate: totalOrders > 0 ? Number((paidOrders / totalOrders).toFixed(4)) : 0,
      totalRevenue: Number(totalRevenue),
    };
  }

  /** 调度器：扫描过期订单 */
  async cleanExpired(): Promise<number> {
    const { Op } = require('sequelize');
    const [affected] = await this.ctx.model.MemberOrder.update(
      { status: 3 },
      { where: { status: 0, expireAt: { [Op.lt]: new Date() } } },
    );
    return affected;
  }

  // ==================== 私有方法 ====================

  private _genOrderNo(): string {
    const now = new Date();
    const ymd = now.getFullYear().toString()
      + String(now.getMonth() + 1).padStart(2, '0')
      + String(now.getDate()).padStart(2, '0');
    const rand = Math.floor(Math.random() * 1e6).toString().padStart(6, '0');
    return `MO${ymd}${Date.now().toString().slice(-6)}${rand.slice(0, 4)}`;
  }

  private async _getConfig(key: string, defaultVal: number): Promise<number> {
    const SystemConfig = (this.ctx.model as any).SystemConfig;
    if (!SystemConfig) return defaultVal;
    const row = await SystemConfig.findOne({ where: { group: 'payment', key } });
    if (!row) return defaultVal;
    const v = Number((row as any).value);
    return isNaN(v) ? defaultVal : v;
  }
}
```

### 4.2 `app/service/payment.ts`（新增）

```typescript
import BaseService from './base';
import { getPaymentProvider, ProviderCode } from '../lib/payment';

interface CreatePaymentInput {
  orderId: number;
  userId: number;
  provider: ProviderCode;
}

export default class PaymentService extends BaseService {
  /** 创建支付（用户在 H5 选完支付方式后调用） */
  async create(input: CreatePaymentInput) {
    const { orderId, userId, provider } = input;

    const order = await this.ctx.model.MemberOrder.findOne({
      where: { id: orderId, userId },
    });
    if (!order) this.ctx.throw(404, '订单不存在');
    const orderData = (order as any).toJSON();
    if (orderData.status !== 0) this.ctx.throw(400, `订单状态不允许支付（当前 ${orderData.status}）`);
    if (new Date(orderData.expireAt) < new Date()) {
      await (order as any).update({ status: 3 });
      this.ctx.throw(400, '订单已过期，请重新下单');
    }

    const existedSuccess = await this.ctx.model.MemberPayment.findOne({
      where: { orderId, status: 1 },
    });
    if (existedSuccess) this.ctx.throw(400, '订单已支付');

    const paymentNo = this._genPaymentNo();
    const payment = await this.ctx.model.MemberPayment.create({
      paymentNo, orderId, userId, provider,
      amount: orderData.amount, status: 0,
    });

    const providerImpl = getPaymentProvider(provider);
    const baseUrl = (this.app.config as any).appConfig?.baseUrl || 'http://localhost:7001';
    const notifyUrl = `${baseUrl}/api/payments/${provider}/notify`;
    const prepayResult = await providerImpl.createPrepay({
      paymentNo,
      amount: Number(orderData.amount),
      subject: `${orderData.planSnapshot?.name || orderData.planCode} 会员订阅`,
      userId, notifyUrl,
    });

    await (payment as any).update({ prepayData: prepayResult.prepayData });

    return {
      paymentNo,
      paymentId: (payment as any).id,
      provider,
      prepayData: prepayResult.prepayData,
      cashierUrl: prepayResult.cashierUrl,
    };
  }

  /**
   * 处理支付成功回调（统一入口）
   * 关键：行级锁 + 状态机校验 + 事务，确保多次回调幂等
   */
  async handleCallback(provider: ProviderCode, headers: Record<string, string>, rawBody: string) {
    const providerImpl = getPaymentProvider(provider);
    const verify = await providerImpl.verifyCallback(headers, rawBody);
    if (!verify.success) {
      this.ctx.logger.warn(`[payment.handleCallback] verify failed: ${verify.error}`);
      this.ctx.throw(400, `支付回调验签失败: ${verify.error}`);
    }

    const payment = await this.ctx.model.MemberPayment.findOne({
      where: { paymentNo: verify.paymentNo },
    });
    if (!payment) this.ctx.throw(404, '支付流水不存在');
    const paymentData = (payment as any).toJSON();

    // 幂等
    if (paymentData.status === 1) {
      return { success: true, skipped: true };
    }

    // 金额校验
    if (Math.abs(Number(paymentData.amount) - verify.amount) > 0.001) {
      this.ctx.throw(400, `金额不一致：期望 ${paymentData.amount}，回调 ${verify.amount}`);
    }

    // 事务：行级锁 + 状态机更新
    let orderForActivation: any = null;
    await this.ctx.model.transaction(async (t: any) => {
      const lockedPayment = await this.ctx.model.MemberPayment.findOne({
        where: { id: paymentData.id }, lock: t.LOCK.UPDATE, transaction: t,
      });
      if ((lockedPayment as any).status === 1) return;

      const lockedOrder = await this.ctx.model.MemberOrder.findOne({
        where: { id: paymentData.orderId }, lock: t.LOCK.UPDATE, transaction: t,
      });

      await (lockedPayment as any).update({
        status: 1,
        providerTradeNo: verify.providerTradeNo,
        callbackPayload: verify.rawPayload,
        paidAt: new Date(),
      }, { transaction: t });

      await (lockedOrder as any).update({
        status: 1, paidAt: new Date(),
      }, { transaction: t });

      orderForActivation = (lockedOrder as any).toJSON();
    });

    if (!orderForActivation) return { success: true, skipped: true };

    // 在事务外调 activatePaidPlan（其内部含独立事务 + 通知）
    await this.ctx.service.member.activatePaidPlan(
      paymentData.userId,
      orderForActivation.planCode,
      { orderId: orderForActivation.id },
    );

    // 触发支付成功通知
    try {
      const updatedMember = await this.ctx.model.UserMember.findOne({
        where: { userId: paymentData.userId },
      });
      const expireAt = (updatedMember as any).paidExpireAt
        ? new Date((updatedMember as any).paidExpireAt).toISOString().slice(0, 10)
        : '永久';
      await (this.ctx.service.notification as any).core.send({
        typeCode: 'BUSINESS_PAYMENT_SUCCESS',
        userId: paymentData.userId,
        variables: {
          orderNo: orderForActivation.orderNo,
          planName: orderForActivation.planSnapshot?.name || orderForActivation.planCode,
          amount: Number(orderForActivation.amount).toFixed(2),
          expireAt,
        },
      });
    } catch (e: any) {
      this.ctx.logger.warn(`[payment.handleCallback] payment_success notification failed: ${e.message}`);
    }

    return { success: true, skipped: false };
  }

  /** 处理支付失败回调 */
  async markFailed(paymentNo: string, reason: string) {
    const payment = await this.ctx.model.MemberPayment.findOne({ where: { paymentNo } });
    if (!payment) this.ctx.throw(404, '支付流水不存在');
    if ((payment as any).status === 1) return;

    await (payment as any).update({ status: 2, failedReason: reason });

    // 取订单 orderNo 用于通知
    const order = await this.ctx.model.MemberOrder.findByPk((payment as any).orderId);
    try {
      await (this.ctx.service.notification as any).core.send({
        typeCode: 'BUSINESS_PAYMENT_FAIL',
        userId: (payment as any).userId,
        variables: {
          orderNo: (order as any)?.orderNo || '',
          reason,
        },
      });
    } catch (e: any) {
      this.ctx.logger.warn(`[payment.markFailed] notification failed: ${e.message}`);
    }
  }

  /** 查支付状态（前端轮询用） */
  async getStatus(paymentNo: string, userId: number) {
    const payment = await this.ctx.model.MemberPayment.findOne({
      where: { paymentNo, userId },
    });
    if (!payment) this.ctx.throw(404, '支付流水不存在');
    const data = (payment as any).toJSON();
    return {
      paymentNo: data.paymentNo,
      orderId: data.orderId,
      status: data.status,
      paidAt: data.paidAt,
      failedReason: data.failedReason,
    };
  }

  private _genPaymentNo(): string {
    const now = new Date();
    const ymd = now.getFullYear().toString()
      + String(now.getMonth() + 1).padStart(2, '0')
      + String(now.getDate()).padStart(2, '0');
    return `MP${ymd}${Date.now().toString().slice(-6)}${Math.floor(Math.random() * 10000).toString().padStart(4, '0')}`;
  }
}
```

### 4.3 `app/service/member.ts` 微改（T5）

仅扩展 `activatePaidPlan` 接受第三个参数，将 orderId 写入 PointsLog 的 `bizId`：

```typescript
async activatePaidPlan(userId: number, planCode: string, extra?: { orderId?: number }) {
  // ... 原有逻辑不变 ...

  // 赠送积分时改为：
  if (planData.giftPoints > 0 || planData.giftGrowth > 0) {
    await this.addPoints({
      userId,
      points: planData.giftPoints || 0,
      growthDelta: planData.giftGrowth || 0,
      source: 'paid_gift',
      bizType: 'order',                                          // 改：subscription → order
      bizId: extra?.orderId ? String(extra.orderId) : planCode,  // 改：planCode → orderId
      remark: `开通${planData.name}赠送`,
    });
  }
  // ... 通知 BUSINESS_PLAN_ACTIVATED variables 加 orderId ...
}
```

**兼容性**：`extra` 是 optional，原有签到/管理员手动开通调用方零改动。

---

## 5. PaymentProvider 适配器层

### 5.1 `app/lib/payment/provider.ts`（接口）

见 § 2.3，完整代码已列出。

### 5.2 `app/lib/payment/mock.ts`（MockProvider 实装）

```typescript
import { PaymentProvider, PrepayInput, PrepayResult, VerifyResult } from './provider';

export class MockProvider implements PaymentProvider {
  readonly code = 'mock' as const;

  async createPrepay(input: PrepayInput): Promise<PrepayResult> {
    return {
      prepayData: {
        mockToken: `mock_${input.paymentNo}_${Date.now()}`,
        amount: input.amount,
        subject: input.subject,
      },
      cashierUrl: `/member/cashier?paymentNo=${input.paymentNo}`,
    };
  }

  async verifyCallback(_headers: Record<string, string>, rawBody: string): Promise<VerifyResult> {
    let payload: any;
    try {
      payload = JSON.parse(rawBody);
    } catch {
      return { success: false, paymentNo: '', providerTradeNo: '', amount: 0, rawPayload: {}, error: 'invalid JSON' };
    }
    if (!payload.paymentNo) {
      return { success: false, paymentNo: '', providerTradeNo: '', amount: 0, rawPayload: payload, error: 'missing paymentNo' };
    }
    if (typeof payload.amount !== 'number') {
      return { success: false, paymentNo: payload.paymentNo, providerTradeNo: '', amount: 0, rawPayload: payload, error: 'missing/invalid amount' };
    }
    return {
      success: true,
      paymentNo: payload.paymentNo,
      providerTradeNo: payload.providerTradeNo || `MOCK_${Date.now()}`,
      amount: payload.amount,
      rawPayload: payload,
    };
  }

  async queryStatus(_providerTradeNo: string): Promise<'pending' | 'success' | 'failed'> {
    return 'pending';
  }
}
```

### 5.3 `app/lib/payment/wechat-jsapi.ts` / `wechat-native.ts`（占位）

```typescript
import { PaymentProvider, PrepayInput, PrepayResult, VerifyResult } from './provider';

export class WechatJsapiProvider implements PaymentProvider {
  readonly code = 'wechat_jsapi' as const;
  async createPrepay(_input: PrepayInput): Promise<PrepayResult> {
    throw new Error('NotImplemented: 微信 JSAPI 支付待接入。需要：商户号 + AppID + APIv3 密钥 + 平台证书 + IP 白名单 + 内网穿透回调。');
  }
  async verifyCallback(): Promise<VerifyResult> { throw new Error('NotImplemented: WechatJsapiProvider.verifyCallback'); }
  async queryStatus(): Promise<'pending'|'success'|'failed'> { throw new Error('NotImplemented: WechatJsapiProvider.queryStatus'); }
}
```

`wechat-native.ts` 同样模式。

### 5.4 `app/lib/payment/factory.ts` + `index.ts`

```typescript
// factory.ts
import { PaymentProvider, ProviderCode } from './provider';
import { MockProvider } from './mock';
import { WechatJsapiProvider } from './wechat-jsapi';
import { WechatNativeProvider } from './wechat-native';

const providers: Record<ProviderCode, () => PaymentProvider> = {
  mock: () => new MockProvider(),
  wechat_jsapi: () => new WechatJsapiProvider(),
  wechat_native: () => new WechatNativeProvider(),
  alipay: () => { throw new Error('Alipay provider not implemented yet'); },
};

export function getPaymentProvider(code: ProviderCode): PaymentProvider {
  const factory = providers[code];
  if (!factory) throw new Error(`Unknown payment provider: ${code}`);
  return factory();
}

// index.ts
export * from './provider';
export * from './factory';
```

---

## 6. Controller 与路由

### 6.1 `app/controller/order.ts`（C 端）

```typescript
import BaseController from './base';

export default class OrderController extends BaseController {
  /** POST /api/orders */
  async create() {
    this.validate({ planCode: { type: 'string', required: true } });
    const userId = (this.ctx.state.user as any).id;
    const { planCode, remark } = this.ctx.request.body;
    const data = await this.service.order.create({ userId, planCode, remark });
    this.success(data, '下单成功');
  }

  /** GET /api/orders */
  async list() {
    const userId = (this.ctx.state.user as any).id;
    const pagination = this.getPagination();
    const { status } = this.ctx.query;
    const result = await this.service.order.listByUser(userId, {
      ...pagination,
      status: status !== undefined ? Number(status) : undefined,
    });
    this.paginated(result);
  }

  /** GET /api/orders/:id */
  async detail() {
    const userId = (this.ctx.state.user as any).id;
    const data = await this.service.order.detail(Number(this.ctx.params.id), userId);
    this.success(data);
  }

  /** POST /api/orders/:id/cancel */
  async cancel() {
    const userId = (this.ctx.state.user as any).id;
    const data = await this.service.order.cancel(Number(this.ctx.params.id), userId);
    this.success(data, '已取消');
  }
}
```

### 6.2 `app/controller/payment.ts`

```typescript
import BaseController from './base';

export default class PaymentController extends BaseController {
  /** POST /api/payments */
  async create() {
    this.validate({
      orderId: { type: 'number', required: true },
      provider: { type: 'string', required: true },
    });
    const userId = (this.ctx.state.user as any).id;
    const { orderId, provider } = this.ctx.request.body;
    const data = await this.service.payment.create({ orderId, userId, provider });
    this.success(data, '已生成支付订单');
  }

  /** GET /api/payments/:paymentNo/status */
  async status() {
    const userId = (this.ctx.state.user as any).id;
    const data = await this.service.payment.getStatus(this.ctx.params.paymentNo, userId);
    this.success(data);
  }

  /**
   * POST /api/payments/mock/notify — Mock 内部回调（不挂 auth）
   * Body: { paymentNo, amount, providerTradeNo?, status?: 'success'|'failed', failReason? }
   */
  async mockNotify() {
    const body = this.ctx.request.body as any;
    if (!body.paymentNo) this.ctx.throw(400, 'paymentNo required');

    if (body.status === 'failed') {
      await this.service.payment.markFailed(body.paymentNo, body.failReason || '用户取消支付');
      this.success({ accepted: true });
      return;
    }

    const rawBody = JSON.stringify({
      paymentNo: body.paymentNo,
      amount: body.amount,
      providerTradeNo: body.providerTradeNo,
    });
    const result = await this.service.payment.handleCallback('mock', this.ctx.request.headers as any, rawBody);
    this.success(result);
  }

  /** POST /api/payments/wechat/notify — 占位 */
  async wechatNotify() {
    this.ctx.throw(501, '微信支付未接入');
  }
}
```

### 6.3 `app/controller/admin/order.ts`

```typescript
import BaseController from '../base';

export default class AdminOrderController extends BaseController {
  /** GET /api/admin/member/orders */
  async list() {
    const pagination = this.getPagination();
    const { userId, status, startDate, endDate } = this.ctx.query;
    const result = await this.service.order.listAll({
      ...pagination,
      userId: userId ? Number(userId) : undefined,
      status: status !== undefined ? Number(status) : undefined,
      startDate: startDate as string,
      endDate: endDate as string,
    });
    this.paginated(result);
  }

  /** GET /api/admin/member/orders/stats */
  async stats() {
    const { startDate, endDate } = this.ctx.query;
    const data = await this.service.order.stats({
      startDate: startDate as string,
      endDate: endDate as string,
    });
    this.success(data);
  }

  /** GET /api/admin/member/orders/:id */
  async detail() {
    const data = await this.service.order.detail(Number(this.ctx.params.id));
    this.success(data);
  }
}
```

### 6.4 路由注册（`app/router.ts` 增量）

在 `// ==================== 会员体系（C 端） ====================` 段后插入：

```typescript
// ==================== 订单（C 端） ====================
router.post('/api/orders', auth, controller.order.create);
router.get('/api/orders', auth, controller.order.list);
router.get('/api/orders/:id', auth, controller.order.detail);
router.post('/api/orders/:id/cancel', auth, controller.order.cancel);

// ==================== 支付（C 端） ====================
router.post('/api/payments', auth, controller.payment.create);
router.get('/api/payments/:paymentNo/status', auth, controller.payment.status);
router.post('/api/payments/mock/notify', controller.payment.mockNotify);   // 不挂 auth
router.post('/api/payments/wechat/notify', controller.payment.wechatNotify); // 占位
```

在 `// ==================== 会员管理（管理端） ====================` 段末（pointsLogs 之后）插入：

```typescript
// 订单管理（精确路径 /stats 必须在 /:id 之前）
router.get('/api/admin/member/orders/stats', auth, perm('member:order:stats'), (controller.admin as any).order.stats);
router.get('/api/admin/member/orders', auth, perm('member:order:list'), (controller.admin as any).order.list);
router.get('/api/admin/member/orders/:id', auth, perm('member:order:detail'), (controller.admin as any).order.detail);
```

---

## 7. 调度任务

### 7.1 `app/schedule/orderExpireCheck.ts`（订单超时清理）

```typescript
import { Subscription } from 'egg';

export default class OrderExpireCheck extends Subscription {
  static get schedule() {
    return {
      interval: '5m',
      type: 'worker' as const,
      immediate: false,
      disable: false,
    };
  }

  async subscribe() {
    try {
      const affected = await this.ctx.service.order.cleanExpired();
      if (affected > 0) {
        this.ctx.logger.info(`[Schedule:OrderExpireCheck] 自动过期订单数: ${affected}`);
      }
    } catch (err) {
      this.ctx.logger.error('[Schedule:OrderExpireCheck] 失败:', err);
    }
  }
}
```

### 7.2 `app/schedule/memberExpireCheck.ts`（到期提醒）

```typescript
import { Subscription } from 'egg';

export default class MemberExpireCheck extends Subscription {
  static get schedule() {
    return {
      cron: '0 0 9 * * *',  // 每天 09:00:00
      type: 'worker' as const,
      immediate: false,
      disable: false,
    };
  }

  async subscribe() {
    try {
      await this._scanExpireSoon(7);
      await this._scanExpireSoon(1);
      await this._scanExpired();
    } catch (err) {
      this.ctx.logger.error('[Schedule:MemberExpireCheck] 失败:', err);
    }
  }

  /**
   * 扫描 daysAhead 天后到期的会员
   * 幂等键: member_expire_${userId}_${expireYmd}_${stage}
   */
  private async _scanExpireSoon(daysAhead: number) {
    const { Op } = require('sequelize');
    const start = new Date();
    start.setDate(start.getDate() + daysAhead);
    start.setHours(0, 0, 0, 0);
    const end = new Date(start);
    end.setHours(23, 59, 59, 999);

    const members = await this.ctx.model.UserMember.findAll({
      where: {
        isPaid: 1,
        paidExpireAt: { [Op.between]: [start, end] },
      },
    });

    const stage = `${daysAhead}d`;
    for (const m of members) {
      const data = (m as any).toJSON();
      const expireYmd = new Date(data.paidExpireAt).toISOString().slice(0, 10);
      const idempotentKey = `member_expire_${data.userId}_${expireYmd}_${stage}`;

      // 取套餐名（best-effort，失败则降级显示 code）
      let planName = data.paidPlanCode;
      try {
        const plan = await this.ctx.model.PaidPlan.findOne({ where: { code: data.paidPlanCode } });
        if (plan) planName = (plan as any).name;
      } catch { /* ignore */ }

      try {
        await (this.ctx.service.notification as any).core.send({
          typeCode: 'BUSINESS_MEMBER_EXPIRE_SOON',
          userId: data.userId,
          variables: { stage, daysLeft: daysAhead, planName, expireAt: expireYmd },
          idempotentKey,
        });
      } catch (e: any) {
        this.ctx.logger.warn(`[MemberExpireCheck] notify ${data.userId} fail: ${e.message}`);
      }
    }
    this.ctx.logger.info(`[MemberExpireCheck] expire-soon stage=${stage} scanned=${members.length}`);
  }

  /**
   * 扫描过去 1 天到现在已过期的会员，置 is_paid=0 + 通知
   */
  private async _scanExpired() {
    const { Op } = require('sequelize');
    const yesterday = new Date();
    yesterday.setDate(yesterday.getDate() - 1);
    yesterday.setHours(0, 0, 0, 0);
    const now = new Date();

    const members = await this.ctx.model.UserMember.findAll({
      where: {
        isPaid: 1,
        paidExpireAt: { [Op.between]: [yesterday, now] },
      },
    });

    for (const m of members) {
      const data = (m as any).toJSON();
      await (m as any).update({ isPaid: 0 });

      const expireYmd = new Date(data.paidExpireAt).toISOString().slice(0, 10);
      const idempotentKey = `member_expired_${data.userId}_${expireYmd}`;

      let planName = data.paidPlanCode;
      try {
        const plan = await this.ctx.model.PaidPlan.findOne({ where: { code: data.paidPlanCode } });
        if (plan) planName = (plan as any).name;
      } catch { /* ignore */ }

      try {
        await (this.ctx.service.notification as any).core.send({
          typeCode: 'BUSINESS_MEMBER_EXPIRED',
          userId: data.userId,
          variables: { planName, expireAt: expireYmd },
          idempotentKey,
        });
      } catch (e: any) {
        this.ctx.logger.warn(`[MemberExpireCheck] expired-notify ${data.userId} fail: ${e.message}`);
      }
    }
    this.ctx.logger.info(`[MemberExpireCheck] expired scanned=${members.length}`);
  }
}
```

**幂等说明**：`notification.core.send` 的 `idempotentKey` 写到 `notification_send_logs` 唯一索引，重复键自动跳过（018 已实装）。

### 7.3 测试触发器（仅开发期）

可选：增加管理端接口 `POST /api/admin/member/expire-check/trigger`（仅 super_admin 用），手动触发扫描，便于联调。**MVP 范围内实装**，权限码 `system:dev:trigger-schedule`（admin 自动具备）。

---

## 8. 后端 Models

### 8.1 `app/model/member_order.ts`

```typescript
import { Application } from 'egg';
import { DataTypes } from 'sequelize';

export default (app: Application) => {
  const { STRING, INTEGER, BIGINT, TINYINT, DECIMAL, JSON: JSONTYPE, DATE } = DataTypes;

  const MemberOrder = app.model.define('MemberOrder', {
    id: { type: BIGINT.UNSIGNED, autoIncrement: true, primaryKey: true },
    orderNo: { type: STRING(32), allowNull: false, unique: true, field: 'order_no' },
    userId: { type: BIGINT.UNSIGNED, allowNull: false, field: 'user_id' },
    planId: { type: INTEGER.UNSIGNED, allowNull: false, field: 'plan_id' },
    planCode: { type: STRING(30), allowNull: false, field: 'plan_code' },
    planSnapshot: { type: JSONTYPE, allowNull: false, field: 'plan_snapshot' },
    amount: { type: DECIMAL(10, 2), allowNull: false },
    status: { type: TINYINT.UNSIGNED, defaultValue: 0 },
    scene: { type: TINYINT.UNSIGNED, defaultValue: 1 },
    paidAt: { type: DATE, allowNull: true, field: 'paid_at' },
    cancelledAt: { type: DATE, allowNull: true, field: 'cancelled_at' },
    expireAt: { type: DATE, allowNull: false, field: 'expire_at' },
    remark: { type: STRING(200), allowNull: true },
  }, {
    tableName: 'member_orders',
    timestamps: true,
    underscored: true,
  });

  (MemberOrder as any).associate = () => {
    MemberOrder.belongsTo(app.model.User, { foreignKey: 'user_id', as: 'user' });
    MemberOrder.hasMany(app.model.MemberPayment, { foreignKey: 'order_id', as: 'payments' });
    MemberOrder.belongsTo(app.model.PaidPlan, { foreignKey: 'plan_id', as: 'plan' });
  };

  return MemberOrder;
};
```

### 8.2 `app/model/member_payment.ts`

```typescript
import { Application } from 'egg';
import { DataTypes } from 'sequelize';

export default (app: Application) => {
  const { STRING, BIGINT, TINYINT, DECIMAL, JSON: JSONTYPE, DATE } = DataTypes;

  const MemberPayment = app.model.define('MemberPayment', {
    id: { type: BIGINT.UNSIGNED, autoIncrement: true, primaryKey: true },
    paymentNo: { type: STRING(32), allowNull: false, unique: true, field: 'payment_no' },
    orderId: { type: BIGINT.UNSIGNED, allowNull: false, field: 'order_id' },
    userId: { type: BIGINT.UNSIGNED, allowNull: false, field: 'user_id' },
    provider: { type: STRING(20), allowNull: false },
    providerTradeNo: { type: STRING(64), allowNull: true, field: 'provider_trade_no' },
    amount: { type: DECIMAL(10, 2), allowNull: false },
    status: { type: TINYINT.UNSIGNED, defaultValue: 0 },
    prepayData: { type: JSONTYPE, allowNull: true, field: 'prepay_data' },
    callbackPayload: { type: JSONTYPE, allowNull: true, field: 'callback_payload' },
    paidAt: { type: DATE, allowNull: true, field: 'paid_at' },
    failedReason: { type: STRING(500), allowNull: true, field: 'failed_reason' },
  }, {
    tableName: 'member_payments',
    timestamps: true,
    underscored: true,
  });

  (MemberPayment as any).associate = () => {
    MemberPayment.belongsTo(app.model.MemberOrder, { foreignKey: 'order_id', as: 'order' });
  };

  return MemberPayment;
};
```

---

## 9. H5 端实现

### 9.1 服务层

#### 9.1.1 `service/payment.ts`（新增）

```typescript
import { request } from '@/utils';
import type { ApiResult } from '../types/auth';
import type { Order, Payment, OrderListItem } from '../types/order';

const API_BASE = '/api';

// ==================== 订单 ====================
export const createOrder = (planCode: string, remark?: string)
  : Promise<ApiResult<{ orderId: number; orderNo: string; amount: string; planName: string; expireAt: string; scene: 1|2 }>> =>
  request.post(`${API_BASE}/orders`, { planCode, remark });

export const getOrder = (id: number): Promise<ApiResult<Order>> =>
  request.get(`${API_BASE}/orders/${id}`);

export const listMyOrders = (params: { page?: number; pageSize?: number; status?: number })
  : Promise<ApiResult<{ list: OrderListItem[]; total: number; page: number; pageSize: number; totalPages: number }>> =>
  request.get(`${API_BASE}/orders`, { params });

export const cancelOrder = (id: number): Promise<ApiResult<{ orderId: number; status: number }>> =>
  request.post(`${API_BASE}/orders/${id}/cancel`);

// ==================== 支付 ====================
export const createPayment = (orderId: number, provider: 'mock' | 'wechat_jsapi')
  : Promise<ApiResult<{ paymentNo: string; paymentId: number; provider: string; prepayData: any; cashierUrl?: string }>> =>
  request.post(`${API_BASE}/payments`, { orderId, provider });

export const getPaymentStatus = (paymentNo: string)
  : Promise<ApiResult<Payment>> =>
  request.get(`${API_BASE}/payments/${paymentNo}/status`);

// ==================== Mock 内部 ====================
export const mockNotify = (body: { paymentNo: string; amount: number; status?: 'success'|'failed'; failReason?: string })
  : Promise<ApiResult<any>> =>
  request.post(`${API_BASE}/payments/mock/notify`, body);
```

#### 9.1.2 `service/member.ts` 扩展

新增 plans 列表与会员状态接口（其实复用已有接口，仅补类型导出）：

```typescript
export const getMemberPlans = (): Promise<ApiResult<PaidPlan[]>> =>
  request.get(`${API_BASE}/member/plans`);

// getMemberInfo 已存在；补类型字段（types/auth 中 MemberInfo 扩展）
```

#### 9.1.3 `types/order.ts`（新增）

```typescript
export interface PaidPlan {
  id: number;
  name: string;
  code: string;
  durationDays: number;
  price: string;
  originalPrice: string;
  benefits?: Record<string, any>;
  giftPoints: number;
  giftGrowth: number;
  description?: string;
  sort: number;
  status: 0 | 1;
}

export type OrderStatus = 0 | 1 | 2 | 3 | 4;
export type PaymentStatus = 0 | 1 | 2 | 3;

export interface OrderListItem {
  id: number;
  orderNo: string;
  planCode: string;
  amount: string;
  status: OrderStatus;
  scene: 1 | 2;
  paidAt?: string;
  expireAt: string;
  createdAt: string;
}

export interface Order extends OrderListItem {
  planSnapshot: PaidPlan;
  cancelledAt?: string;
  remark?: string;
  payments?: Payment[];
}

export interface Payment {
  id: number;
  paymentNo: string;
  orderId: number;
  provider: string;
  providerTradeNo?: string;
  amount: string;
  status: PaymentStatus;
  paidAt?: string;
  failedReason?: string;
  createdAt: string;
}
```

### 9.2 页面：`pages/member/index.tsx`（重写）

**布局**：
```
┌─────────────────────────────────┐
│ AppHeader: 会员                 │
├─────────────────────────────────┤
│ ┌─ 当前会员状态卡 ────────────┐ │
│ │ 头像 用户名                 │ │
│ │ ⭐ 黄金会员                 │ │
│ │ 付费状态: 月度会员（剩 5天）│ │  ← 已付费时显
│ │ 或：未开通付费会员           │ │  ← 未付费时显
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ 选择套餐                         │
│ ┌─────┐  ┌─────┐  ┌─────┐ ┌──┐ │
│ │ 月卡│  │ 季卡│  │ 年卡│ │终身│ │
│ │¥6.8 │  │¥16.8│  │¥39.9│ │¥99│ │
│ │原¥9.9│ │原¥29│  │原¥96│ │原¥199│
│ └─────┘  └─────┘  └─────┘ └──┘ │
│ * 当前为月度会员时：季卡/年卡/终身禁用，按钮文案「当前已订阅」│
├─────────────────────────────────┤
│ [立即订阅 ¥6.8]                 │  ← 同套餐：「立即续费 ¥6.8」
└─────────────────────────────────┘
↑ 底部固定按钮
└─ 「我的订单」 (右上角图标，跳 /member/orders)
```

**逻辑要点**：
- `useEffect`: 并行 `getMemberInfo()` + `getMemberPlans()`
- 卡片状态分三类：
  - 未付费：所有卡片可点击，按钮文案"立即订阅"
  - 已付费同套餐 → "立即续费（剩 X 天）"
  - 已付费其他套餐 → 灰底禁用，按钮置灰
- 点击订阅 → `createOrder(planCode)` → 拿 orderId → `createPayment(orderId, 'mock')` → 跳 `cashierUrl`
- 错误处理：
  - 后端 400 "您有未完成订单 MOxxx" → 弹 `AppModal` 询问是否跳订单详情
  - 后端 400 "您当前为「xx」套餐" → toast 提示

### 9.3 页面：`pages/member/cashier/index.tsx`（新）

**布局**：
```
┌─────────────────────────────────┐
│ AppHeader: 收银台                │
├─────────────────────────────────┤
│ ┌─────────────────────────┐    │
│ │ 订单号: MOxxx           │    │
│ │ 套餐: 月度会员           │    │
│ │ 金额: ¥6.80             │    │
│ │ 支付方式: 微信支付（Mock）│   │
│ └─────────────────────────┘    │
├─────────────────────────────────┤
│ ⏱️ 倒计时：29:55                │
│                                 │
│ [✅ 模拟支付成功]                │
│                                 │
│ [❌ 模拟支付失败]                │
│                                 │
│ [⏳ 等待回调（前端不动作）]      │
│                                 │
│ <仅开发期：实际生产接 JSAPI>    │
└─────────────────────────────────┘
```

**逻辑要点**：
- URL 参数 `?paymentNo=xxx`
- `useEffect`: `getPaymentStatus(paymentNo)` 拉支付详情，根据 `orderId` 拉订单（拼装显示）
- 倒计时：基于订单 `expireAt`，到 0 跳订单详情页 + toast "订单已过期"
- 三按钮：
  - **模拟成功**：`mockNotify({paymentNo, amount, status: 'success'})` → 成功后跳 `/member/orders/:id` 详情页
  - **模拟失败**：`mockNotify({...status: 'failed', failReason: '用户取消'})` → 跳详情页（显示失败状态 + 重新支付按钮）
  - **等待回调**：不发请求，开始 3s 轮询 `getPaymentStatus`，10 次内若变成 success 跳详情；超时回到当前页提示"超时未收到回调"
- 防误操作：`AppModal` 二次确认

### 9.4 页面：`pages/member/orders/index.tsx`（新）

**布局**：
```
┌─────────────────────────────────┐
│ AppHeader: 我的订单              │
├─────────────────────────────────┤
│ [全部] [待支付] [已支付] [已取消] │  ← Tab 筛选
├─────────────────────────────────┤
│ ┌─ 订单卡 ──────────────────┐  │
│ │ MOxxx  待支付              │  │
│ │ 月度会员  ¥6.80            │  │
│ │ 2026-05-23 15:00            │  │
│ │ [立即支付] [取消订单]        │  │  ← status=0
│ └─────────────────────────────┘  │
│ ┌─ 订单卡 ──────────────────┐  │
│ │ MOxxx  ✓ 已支付            │  │
│ │ 年度会员  ¥39.90           │  │
│ │ 2026-05-21 09:23           │  │
│ └─────────────────────────────┘  │
│ ...                              │
├─────────────────────────────────┤
│ 下拉刷新 / 上拉加载更多          │
└─────────────────────────────────┘
```

**逻辑要点**：
- Tab 切换 → `setStatus + listMyOrders`
- 卡片点击整体 → 跳详情页
- "立即支付" → `createPayment` → 跳收银台
- "取消订单" → `AppModal` 确认 → `cancelOrder(id)` → 刷新列表
- 上拉加载用现有 `IntersectionObserver` 模式（参考 `pages/notifications`）

### 9.5 页面：`pages/member/orders/[id]/index.tsx`（新）

**布局**：
```
┌─────────────────────────────────┐
│ AppHeader: 订单详情              │
├─────────────────────────────────┤
│ 状态: 已支付 ✓                   │
│                                 │
│ 订单号: MOxxx                   │
│ 套餐: 月度会员                   │
│ 金额: ¥6.80                     │
│ 创建时间: 2026-05-23 15:00      │
│ 支付时间: 2026-05-23 15:02      │
│                                 │
├─ 支付流水 ──────────────────────┤
│ #1  Mock  成功  ¥6.80           │
│     2026-05-23 15:02            │
├─────────────────────────────────┤
│ <底部按钮区>                     │
│ status=0: [立即支付] [取消订单]   │
│ status=2失败: [重新支付]         │
│ status=1已付: <无>               │
└─────────────────────────────────┘
```

### 9.6 `pages/mine/index.tsx` 改动

在"我的"页菜单中，在合适位置（如"我的反馈"上方）增加：

```tsx
<div className="mine__menu-item" onClick={() => safeNavigate('/member/orders')}>
  <Icon name="order" />
  <span>我的订单</span>
  <Icon name="arrow-right" />
</div>
```

### 9.7 路由注册（`routes.config.ts`）

```typescript
// 在 member 二级页之后插入：
{ path: '/member/cashier', component: '@/pages/member/cashier', title: '收银台' },
{ path: '/member/orders/:id', component: '@/pages/member/orders/[id]', title: '订单详情' },
{ path: '/member/orders', component: '@/pages/member/orders', title: '我的订单' },
// /member 已存在
```

### 9.8 Store：`store/order.ts`（新）

最小化（仅缓存当前 orders 列表，避免重复请求）：

```typescript
import { atom, useAtom } from 'jotai';
import type { OrderListItem } from '../types/order';

const ordersAtom = atom<OrderListItem[]>([]);
const ordersLoadingAtom = atom(false);

export const useOrders = () => {
  const [orders, setOrders] = useAtom(ordersAtom);
  const [loading, setLoading] = useAtom(ordersLoadingAtom);
  return { orders, setOrders, loading, setLoading };
};
```

---

## 10. 管理端实现

### 10.1 路由：`config/routes/modules/member.ts` 增量

```typescript
{
  path: '/member/orders',
  component: '@/pages/Member/Orders',
  wrappers: ['@/components/AuthWrapper'],
},
```

### 10.2 Service：`src/services/order.ts`（新增）

```typescript
import request from '@/utils/request';

export interface AdminOrder {
  id: number;
  orderNo: string;
  userId: number;
  planId: number;
  planCode: string;
  planSnapshot: any;
  amount: string;
  status: 0 | 1 | 2 | 3 | 4;
  scene: 1 | 2;
  paidAt?: string;
  cancelledAt?: string;
  expireAt: string;
  remark?: string;
  createdAt: string;
  user?: { id: number; username: string; nickname?: string; phone?: string; email?: string };
}

export interface OrderListQuery {
  page?: number;
  pageSize?: number;
  userId?: number;
  status?: number;
  startDate?: string;
  endDate?: string;
}

export interface OrderStats {
  totalOrders: number;
  paidOrders: number;
  payRate: number;
  totalRevenue: number;
}

export async function listOrders(params?: OrderListQuery) {
  return request('/api/admin/member/orders', { params });
}

export async function getOrder(id: number) {
  return request(`/api/admin/member/orders/${id}`);
}

export async function getOrderStats(params?: { startDate?: string; endDate?: string }) {
  return request('/api/admin/member/orders/stats', { params });
}
```

### 10.3 页面：`pages/Member/Orders/index.tsx`（订单列表）

**布局**：
```
┌─ Card title="会员订单" ────────────────────────────┐
│ 统计: [总订单 200] [已支付 150] [付费率 75%] [营收 ¥3000] │
│ ─────────────────────────────────────────────  │
│ 筛选: [用户ID] [状态] [日期范围] [查询] [重置]    │
│ ─────────────────────────────────────────────  │
│ ┌─Table───────────────────────────────────────┐ │
│ │订单号  用户  套餐  金额  状态  支付时间  操作│ │
│ │MO...  alice 月卡 ¥6.8 已付  ...     [详情] │ │
│ └─────────────────────────────────────────────┘ │
│ 分页                                            │
└─────────────────────────────────────────────────┘
```

**列定义**：

| 列 | dataIndex | 渲染 |
|---|---|---|
| 订单号 | orderNo | 文本（mono） |
| 用户 | user | `${username}(#${userId})` 或 `#${userId}` |
| 套餐 | planSnapshot.name / planCode | 优先 name |
| 金额 | amount | `¥xxx.xx`（formatCurrency） |
| 状态 | status | Tag（待支付灰/已支付绿/已取消橙/已过期红/已退款紫） |
| 场景 | scene | 1=新购 / 2=续费 |
| 创建时间 | createdAt | datetime |
| 支付时间 | paidAt | datetime / `-` |
| 操作 | - | 详情（仅一个按钮） |

**实现要点**：
- 状态映射常量集中在 `utils/orderFormat.ts`
- 复用 C2b 的 `formatCurrency` / `formatDateTime`
- `DetailDrawer` 显示订单基础信息 + 套餐快照（JSON.stringify 折叠）+ 支付流水（嵌入 Table）
- **本 MVP 仅只读，不提供任何写操作**

### 10.4 测试：`services/__tests__/order.test.tsx`（≥ 5 case）

```typescript
import { listOrders, getOrder, getOrderStats } from '@/services/order';

jest.mock('@/utils/request');
import request from '@/utils/request';

describe('admin order service', () => {
  beforeEach(() => jest.clearAllMocks());

  it('listOrders → GET /api/admin/member/orders', async () => {
    await listOrders({ page: 1, pageSize: 20, status: 1 });
    expect(request).toHaveBeenCalledWith('/api/admin/member/orders', {
      params: { page: 1, pageSize: 20, status: 1 },
    });
  });

  it('getOrder → GET /api/admin/member/orders/:id', async () => {
    await getOrder(7);
    expect(request).toHaveBeenCalledWith('/api/admin/member/orders/7');
  });

  it('getOrderStats → GET /api/admin/member/orders/stats', async () => {
    await getOrderStats({ startDate: '2026-05-01', endDate: '2026-05-23' });
    expect(request).toHaveBeenCalledWith('/api/admin/member/orders/stats', {
      params: { startDate: '2026-05-01', endDate: '2026-05-23' },
    });
  });

  it('listOrders 不传参数 → params 为 undefined', async () => {
    await listOrders();
    expect(request).toHaveBeenCalledWith('/api/admin/member/orders', { params: undefined });
  });

  it('listOrders 仅传 userId', async () => {
    await listOrders({ userId: 5 });
    expect(request).toHaveBeenCalledWith('/api/admin/member/orders', {
      params: { userId: 5 },
    });
  });
});
```

---

## 11. 测试矩阵

### 11.1 后端单测

#### 11.1.1 `service/__tests__/order.test.ts`（≥ 6 case）

- `create` 正常下单 → 返回 orderId/orderNo
- `create` 套餐不存在 → 抛 404
- `create` 跨套餐购买 → 抛 400
- `create` 存在未支付订单 → 抛 400
- `cancel` 正常取消 → status=2
- `cancel` 已支付订单 → 抛 400
- `cleanExpired` 扫描过期订单 → 受影响行数

#### 11.1.2 `service/__tests__/payment.test.ts`（≥ 6 case）

- `create` 正常 → 返回 paymentNo
- `create` 订单已支付 → 抛 400
- `handleCallback` 正常成功 → status=1 + 触发 activatePaidPlan
- `handleCallback` 重复回调 → skipped=true
- `handleCallback` 金额不一致 → 抛 400
- `handleCallback` 验签失败 → 抛 400
- `markFailed` 正常失败 → status=2 + 通知

#### 11.1.3 `lib/payment/__tests__/mock.test.ts`（≥ 4 case）

- `createPrepay` 返回 mockToken + cashierUrl
- `verifyCallback` 正常 → success=true
- `verifyCallback` invalid JSON → success=false
- `verifyCallback` missing paymentNo → error message

#### 11.1.4 `schedule/__tests__/memberExpireCheck.test.ts`（≥ 3 case，可选 mock 时间）

- `_scanExpireSoon(7)` 找到目标用户 → 触发 send（用 jest.spyOn(notification.core, 'send')）
- `_scanExpired` 找到过期 → update is_paid=0
- 幂等键格式正确

### 11.2 前端单测

| 文件 | case | 覆盖 |
|---|---|---|
| `super-tools-admin/src/services/__tests__/order.test.tsx` | ≥ 5 | listOrders / getOrder / getOrderStats / 边界 |

H5 不强制单测（与 C2a/C2b 一致，依赖手工验收）。

### 11.3 后端 PowerShell smoke（联调用）

```powershell
$body = '{"username":"admin","password":"Admin@123456","clientId":"admin_client","clientSecret":"ADMIN_SECRET"}'
$loginRes = Invoke-RestMethod -Uri "http://localhost:7001/api/auth/login" -Method POST -Body $body -ContentType "application/json"
$token = $loginRes.data.accessToken
$headers = @{ "Authorization" = "Bearer $token" }
$jsonHeaders = @{ "Authorization" = "Bearer $token"; "Content-Type" = "application/json" }

# 1. 用户用普通账号登录（避免给 admin 加会员记录）
$userBody = '{"username":"normaluser","password":"User@123456"}'
$ulogin = Invoke-RestMethod -Uri "http://localhost:7001/api/auth/login" -Method POST -Body $userBody -ContentType "application/json"
$utoken = $ulogin.data.accessToken
$uHeaders = @{ "Authorization" = "Bearer $utoken" }
$uJsonHeaders = @{ "Authorization" = "Bearer $utoken"; "Content-Type" = "application/json" }

"--- (1) 创建订单 ---"
$createRes = Invoke-RestMethod -Uri "http://localhost:7001/api/orders" -Method POST -Headers $uJsonHeaders -Body '{"planCode":"monthly"}'
$orderId = $createRes.data.orderId
"orderId=$orderId, orderNo=$($createRes.data.orderNo)"

"--- (2) 创建支付 ---"
$payRes = Invoke-RestMethod -Uri "http://localhost:7001/api/payments" -Method POST -Headers $uJsonHeaders -Body "{`"orderId`":$orderId,`"provider`":`"mock`"}"
$paymentNo = $payRes.data.paymentNo
"paymentNo=$paymentNo"

"--- (3) Mock 回调成功 ---"
$amount = 6.8
$callbackBody = "{`"paymentNo`":`"$paymentNo`",`"amount`":$amount}"
Invoke-RestMethod -Uri "http://localhost:7001/api/payments/mock/notify" -Method POST -Body $callbackBody -ContentType "application/json"

"--- (4) 查支付状态（应为 1 success） ---"
(Invoke-RestMethod -Uri "http://localhost:7001/api/payments/$paymentNo/status" -Headers $uHeaders).data.status

"--- (5) 查订单详情（应为 1 已支付） ---"
(Invoke-RestMethod -Uri "http://localhost:7001/api/orders/$orderId" -Headers $uHeaders).data | Select-Object orderNo, status, paidAt

"--- (6) 查会员状态（应为 isPaid=1） ---"
(Invoke-RestMethod -Uri "http://localhost:7001/api/member/info" -Headers $uHeaders).data.paid

"--- (7) 重复回调（应 skipped） ---"
Invoke-RestMethod -Uri "http://localhost:7001/api/payments/mock/notify" -Method POST -Body $callbackBody -ContentType "application/json"

"--- (8) 跨套餐购买（应 400） ---"
try {
  Invoke-RestMethod -Uri "http://localhost:7001/api/orders" -Method POST -Headers $uJsonHeaders -Body '{"planCode":"yearly"}'
} catch { $_.ErrorDetails.Message }

"--- (9) 同套餐续费（应 200，scene=2） ---"
$renewRes = Invoke-RestMethod -Uri "http://localhost:7001/api/orders" -Method POST -Headers $uJsonHeaders -Body '{"planCode":"monthly"}'
"scene=$($renewRes.data.scene)"

"--- (10) 取消刚创建的续费订单 ---"
Invoke-RestMethod -Uri "http://localhost:7001/api/orders/$($renewRes.data.orderId)/cancel" -Method POST -Headers $uHeaders

"--- (11) 我的订单列表 ---"
(Invoke-RestMethod -Uri "http://localhost:7001/api/orders?page=1&pageSize=10" -Headers $uHeaders).data.list.Count

"--- (12) 管理端订单统计 ---"
(Invoke-RestMethod -Uri "http://localhost:7001/api/admin/member/orders/stats" -Headers $headers).data

"--- (13) 通知接收（应有 BUSINESS_PAYMENT_SUCCESS + BUSINESS_PLAN_ACTIVATED + BUSINESS_POINTS_CHANGE）---"
(Invoke-RestMethod -Uri "http://localhost:7001/api/notifications?page=1&pageSize=5" -Headers $uHeaders).data.list | Select-Object id, typeCode, title
```

---

## 12. 验收清单

### 12.1 后端

- [ ] 021/022/023 三个迁移脚本执行无错（`npm run db:migrate` 或手动 source）
- [ ] `member_orders` / `member_payments` 表结构与 spec 一致
- [ ] PaymentProvider 工厂可正确分发（mock 实装、wechat 占位 throw）
- [ ] 11 个 PowerShell smoke 全部通过（含跨套餐拦截 400、重复回调幂等）
- [ ] 单测 ≥ 19 case PASS（order 6 + payment 7 + mock 4 + schedule 3 - 重叠）
- [ ] `npm run dev` 启动无错，schedule 正常注册

### 12.2 H5

- [ ] `/member` 页面：未付费时所有套餐可点 / 已付费时同套餐"立即续费"+ 其他套餐禁用
- [ ] 点订阅 → 跳收银台 + URL 含 `paymentNo`
- [ ] 收银台模拟成功 → 订单详情显示已支付 + 收到 3 条通知（支付成功/套餐开通/积分变动）
- [ ] 收银台模拟失败 → 订单详情仍可重新支付
- [ ] "我的订单"列表 Tab 切换 / 上拉加载 / 下拉刷新
- [ ] 订单详情底部按钮根据 status 正确显隐
- [ ] mine 页菜单"我的订单"入口正常跳转

### 12.3 管理端

- [ ] `/member/orders` 列表正常加载
- [ ] 4 张统计卡显示正确数据
- [ ] 详情抽屉显示订单 + 支付流水
- [ ] AuthButton 包裹 + RBAC 控制（用 operator 账号能看，不能写——本 MVP 无写）
- [ ] 测试 ≥ 5 case PASS

### 12.4 调度

- [ ] 手工触发 `memberExpireCheck` 模拟会员到期 7d/1d/已过期 → 收到对应通知
- [ ] 重复触发 → 不发重复通知（幂等键生效）
- [ ] 创建订单 → 等待 30 分钟 → `orderExpireCheck` 自动置 status=3

### 12.5 闭环验收

完整闭环（端到端）：
1. 新用户登录 H5 → /member 看到"未付费会员" + 套餐列表
2. 选月卡 → 跳收银台 → 模拟成功
3. 跳订单详情 → 显示已支付
4. 退回 /member → 头部显示"月度会员（30 天）"
5. /notifications 看到 3 条通知（支付成功/套餐开通/积分 +50）
6. /mine 头部显示积分余额已增加
7. 管理端 /member/orders 看到该笔订单
8. 7 天后定时器触发 → 用户收到"还有 23 天到期"通知（手工模拟可调整 paidExpireAt 测试）

---

## 13. 风险与边界

### 13.1 技术风险

| 风险 | 等级 | 缓解 |
|---|---|---|
| 重复回调（同 paymentNo 多次回调） | 高 | service 层 `SELECT FOR UPDATE` + status 校验 + 幂等返回 skipped |
| 金额被篡改 | 高 | service 层校验 `payment.amount === verify.amount`（差值 < 0.001） |
| 跨套餐购买绕过前端 | 中 | service 层强制校验，前端只是体验优化 |
| schedule 多实例重复跑 | 中 | egg-schedule 默认 `type='worker'` 是单进程；如多实例部署需用 `disable: { scheduler: 'master' }` |
| 通知模板缺失导致 send 失败 | 中 | spec 要求 023 迁移先于代码上线；activatePaidPlan / addPoints 已 try/catch 兜底 |
| MockProvider 被生产环境误启用 | 高 | 配置 `enabled_providers` 在生产环境去掉 mock；前端仅渲染 enabled 列表 |
| activatePaidPlan 内部事务与 handleCallback 外部事务嵌套 | 中 | 拆分：handleCallback 内事务只更订单/支付状态；activatePaidPlan 在事务外调用（自带独立事务） |
| 订单 expireAt 与支付完成时间存在竞态 | 中 | 回调时优先看 `payment.status`，订单过期不阻断支付成功（但记 audit warn） |
| H5 模拟收银台用户停留过久后订单已过期 | 低 | 前端轮询 + 倒计时；超时跳订单详情提示 |
| 用户付款成功但通知模板渲染异常 | 低 | notification.core.send 已有 try/catch；订单/支付主流程不受影响（仅日志 warn） |

### 13.2 业务边界

| 边界场景 | 处理 |
|---|---|
| 用户付款成功但断网，未跳转详情页 | `getPaymentStatus` 轮询 + 服务端已成功（用户下次进入订单看到已支付） |
| 用户连续点"立即支付"创建多笔订单 | service.create 强校验"未支付订单"，第二次抛 400 |
| 用户在收银台同时点"成功"+"失败" | 第一次回调成功后 status=1，第二次 markFailed 因 status=1 跳过 |
| 同 order 多笔 payment 都未成功 | 用户重新创建 payment 即可，旧的保留 status=2 作为审计 |
| 续费时旧套餐还有 5 天 | activatePaidPlan 已正确处理（baseDate = max(currentExpire, now)） |
| 套餐被管理员下架（status=0）后用户已下单未支付 | service.payment.create 会校验订单 expire_at，但不会校验套餐——故意保留（订单基于快照） |
| 用户支付前管理员改了套餐价格 | `planSnapshot` 字段保留下单时快照；订单金额不受影响 |

---

## 14. 文件增量统计

| 模块 | 新增 | 修改 | 估行 |
|---|---|---|---|
| 数据库迁移 | 3 | 0 | ~280 |
| 后端 lib/payment | 6 | 0 | ~250 |
| 后端 model | 2 | 0 | ~100 |
| 后端 service | 2 | 1（member.ts +20） | ~570 |
| 后端 controller | 3 | 0 | ~220 |
| 后端 router | 0 | 1（+13 行） | +13 |
| 后端 schedule | 2 | 0 | ~180 |
| 后端 单测 | 4 | 0 | ~400 |
| H5 service/types/store | 3 | 1 | ~250 |
| H5 pages | 4 新（+ 1 改 mine） | 1 改 + 1 改 routes | ~700 |
| 管理端 service/pages/test | 5 | 1 改 routes | ~480 |
| **合计** | **34 新建 / 5 修改** | | **~3450** |

---

## 15. 与现有 spec 的对齐

| 维度 | C2a/C2b | 本 spec |
|---|---|---|
| 测试扩展名 | .tsx（避开 ts-jest 26 + TS 5.4） | 同（admin 测试用 .tsx；后端 .ts） |
| AuthButton 包裹 | 操作列每个 action | 本 MVP 管理端只读，不需 AuthButton |
| 审计埋点 | 写操作前后调 audit.log | 用户端订单/支付不写 audit（用 payments 表本身追溯）；管理端只读不需要 |
| paginate helper | 已有 | 复用 |
| 通知 send | 已有 | 复用 |
| activatePaidPlan | 已有 | 微改（接 orderId 参数） |

---

## 附录 A：未来扩展接入清单

接入真实微信支付 JSAPI 时，仅需：

1. 实现 `app/lib/payment/wechat-jsapi.ts`：
   - `createPrepay`：调微信 v3 API `POST /v3/pay/transactions/jsapi`，返回 `prepay_id`
   - `verifyCallback`：用微信平台证书验证 `Wechatpay-Signature` 头 + 解密 AEAD-AES-256-GCM
   - `queryStatus`：调 `GET /v3/pay/transactions/out-trade-no/{paymentNo}`
2. 配置 `system_configs.payment.enabled_providers = ["wechat_jsapi"]`
3. 配置商户号/AppID/APIv3Key/证书路径（建议放 `config.local.ts`，加 `is_secret=1`）
4. 暴露 `POST /api/payments/wechat/notify` 真实回调（router 已注册，仅需取消 controller 内 `throw 501`）
5. H5 收银台改用 `WeixinJSBridge.invoke('getBrandWCPayRequest', prepayData, ...)`

**业务代码（OrderService / PaymentService 主流程）零改动**——这是 Q3=D 选型的最大收益。

---

**结尾**：本设计文档待 user review 通过后即进入实施计划撰写。下一步预计产出 `2026-05-23-会员套餐订阅与支付MVP实施计划.md`。



